# Vision Catalog

Order: Template → OCR → Detector → Qwen. Vision never clicks; normalized targets become pixels only at the Executor boundary.

- `ReplayVision`: exact reviewed mapping, explicitly `SIMULATION`.
- `SignatureVision`: perceptual L1 gate with unknown rejection.
- `SemanticROIVision`: semantic matching in normalized ROIs.
- `SemanticWorldVision`: WorldState for Gather, Build, Research, Training, Intel, Beast, Daily, Alliance, Mail, and Exploration.
- SHA-256/pHash/dHash cache, deduplication, validation, and normalized extraction.

Evidence: 964 audited screenshots, 110 replay samples, 316 semantic ROI templates, 51 verified visual semantics, and 0 Production templates. Live coverage now includes title-first Intel mission routing, ordinary Beast, Firebeast, Rescue Survivors, Hero Journey failure, Master Bounty power blocking, cross-tab Mail reward/all-clear transitions, Exploration claimable/reward/disabled transitions, verifier-clean bounded Gather starts, full-queue stopping, exit-popup recovery, and correct HOME↔MAP navigation. Hybrid Vision delegates dynamic counters—including stamina, Ally Gift badges, and march counts 3/6 through 6/6—to OCR; a narrow red-badge detector handles variable Mail tab counts when OCR misses a digit. General Vision/OCR rates remain unmeasured.

The moving world-map background caused one `BTN_OPEN_RESOURCE_SEARCH` pHash miss at 5/6. The action was not executed. That screenshot is now a Replay sample and Candidate template; only this fixed-position, post-verified control receives a distance tolerance of 12, while all other semantics keep the stricter global threshold.

UNKNOWN triggers rescreenshot, knowledge lookup, bounded recovery, or `BLOCKED`—never a guessed click.
