# Test Report

Run date: 2026-09-07  
Mode: `UNIT + REPLAY(SIMULATION) + PRODUCTION LIVE EVIDENCE`

- Full suite: **165 passed, 0 failed**.
- Replay corpus: 110 reviewed samples, always `SIMULATION`.
- Dataset: 964 screenshots = 583 candidates, 381 duplicates, 0 damaged.
- Templates: 316 normalized ROI candidates; 0 Production.

Covered contracts include hashes/deduplication, resolution normalization, device boundary, strict Qwen JSON, UNKNOWN safe-stop, payment hard-block, resource logging, bounded recovery, single Brain/registry flow, semantic Vision, and dedicated Gather/Build/Research/Training/Intel/Beast/Daily/Alliance Verifiers.

Targeted RapidOCR evidence: Research, Training, Intel, Daily, and Alliance classification remains calibrated. Research node/progress/timer/queue, Alliance attempt/reward/contribution values, Daily task progress/activity, and Training timer/batch are structured. The new 9-screen Hybrid Vision chain passed while template-only dynamic-number verification was rejected.

| Live capability | Result |
|---|---|
| GATHER_RESOURCE | 3/3 consecutive acceptance; 5 total |
| BUILDING_UPGRADE | Warehouse 26→27, 1/1 |
| RESEARCH | BLOCKED: `病房扩建VII 2/3`, rechecked at 5d00:55:44 remaining |
| TRAIN_TROOPS | Infantry T10 806-unit batch started, 1/1; timer 10:03:12 |
| INTEL | 10 ordinary Beast + 2 Firebeast + 1 Rescue Survivors missions completed and claimed; special missions are separately gated |
| BEAST_HUNT | level-22 Great Horned Deer plus four low-level stamina-priority hunts; moving-target positive/negative gates pass |
| DAILY_HERO_RECRUIT | free recruit + claim, activity 270→280 |
| DAILY_CLAIM_REWARDS | 3/3 live cycles; latest Alliance task 3/5→5/5, activity 65→70 |
| ALLIANCE_TECH_CONTRIBUTE | 5/5 across two sessions; Meat 50,000; 49,080→50,040; base/critical rewards verified |
| ALLIANCE_HELP | `NOT_AVAILABLE/AUTO_ACTIVE`, switched task |
| ALLIANCE_GIFTS | Victory Loot 2/3; reset interruption rejected and recovered |
| ALLIANCE_ALLY_GIFT_CLAIM | 9/9; level-1 +30 and level-3 +120 variants; live-loop clear-state SAFE_STOP passed |
| LiveRuntime GATHER start | Nine verifier-clean bounded dispatch runs; latest control-panel 5/6→6/6 and automatic zero-click full guard passed |
| Full march queue | 6/6 produced `SAFE_STOP/no_idle_march`; zero action executed |
| Search Vision variance | 5/6 map miss blocked the click, became Replay/Candidate, then live retry passed |
| MAIL_CLAIM_REWARDS | Latest sweep cleared 19 badges (Report 10 + Alliance 4 + System 5); reward feedback and all-clear state verified |
| EXPLORATION_IDLE_CLAIM | 1/1 live; 9-hour reward feedback plus disabled post-state |

Production multi-task evidence now proves `TRAIN_TROOPS SUCCESS → WOOD march 1/6→2/6 → GATHERING`. The new gather has a displayed 09:30:59 duration, so it is recorded as `IN_PROGRESS`, not another completed gather cycle.

Second Production chain proves `DAILY alliance task 3/5 → two Alliance Technology contributions → 5/5 CLAIMABLE → activity 65→70 → next tier 5/20`, with Alliance attempts and contribution totals verified independently.

The production loop now performs bounded screenshot→Hybrid Vision→Brain→single Scheduler→semantic Executor→Verifier cycles. It supports goal-directed HOME/MAP navigation and a verifier-gated Gather start. UNKNOWN transition frames trigger observation-only refreshes, never duplicate actions.

Every verified live action and execution failure now automatically appends an Episode. A real semantic-target rejection was recorded without clicking; after adding the reviewed current-client search template, the retry recorded four verifier-passing Episodes and moved the march count from 3/6 to 4/6.

The desktop task selector no longer uses ambiguous checkbox glyphs. `✓ 已启用` means execute, `○ 未启用` means do not execute, and unsupported tasks are disabled as `◇ 待接入`.

Mail is now a real unattended panel task. The final Production chain closed an open resource-search overlay, returned MAP→HOME, opened Mail, verified every tab clear, then continued through Intel → Beast → reserved-march Gather fallback. A single execution-lane lock prevents duplicate runtime threads, while goal isolation prevents Mail/Intel/Beast from inheriting Gather actions.

The Windows control-panel audit additionally passed 42 targeted tests and a 165-test warning-as-error full suite. Live desktop validation proved that Runtime and ADB children no longer create visible console windows, a second launcher invocation is rejected by the named single-instance guard, the GUI retains only concise event summaries, and every chained task leaves `运行中` for `已检查` at completion.

The startup bootstrap now distinguishes a supported Gather page from a known-but-unsupported game page and a transient UNKNOWN launch frame. It performs bounded wait/back recovery and raises an explicit recovery failure rather than starting a run that can only stop at `unknown_page`.

The upgraded desktop panel was itself used for a Production proof: it restored Gather + continuous mode, filled `5/6→6/6`, wrote four Episodes, automatically launched the next observation, safely executed zero actions on the full queue, and changed its next interval to 10 minutes. Evidence: `evidence/control_panel_continuous_production.json`.

The latest panel run exposed and fixed a Windows screenshot overwrite conflict by assigning every unattended run a timestamped evidence directory. The live retry then completed Intel check → Beast check → Wood Gather 4/6→5/6, followed by a zero-click `reserved_march_for_stamina` guard. Control-panel targeted tests remain 11/11 passing.

The reward verifier rejects mail without attachment feedback and rejects Exploration when the post-claim control remains enabled. Non-reward red dots are classified but never counted as successful claims.

Not yet proven: general OCR/general Vision accuracy, Maa-native execution, cross-page unattended endurance, multi-role, an actual Research start, or any `STABLE` skill.

Special Intel regression now proves that the shared `前往查看` button cannot collapse all mission types into ordinary Beast. `英雄之旅 等级10` is recorded from a real failed battle and remains blocked pending formation research. `大师悬赏：20号` is blocked before dispatch because the live recommendation (189,295,920) materially exceeds observed chief power (56,089,968).

Current-client Firebeast regression distinguishes its orange mission pin, mission dialog, and level-20 target from ordinary Beast and Master Bounty before reusing the verified victory-gated march chain. Rescue Survivors regression proves the orange shelter pin, mission dialog, displayed 12-stamina target, active state, claimable transition, reward popup, and cleared claim.
