# 2026-09-15 工作流水

## 情报循环自动化运行（00:01 GMT+8 / 2026-09-14T16:01Z）

- 设备：MuMu 已连接（`already connected to 127.0.0.1:7555`），前台 `com.gof.china`，720×1280。
- 执行：`tools/run_intel_loop.py 6`。
- 结果：3 轮全部 `stop_reason=intel_not_available`，exit 0，verifier PASS；
  dispatches 0 / claims 0；情报板读数 stamina=162（未变化，无派兵无领取）。
  第 3 轮后脚本主动停止（"intel list stayed empty for 3 consecutive cycles"）。
- 证据：`evidence/intel_loop_20260914_160145.log`、`learning/runtime_snapshot.json`
  （page=INTEL, status=NOT_AVAILABLE, available_count=0, list_read=true）。

## 关键发现：`intel_not_available` 判据被真机对照证伪

对本次循环截图跑生产视觉（`production_vision().observe()`）拿到 OCR 全量文本：

```
情报 / 162 / 下次刷新：07：53：52 / 满级 / 10
```

只有表头，模板层 → `UNKNOWN`，OCR 回落按 `has_header` 判 `NOT_AVAILABLE`。

对照帧 `live_intel_full_run6_step_001_before_20260914T082814883105.png` 的 OCR 全量：

```
情报 / 305 / 下次刷新：07:31:46 / 满级 / 40
```

**结构完全相同、同样没有 `前往查看`**，但该帧是真有可派任务的（vision 判 AVAILABLE/BEAST，
随后 episodes 里 `DISPATCH_INTEL_BEAST` 真派兵 + `INTEL_CLAIM_REWARDS` 真领奖）。
⇒ `has_header ⇒ NOT_AVAILABLE` 不成立；该 stop_reason 会静默掩盖"有任务但模板漏检"。

已写入长期记忆 `.workbuddy-ai/memory/MEMORY.md`（含修法方向与 PROMOTION BLOCKED 说明）。
未改代码——按项目闸门只有 1 张正样本帧，且本次为无人值守运行。

## 下一轮建议

1. 判定"情报板是否真空"要用 `tools/run_intel_pins.py` 的逐 pin `card_opened/productive` 证据，
   不要采信 `intel_not_available`。
2. 体力 162 闲置；下一次情报刷新约 2026-09-15 07:59 (GMT+8) / 23:59 UTC。
3. `runtime_snapshot.json` 里 `agent_state=DEGRADED`、`unexpected_worker_exits=15`、
   `watchdog_restart_count=13` 为既有计数，本次未清零，值得单独排查。

---

## 情报循环自动化运行（01:10 GMT+8 / 2026-09-14T17:10Z）— 上一轮结论被本轮坐实并纠正

### 1) `run_intel_loop.py 6` 又一次空转（且理由不成立）

- 设备：`already connected to 127.0.0.1:7555`，前台 `com.gof.china`，720×1280。
- 结果：3 轮全部 `stop_reason=intel_not_available`，exit 0，`steps=1`，
  dispatches 0 / claims 0 / `stamina_before=null`（情报循环期间体力的两条帧读数均为 **175**）。
  第 3 轮后按 `QUIET_CYCLES=3` 自行停止。
- 证据：`evidence/intel_loop_20260914_171035.log`、三张 `..._step_001_before_*.png`。

**关键**：本轮三张连拍帧全部显示情报页是**满板 8 个 pin 的地图**
（purple×3 / orange×3 / blue×2，标题「情报」、`下次刷新:06:49:22`、体力 175、底部 `20 满级`）。
即 `intel_not_available` **又一次是假阴性**——不是"板已排空"。
⇒ 任务书里"报 intel_not_available = 情报板已排空 = 正常终态"这个前提**不成立**，
且它自带前提"列表**确**为空"并未满足，故本轮不能按终态收尾。

### 2) 改用 `tools/run_intel_pins.py`（逐 pin 驱动）→ 真实产出

`E:/dongri-mumu-bot/.venv/Scripts/python.exe -u tools/run_intel_pins.py 8`，13m23s，EXIT=0：

| 项 | 值 |
|---|---|
| pins_processed / skipped | 8 / 0 |
| card_opened | 8/8 = true |
| **派兵 dispatches（DISPATCH_INTEL_BEAST，verifier ok）** | **3** |
| **英雄之旅 INTEL_HERO_DISPATCH（verifier ok）** | **2** |
| **领取 claims（INTEL_CLAIM_REWARDS，verifier ok）** | **7** |
| 体力（情报页表头 OCR，唯一可信读数） | **176 → 107，净 −69** |
| 非产出 pin | 3：pin_00 `INTEL_RESCUE_START_NOT_PROVEN`(exit2)、pin_02 无动作(exit0)、pin_04 `INTEL_BEAST_MARCH_NOT_PROVEN`(exit2) |

证据：`evidence/intel_pins_20260914_171640.json`（10 条 per_pin，含 2 条 nav cycle）。
注：两条 nav cycle 的 `stamina_after` 读出 16 / 36，是**已知的 HUD 体力条在浮层下误读**
（`intel_stamina()` 的注释已记录 295→28、275→2），不入账；只采信情报页表头读数。
收尾帧 `dataset/raw/control_panel/probe/post_pins_state.png`：情报页、体力 107、余 5 pin、
无弹窗无卡死 → 客户端状态干净。

### 3) 根因（与上一轮同源，本次补上"为什么工具对了、循环错了"）

- 情报页是**pin 地图**；任务卡只在**点开 pin 之后**才出现。
- `winter_agent_v2/vision.py` / 模板清单是按"单张任务卡"建的 → 地图上永远命不中；
  template 层给 `INTEL` 但 status `UNKNOWN` → 进 `ocr.py` INTEL 回落分支 →
  只有表头 `下次刷新` ⇒ `NOT_AVAILABLE`。
- `winter_agent_v2/intel_pins.py::intel_pin_centers()`（颜色 blob + 白色图标校验）
  **只被 `tools/run_intel_pins.py` 用，没有接进生产 vision/OCR 路径**（grep 确认）。
  ⇒ 修法方向：让 INTEL 的可用性判据来自 **pin 计数**（`pins>0 ⇒ AVAILABLE`），
  而不是表头文字；`NOT_AVAILABLE` 只允许由"pin 数为 0"或逐 pin `card_opened/productive` 支撑。

### 4) 提升闸门状态（仍未放开，未改代码）

- 正样本：上一轮 1 张（08:28Z run6）+ 本轮 3 张独立真机帧 + 8 条 pin 级
  `card_opened=true` 记录 ⇒ **≥3 正样本已满足**。
- 负样本（**真正空板**的帧）：仍未确立。`evidence/intel_pins_*.json` 里有 6 个会话
  `pins_processed=0`，但那更可能是导航失败而非空板，**不能当负样本用**。
- 结论：仍 `PROMOTION BLOCKED`，本次**未改任何生产代码**（无人值守 + 负样本不足）。

---

## 情报循环自动化运行（06:33 GMT+8 / 2026-09-14T22:33Z）— 第 3 次，首次"体面收尾"

### 1) 前置
`already connected to 127.0.0.1:7555`（未走 launch 分支）；前台 `com.gof.china`，720×1280。
起始真机帧（`intel_loop_20260914_223418_c01_...png`）：情报页 **6 pin**（orange×2/purple×2/blue×2）、
表头体力 **168**、`下次刷新:01:25:40`。

### 2) `run_intel_loop.py 6` 依旧是错的工具（第 3 次复现假阴性）
3 轮 `intel_not_available` / exit 0 / steps 1 / dispatches 0 / claims 0 / stamina null，
4m32s（`out_intel_loop_auto.txt`、`evidence/intel_loop_20260914_223418.log`）。
**同一时刻的连拍帧是满板 6 pin** ⇒ 与 00:01、01:10 两次同源，判据仍不可信。

### 3) 改跑 `tools/run_intel_pins.py 8` → 真实产出（6m31s，EXIT=0）
| 项 | 值 |
|---|---|
| pins_processed / skipped | 4 / 0（+1 条 nav cycle） |
| card_opened | 4/4 = true |
| **派兵（verifier ok）** | **DISPATCH_INTEL_BEAST ×2 + INTEL_HERO_DISPATCH ×1** |
| **营救** | EXECUTE_INTEL_RESCUE_SURVIVORS ×1（verifier ok） |
| **领取（verifier ok）** | **INTEL_CLAIM_REWARDS ×4** |
| 体力（情报页表头，唯一可信） | **169 → 157 → 147 → 138 → 128，净 −41** |
| 失败 pin | pin_00（橙·帐篷）`INTEL_BEAST_TARGET_NOT_PROVEN` exit2，如实记录 |

证据：`evidence/intel_pins_20260914_223903.json`。逐 pin 体力读数取自 episodes 里
`OPEN_INTEL` 的 `intel.stamina`（表头 OCR，置信度 0.999）；**pin_02 的 `stamina_after=13`
是 HUD 浮层误读，不入账**。

### 4) 新发现：pin 检测器的长宽比闸门漏检"带光晕"的橙 pin（本轮真正的 bug）
收尾后 `intel_pin_centers()` 返回 **0 pin**，但帧上明明还剩 1 个橙色巨兽 pin。
逐 blob 诊断（`out_auto_pin_diag.txt`）：

```
ORANGE area 4575 w 87 h 136 ratio 0.64 fill 0.39 bbox (170,606,256,741)
```

`intel_pins.py` 的闸门是 `0.65 <= w/h <= 1.3` ⇒ **0.64 差 0.01 被丢**。
原因：橙 pin 的**光晕把 blob 向下拉长**（h 136 而 pin 本体 ~90）→ 比例跌破下限。
⇒ `run_intel_pins.py` 打出的 "no actionable pins left" **本轮也不是"板空"的证据**，
只是检测器漏了一个。

**手工补做该 pin（不是新增子系统，只是把工具漏掉的坐标点开 + 交给同一条生产循环）**：
tap (213,663) 开出的卡是 `大师悬赏：20号`（INTEL_MASTER_BOUNTY / level 20，推荐战力 189M），
`run_live.py --goal INTEL` 判 `intel_master_bounty_power_blocked` → `BACK`（verifier ok），
**未出兵、未花体力、无奖励可领**（卡上只有 `前往查看`，无领取按钮）。
⇒ 收尾时盘面**确实没有可动作的情报任务**，这一次是**有证据的终态**（点开 → brain 读 BLOCKED → BACK），
不是靠表头文字推的。

### 5) 收尾状态与记账
- `dataset/raw/control_panel/probe/auto_final_20260915.png`：情报页、表头体力 **128**、
  `下次刷新:01:13:03`、只剩 1 个被战力阻断的橙 pin、无弹窗。
- `learning/runtime_snapshot.json`：`stop_reason=intel_not_available`、`stamina=128`、
  `unexpected_worker_exits=15`、`watchdog_restart_count=13`（**均未清零、未篡改**）。
- `learning/episodes.jsonl` 1003 行，最新 26 条全部 verifier ok，唯 1 条 FAILURE 已如实保留。

### 6) 下一轮建议
1. 仍**不要**用 `run_intel_loop.py` 判情报可用性；先把 `intel_pin_centers()` 的长宽比下限
   从 0.65 放到 ~0.5（或改用"pin 本体去掉光晕"的量法）——本轮已有 **2 张独立正样本**
   （06:35 帧 + 06:47 帧，同一块光晕橙 pin 均被漏），配昨天的帧可凑齐 ≥3 正；
   负样本仍需专门去拍一张"真空板"帧，补齐前不改生产代码。
2. 只要盘面还有 **非光晕** pin，`run_intel_pins.py <budget>` 就是正确入口。
3. 大师悬赏 20 号（推荐战力 189M）是**长期阻断项**，战力不够前它会一直占着地图——别当失败反复重试。


## 17:5x–18:3x GMT+8 第 17 轮（长期结论已写入 `.workbuddy-ai/memory/2026-09-15.md` 与 `MEMORY.md`）

- **`0ax` 已修**：`SEMANTIC_TARGET_NOT_VERIFIED` 的近因是**客户端把花费画成红色**（付不起），
  不是控件缺失。29 帧实测：成功 25/25 = pHash 距离 0、红像素 0；失败 4/4 = 距离 26、红像素 452。
  复用既有 `unaffordable_cost_pixels()`，新增零行颜色代码。
- **`0ay` 已解释**：6 条 `BTN_DISPATCH` 失败是 `小队设置` 页（绿色「战斗」按钮），修复前历史。
- **`0az` 已修**：BLOCKED 巨兽卡 ⇒ 无人值守静默停摆。先测量 BACK 落点（→ MAP）再落地。
- **`0ba` 新发现（未修）**：`SELECT_INTEL_PIN` 的「pin 耗尽」刻意拒绝也被记成视觉失败。
- **`0ap` 反转**：自动化**确实在跑**（81 条 episode、`dispatches=4 claims=10`，非手动发起）。
- **全量测试**：599 passed / 7 skipped / 73 subtests / 0 failures（JUnit XML 复核）。
- commit：`03ede69`（修复+证据）→ `e4fd245`（checkpoint）→ `e1c8fa3`（handoff 产物）。


## 18:37 GMT+8 操作者要求取消「情报定时循环」

- 用**自动化接口**查得 `43aef0ad-d5bc-4d79-9291-0a4da0b0dc27`「Winter V2 情报循环（每小时）」
  为 `ACTIVE` ⇒ 置为 **`PAUSED`**，并再次 `list` 复核确认。
- **这与 `0ap` 的历史病必须区分**：`0ap` 那 4 次是「接口说 ACTIVE、实际毫无运行」；
  **这次是操作者人为停用**。⇒ 以后看不到新 episode 时**先查 status**，
  不要一律当故障去重建（重建会覆盖操作者的意图）。恢复只需置回 `ACTIVE`，id 与 prompt 都在。
- 已同步更正可能误导下一轮的三处：`handoff/04_OPEN_ISSUES.md`（`0ap` 行）、
  `handoff/10_LAST_HANDOFF.md`（第六轮那节的覆盖声明）、`.workbuddy-ai/memory/MEMORY.md`（外部状态规则）。


## 第 18 轮：操作者纠偏（Intel 冻结 → MAA 优先级）。接管时发现工作树是脏的

### 0. 最重要的动作：把上一会话**未提交的 2 小时工作**先锁进 git

接管时 `git status` 显示大量未提交改动，里面已经躺着 `0ba` **和 MAA 采集生产化**
（`runtime.py` / `brain.py` / `run_live.py` / 新测试 / 9 条新 episode）。
本项目有「外部编辑器回滚未提交改动」的前科 ⇒ **先校验（`check_wiring` problems:0 + 74 passed）
再立刻提交**（`ae9f71b`）。**接管脏树时的第一件事永远是先落基线，不是先读结论。**

### 1. 生产证据推翻了「待办清单」：MAA 步骤 1–3 早已完成

操作者的计划把「MAA Capture Production」列为第一步待做。但逐条查生产 episode：

| 判据 | 实测 |
|---|---|
| `capture_backend = MAA_MUMU_EXTRAS` | **349** 条 |
| `recognition_backend = MAA` | **31** 条（`INTEL_HERO_DISPATCH`/`START_MARCH` = 战斗按钮） |
| `action_backend = MAA` | **128** 条 |
| `executor_backend = MAA / HYBRID` | **74 / 52** 条 |
| 真机采集 | MAA **12.6–15.6ms** vs ADB **404–525ms**（26–42×） |

⇒ **先核实再动手**，否则会把已经做完、只是没提交的工作重做一遍。
`MaaExecutorAdapter` 也已存在且能力齐全（`recognize/find/ocr/wait_page/click/swipe/run_task/save_annotated`）。

### 2. `0bc`：城市视图被判成 `MAP` ⇒ 采集工作流根本无法起步（**很可能是 `MARCH_PAGE_NOT_OPEN` 的真根因**）

想跑步骤 4（OPEN_MARCH_PAGE A/B）时，**三次 `GATHER_RESOURCE` 全在第 1 步死**：
`SKILL_NOT_ENABLED_FOR_LIVE_LOOP`。链条：城市视图 → vision 判 `MAP` →
`march_used=None`（跨帧持续，非单帧遮挡）→ 大脑选 `CHECK_MARCH` →
该技能 `verifier=None`、不在 `VERIFIED_ATOMIC` → 运行时拒绝。

**这就是 `START_GATHER` 从未在 MAA 上跑过的原因**，也解释了为什么「步骤 4 无法测量」。

已修的一半（`f9aa8be`）：`verify_march_count_readable` + 登记进 `VERIFIED_ATOMIC`。
真机 before/after 证据：
`execution=null / stop=SKILL_NOT_ENABLED_FOR_LIVE_LOOP` → `executed=true / verifier=MARCH_COUNT_NOT_READ`。

**没修的一半**：计数在城市视图上就是不画 ⇒ 采集仍起不来。城市 vs 世界地图的判定
（城市有底部导航 探险/英雄/背包/商店/联盟/城镇 + `城镇` 按钮）是**独立的页面分类调查**。

### 3. `0bd`：本机全量测试的「假红」——宿主批量删除闸门，不是回归

`pytest tests -q` 在 28–50% 出现 26 个 **setup 阶段**的 `E`。堆栈进入宿主 shim
`sitecustomize.py → _check_bulk_delete_guard → SystemExit(1)`：pytest 清理
`%TEMP%\pytest-of-<user>\garbage-*`（实测 **463** 文件）时触发宿主闸门，
中断污染 fixture teardown，级联出 25 个 `AssertionError: assert not self._finalizers`。

- **判定证据**：可疑文件**单独跑 12 passed / 0 error**。
- **可复现跑法**：`PYTEST_DEBUG_TEMPROOT=<项目内>` **加上** `--basetemp=<项目内>`。
  **光有 `--basetemp` 不够** —— pytest 仍会去清 `%TEMP%` 下的历史目录。
- ⇒ 以后看到 `E` **先怀疑这个**，判定方法就是**把可疑文件单独跑一遍**。

### 方法论（本轮）

1. **计划可能与事实不符，先查生产证据再动手。** 用户给的顺序里前 3 步已完成；
   若照计划重做，就是白花两小时。
2. **接管时第一件事是落 git 基线，不是读 handoff。** 「未提交 = 随时可能消失」。
3. **同一个失败数字（`SKILL_NOT_ENABLED_FOR_LIVE_LOOP`）底下是集成裂缝，不是游戏事实。**
   与 `0ax`/`0ba` 同类：把内部缺陷当成一次运行的结果来汇报。
4. **`E` ≠ 回归**，先单独跑一遍再下结论。
