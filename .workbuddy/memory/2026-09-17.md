# 2026-09-17 — 工作日志（宿主会话记录）

## R27：训练路线真机跑通（7 步 → 4 步）；建筑入口找到但没落地；情报领到奖励却在弹窗识别翻车

操作者要求：「建筑和训练优先落地，继续，情报记得及时做」。

### 训练（TRAIN）✅ 路线 4 跳全部真机 verifier OK，收敛 4 步（原 7 步），exit 0

```
1 OPEN_POWER_OVERVIEW    HOME -> POPUP/POWER_OVERVIEW         OK
2 OPEN_POWER_DETAILS     POPUP/POWER_OVERVIEW -> POWER_DETAILS OK
3 NAVIGATE_INFANTRY_CAMP POPUP/POWER_DETAILS -> HOME(camp focus) OK
4 OPEN_INFANTRY_TRAINING HOME(menu_open) -> TRAINING           OK
5 SAFE_STOP              training_queue_busy
```

**关键判断**：这条链路的**决策、verifier、goal 入口早就全存在**；唯一断点是
**模板全裁自 2026-09-08 的另一个账号** ⇒ `world.training` 永为空 ⇒ 分支不可达。
⇒ **本轮没改大脑、没改 verifier，只换模板 + 收一处门禁。**

⚠ **自纠（重要）**：第一版结论"战力入口是值依赖模板、必须重裁"是**错的**。
`skills.py` 点的是 `BTN_OPEN_POWER_OVERVIEW_ICON`（**只裁图标的既有记录**），
**今天就能命中 d=4**（09-08 账号 d=10、门禁 12）⇒ "只裁图标、避开会变的数字"**早就有人做过**，
只是换了名字；我按错误的语义名 `BTN_OPEN_POWER_OVERVIEW` 量出 MISS 就下了结论。
那条多余记录**没有动作引用**，已删除，改由测试钉住事实。
**⇒ 教训：判定控件"坏了"之前，先找到动作真正指向的语义名。**

真正的断点 = `POPUP_POWER_OVERVIEW` / `BTN_OPEN_POWER_DETAILS` / `POPUP_POWER_DETAILS` /
`BTN_POWER_TROOP_IMPROVE` 四条 MISS + 兵营菜单门禁（普通城市帧 d=16 落在旧门禁 17 之内）。

**真实缺陷：兵营聚焦浮层是动画**（引导手指 + 呼吸光圈）。旧裁剪 7 帧只中 3 帧
⇒ verifier **连刷 21 秒**（07:41:18→29→39），而浮层寿命约 **2 秒** ⇒ 下一步观测时已消失
⇒ **路线被迫重走**。新裁剪 = **以训练按钮为圆心的 300×300**（点击落点 = ROI 中心；
执行器只有 `TAP_SEMANTIC`，没有绝对坐标动作），7 帧全 0–8 ⇒ 第一次观测即通过 ⇒ 4 步。

**门禁 17 → 12**（全语料 3522 帧）：≤8=17 帧 / ≤12=22 帧 / ≤17=36 帧，而 **plain HOME = 16**（落进 17）。
≤12 那批抽查 OCR 全是兵营菜单帧；09-08 菜单帧（新裁剪 d=18）靠旧记录 d=0 仍命中，不丢召回。

### 建筑（BUILD）⚠️ 入口找到，**故意不落地**

`建筑实力 提升`(605,550) → 聚焦 **民居1（3级）** + 家具面板 + `升级`（157.7万肉 vs 当时 16.6万 ⇒ 禁用）。
**不落地理由**：`vision.py` 的 `BTN_BUILD_UPGRADE` 分支**写死** `{STOREHOUSE,26,27}`
⇒ 一注册就会为任何被聚焦的建筑编造该身份，而 `verify_building_upgrade` 正是按它判定
⇒ **会验证通过没发生的事**。先让身份从画面读（问题 #21）。

### 情报（INTEL）✅⚠ 两次运行都真拿到价值，两次都倒在同一处弹窗识别

第二次补跑走得远：`SELECT_INTEL_PIN` → `OPEN_INTEL_BEAST_TARGET` →
`INTEL_BEAST_START_MARCH` → `DISPATCH_INTEL_BEAST`（**真派出一队**，体力 237→227）→
`OPEN_INTEL` → `INTEL_CLAIM_REWARDS`（**又领到一份**，未试 pin 6→5）→ 第 7 步失败。
⇒ 6 步真机动作、2 份奖励、1 次真派兵，**只有解除那一步判错，且复现两次**。

**根因已量清**：`POPUP_DAILY_REWARD_CURRENT` 的 ROI = `x 0.08 y 0.2 w 0.84 h 0.4`
（**整个奖励格区域**）⇒ 对**任何来源**的奖励弹窗都命中（两次实测 **d=6 / d=2**），
带来源信息的 `POPUP_GENERIC_REWARD_HEADER` 是 12/16；`vision.py` 1046 行又排在通用表头**之前**
⇒ 报成 `DAILY_REWARD` ⇒ 跑每日的解除技能。**该裁剪在结构上无法区分来源。**
大脑已有 goal 上下文分支（brain.py 221–232）；**改前必须先量真每日奖励那一侧**（问题 #22）。
**禁止调阈值修。**

### 新增产物

- 工具：`tools/probe_power_route.py`（`--tap x,y` 逐跳 + 每跳留帧 + 全量 OCR token +
  `--allow-page` + `--leave`）、`tools/register_power_route_templates.py`、
  `tools/probe_camp_menu_gate.py`（全语料两总体）。
- 测试：`tests/test_power_route_templates.py`（14 项）。
- `tools/check_wiring.py`：+11 条（6 大脑决策 + 5 verifier 绑定）——**原文件对这条链路零覆盖**。
- 证据：`dataset/truth_audit/power_route_20260917/`（`key/` 12 帧进公开仓库，其余 ~19 帧留本地）。
- `.gitignore` 白名单只放行 `key/**`。
- 未决问题 #21 建筑身份写死 / #22 情报弹窗误读 / #23 TRAIN 结束停训练页 / #24 troop_type 恒 INFANTRY。

### 环境注意

- `--goal TRAIN` 以 `training_queue_busy` 收尾会把客户端**留在训练页**；下一次换 goal 会立刻
  `goal_page_mismatch`（本轮亲历：`--goal INTEL` 第一次只走了 1 步）。跑下一个 goal 前先确认页。
- 本机 pytest 全量拿不到汇总行 ⇒ 用 `-o tmp_path_retention_policy=all` 或 `PYTEST_PLUGINS=winter_failwatch`。
- repo 仍可能有第二个写入方：改完立刻 `git log --oneline -3`。

## 续：研究（RESEARCH）路线真机跑通 —— 第二个 BLOCKED 目标打通

`--goal RESEARCH` exit 0，`OPEN_POWER_OVERVIEW → OPEN_POWER_DETAILS → NAVIGATE_RESEARCH_LAB
→ OPEN_RESEARCH → SAFE_STOP research_page_no_startable_node`，**4 跳 verifier 全 OK**。
还是那条战力路线，只是取 `科技实力` 那一行。**与训练同一模式**：路线早记录在
`knowledge/skills/RESEARCH_RESEARCH.md` 第 38 行、`GOAL_REQUIREMENTS` 早点名
`("OPEN_RESEARCH","RESEARCH")`、verifier 早绑定 —— 缺的只是真机模板 + 决策。

修的两处：① 旧 `BTN_OPEN_RESEARCH` ROI 中心 **(536,840)** 不在控件上（实测按钮中心 **(478,878)**）⇒
即便匹配成功也点偏 ~65px；新裁剪 = 以按钮为圆心的 340×340，正样本 0/8/8、负样本全不中。
② RESEARCH goal 完全没有导航。

**没有声称能"开始"研究**（无可开始项、节点/花费读数不存在、`BTN_START_RESEARCH` 零模板）；
停止是具名的 `research_page_no_startable_node`。

⚠ **导航事实 ≠ 队列事实**：城市帧只写 `menu_open`，不写 `queue_available`
（隔壁训练分支硬编码 `True`，goal library 会读成"队列空闲"）。注册表 88 → 90。

## 续（12:1x–13:0x）：#22 关闭 + #23 落地 + 工作队列重建

**#22 已修（真机验证）**：根因**不是阈值** —— `POPUP_DAILY_REWARD_CURRENT` 的 ROI 是**整个奖励格区域**
（`x .08 y .20 w .84 h .40`），识别的是**内容**而非**来源**，且排在通用横幅之前。
在 103 帧有标签生产帧上量四组总体，**只有页脚 `BTN_DISMISS_INTEL_REWARD` 几乎处处在场（102/103、≤2）**；
**真实每日帧与真实情报帧的页脚 phash=4** ⇒ 同一个「获得奖励」弹窗，来源不在画面里。
修法：先判共用弹窗 → 报目标中立的 `GENERIC_REWARD` → 由 goal 选解除技能。**没碰 verifier**
（五个 `*_reward_dismissed` 早已接受 `GENERIC_REWARD`）。真机 `DAILY_CLAIM_REWARDS` 与
`MAIL_CLAIM_REWARDS` 各真领到，`DISMISS_*_GENERIC_REWARD` verifier OK；**3007 帧闸门 0 假阳性**。

**#23 叶页退出**：实现 + 12 项单测 + 4 条接线检查；**真机 episode 未取到**（叶页存活很短，#27）。
⚠ 第一版守卫写宽了（无 goal 的扫掠在可训练页上不再训练），被 `check_wiring` 既有检查抓住后收窄。

**工作队列重建**：旧 R19 队列归档为 `WORK_QUEUE_20260916_R19_ARCHIVED.json`，新队列 8 单
（INTEL 巨兽目标 / EXIT_CONFIRM 安全 / 建筑身份 / 叶页证据 / 训练两阶段 / ALLIANCE_HELP / ARENA / 机会型）。

**新发现**：#25 点「前往查看」打开「英雄之旅」而非巨兽任务（INTEL 链当前前沿）·
#26 `EXIT_CONFIRM` 在 brain 无分支、只点 `BTN_CLOSE`（安全缺口）· #28 兵营聚焦先出教程手指、径向菜单未画。

**教训**：**模板裁剪覆盖"内容会变"的区域 ⇒ 识别的是内容不是来源**（这是同一坑的第四次）；
**探针的匹配阈值 ≠ 生产阈值**（`SemanticWorldVision=8` vs 独立 `SemanticROIVision=6`），
用独立探针量读数会偏严，差点据此改错方向。

## 操作者定规：任何 Capability 开工前 2 分钟 Reuse Check

两条禁令：**别在有成熟本地实现时去 GitHub 重新研究；别在外部已有成熟实现时自己摸 UI 两小时。**

落点（四处，都是"会被读到"的地方）：`docs/ADDING_A_LIVE_ROUTE.md` §0（本地五查 → 七条升级触发 →
索引优先 → 外部只是 prior）· `START_HERE.md` 第 5 步第 2 条 · **专家包 v1.1.1**（Agent 行为循环插入
强制 REUSE CHECK；`external-capability-mining` §0 重写）· `.workbuddy-ai/memory/MEMORY.md`。

**并落成一条命令**：`tools/reuse_check.py <TERM>` 打印 8 段（registry / brain / verifier+真机历史 /
契约 / knowledge / legacy evidence / 外部卡片 / 判定）。实测 `ARENA` → NO LOCAL PATH（且直接给出
`ArenaRoutine.java`）；`RESEARCH`、`OPEN_MAIL` → LOCAL PATH EXISTS。
新增 `tests/test_reuse_check.py`（8 项）+ `check_wiring` 5 条。

---

## 三层分工 doctrine 落地 + P0：生产链跑在没有 MAA 的解释器上（15:50–16:20）

操作者下达《External Knowledge × MAA × V2 总执行原则》。先核对指令 vs 代码现实：MAA 层**已存在
且是生产默认**，`§2b` 的两轴实测结论比指令更细（识别逐语义按证据），已在新章节 §2c 显式交叉引用。
三层分工 / 统一生产链 / 五步流程 / Fast·Normal·Strict Lane / 15 分钟规则 / 指挥中心边界
已写入 `.workbuddy-ai/handoff/00_MASTER_RULES.md §2c`（宪法）。

查出 P0：正在跑的面板用的是 `codex-runtimes` 内置 Python，**缺 maa / cv2 / rapidocr / adbutils**；
`control_panel.py` 用 `sys.executable` 派生 worker ⇒ worker 一起缺；`.ps1` 启动器 pin 同一个坏解释器
（且 `matchers.py` 顶层 import cv2 ⇒ 该路径根本跑不起来）；`.cmd` 用 Python312 ⇒ 三个解释器并存。
唯一痕迹是 worker stdout 一行 `MAA_IMPORT_FAILED`。

影响诚实界定：台账里 ADB 行**没有一行**属于已提升到 MAA 的 10 个技能 ⇒ **潜在缺陷，不是已付成本**。

修复：`winter_agent_v2/runtime_env.py`（要求表+子进程探测+解析）· `tools/preflight.py` ·
`control_panel.py`（worker 用解析解释器；不合格时拒绝启动并写
`RUNTIME_ENV_NOT_PRODUCTION_READY`，已进 `ENVIRONMENT_FAILURES`）· 两个启动器 ·
`01_CURRENT_TRUTH.md` §J 后端轴 · 新卡 `TOOLING_INTERPRETER_DRIFT.md` ·
`tests/test_runtime_interpreter.py`（21 项）。

真机证据：面板 AUTO 真实命令跑通，`BACK` 走 MAA / MAA_MUMU_EXTRAS，verifier ok=true，exit 0。

教训：**记下来 ≠ 发现得了**（台账记录了降级却没人比对，且台账只记已执行步骤）；
**一个文件一次编辑**（同消息两个 Edit 会互相覆盖，本次丢了 2 处改动）。

操作者 §八 优先清单：BUILD / ALLIANCE_HELP / ARENA / LABYRINTH / PET / VIP / PROMOTE / HEAL /
JOIN_RALLY / START_RALLY / BEAR。

---

## DEVELOPMENT_VALIDATION 策略落地 + 一个规模化契约根因（16:40–18:0x）

策略写入 `00_MASTER_RULES.md §8b`：T0–T4 分级（T2 自动执行 / T3 有界尝试 / T4 不放宽）、
允许主动制造验证条件、15 分钟规则、开发与生产分开。**未新增 config 块**（避免"死键"前科）。
宪法里写明：`config/v2.json` 的 `production: true` 是真机互锁，不是"生产模式"。

**新工具 `tools/verifier_binding_audit.py`（只读可复现）测出：18 个 verifier 写了却插不上插座**
（10 参数型 / 7 帧型 / 4 单态型），因为 runtime 只支持两帧 `(before, after)` 且无任何校验
⇒ "绑不上"与"没写过"无法区分。3 个（BUILD / TRAIN / RESEARCH）已用 lambda 适配器接上。
**A 组 15 个技能"拼图齐了、从未真机成功"**（含 BUILDING_UPGRADE、TRAIN_TROOPS、BEAST 三件套、
ALLIANCE_GIFTS、EXPLORATION_IDLE_CLAIM、DISMISS_MAIL_REWARD…）。
卡：`knowledge/failure_patterns/architecture/VERIFIER_SHAPE_MISMATCH.md`。

三次第一直觉被测量推翻（三元≠三帧 / 18 个不是都绑不上 / 自指注入不都是伪造）⇒ 先枚举再下结论。
旧探针曾把无 `recorded_at` 的导入行算成"4 次真机成功"，新工具已改用 `reuse_check` 的口径。

真机：`--goal MAIL` → `mail_all_clear`（邮箱真空，属自然状态）⇒ **本轮无新 LIVE_VERIFIED，如实记录**。

---

## TRAIN / RESEARCH 缺少「先回城」——已修 + 真机验证（17:42–18:0x）

排查 `TRAIN_TROOPS` 为何 0 次真机尝试，找到最小缺口：命名路线 goal 从**常态停留页 MAP** 起不来。
MAIL / EXPLORATION / DAILY / ALLIANCE 一直有 `MAP → OPEN_HOME`，**TRAIN 与 RESEARCH 没有**
⇒ `--goal TRAIN` 第 1 步零动作 `SAFE_STOP training_entry_not_verified`，退出码却像正常结束（**静默**）。
修：两分支各补 `MAP → OPEN_HOME`（含 resource_search_open → BACK），无关页仍照旧具名停止。
12 项测试 + `check_wiring` 0。

真机：路线现在走通 4 跳 verifier 全 OK（OPEN_POWER_OVERVIEW → OPEN_POWER_DETAILS →
NAVIGATE_INFANTRY_CAMP → SELECT_INFANTRY_CAMP），第 4 步复现开放问题 **#28**
（兵营高亮 + 教程手指，菜单未绘制，点下去落到地图）。**前沿从第 0 步推进到第 4 步**，
无新 LIVE_VERIFIED（如实）。新工具 `tools/probe_return_to_start.py`（只按 BACK、每步取证）。

---

## CAP-D01 训练两阶段真机闭环 + 队列接上执行循环（18:00–18:10）

Stage A（兵营高亮 + 教程手指、菜单未绘制）旧行为是点兵营 ⇒ 落到 MAP。修：新增
`WAIT_FOR_CAMP_MENU`（**OBSERVE、不发送输入**）+ verifier「等待若结束在 MAP 永不算成功」；
Stage B（`menu_open`）仍走 `OPEN_INFANTRY_TRAINING`。真机：7 次 reobserve 全 OK 且
`page_after=HOME`（**跳地图已消失**），限界后 2 次等待 → `SAFE_STOP camp_menu_never_drawn`。
新测量：Stage A 是**持续态**（9 次 menu_drawn=false）⇒ 等待安全但不充分；
`TARGET_INFANTRY_CAMP_HIGHLIGHTED` 的 **ROI 宽 59% 屏宽**、中心不是兵营 ⇒ 需重裁兵营本身。

§六：`EXECUTION_STATE.current_task=null` 的真因是工单 `status` 全为空，而选择器判据是
`status=="READY"` ⇒ `plan` 报 RUNNABLE(0)。已把 7 个工单置 READY、CAP-A02 置 DONE；
现在 RUNNABLE 6→5，队列、current_task、results、下一任务真正串起来。
CAP-D01 已 `finish --require-live` 结算；按操作者 §七 认领 CAP-B01（偏离选择器的 P0 排序，
原因已写进回报）。

---

## CAP-B01 完成：建筑身份从像素读（18:2x–19:5x）

新模块 `winter_agent_v2/building_identity.py`：统一输出
`id / name / level / target_level / identity_confidence / identity_source`；
等级必须**紧邻建筑名左侧同行**的裸整数（同帧的 467/27/113.1万/01:12:50/32/32/任务进度全部被拒）；
名字不在表里 ⇒ `id=UNKNOWN`（名字与等级照实报告）；拼错不做模糊匹配；不完整 ⇒ UNKNOWN。
接入 `HybridVision.observe`（城市帧按实测选择态模板门控 + 对话框按 `Page.BUILDING`），
**删掉 `vision.py` 两条伪造常量**（STOREHOUSE 26→27 / queue_building）。

真机（19:51 GMT+8）点 (280,640) 选中步兵营并打开径向菜单，生产路径得出
`{"id":"UNKNOWN","name":"盾兵营","level":12,"target_level":13,"identity_source":"FLOATING_LABEL_OCR+LEVEL_PLUS_ONE"}`；
对话框帧得到 `{"id":"STOREHOUSE","name":"仓库","level":null,...}`（**不编造等级**）。
测试 25 项（含两帧 replay），相关组 70 passed，`check_wiring` 0。

发现：① 真实营名是**盾兵营**（表里是步兵营）⇒ 映射**故意不加**、返回 UNKNOWN；
② **点建筑本体才能打开径向菜单** —— 这才是 CAP-D01 Stage B 的真修法；
③ 点城市像素可能毫无反应，可点区域仍需逐个量。
边界：身份**未传递**到派发步（需 PARAMETER verifier 形状），建筑面板仍无路由。

---

## WorkBuddy 本机双向集成第一步（gateway 实测 + 最小 bridge）

**连通性实测全部通过**（真机，CodeBuddy 2.137.1）：CLI 不在 PATH，本体在
`E:\work Buddy国内\WorkBuddy\resources\app.asar.unpacked\cli\bin\codebuddy`；
`--serve --port 8080 --session-id …` 可用；`GET /api/v1/health` **需 Bearer**
（无认证 401）；`/api/v1/info` 回报 `cwd=E:\无尽冬日智能体`。
密码用 `CODEBUDDY_GATEWAY_PASSWORD` 固定（实测生效、旧随机密码 401）⇒ **凭据不进仓库**。
`/jobs`（后台）与 `/runs`（交互 SSE）都在，选 `/jobs`。
四个操作 health/submit/status/stop **全部真机验证通过**。

两个**测出来**的默认值：`permissionMode=dontAsk`（探针真的跑了 `git rev-parse HEAD`）、
`bgIsolation=none`（有 worktree 则提交落在临时分支，V2 看不到，升级会"假成功"）。

交付：`winter_agent_v2/workbuddy_bridge.py`（只 4 个操作，纯 stdlib，不读 config/v2.json）
+ `tools/workbuddy_bridge.py`（`--check/--prompt/--submit/--status/--cancel`）。
触发门只有操作者点名的五种条件，**普通 Tick 联网前就被拒**，每次提交写
`learning/workbuddy_escalations.jsonl`。prompt 必带全部上下文，且 **WorldState 标注来源**
（之前无条件写"last live observation"是对不知道出处的值下断言）。

**实测抓到 bug**：`--status` 读了 `args.job_id`（argparse 存在 `args.status`）⇒ 每次真跑
都 AttributeError，而 **48 项单测全绿**。已修 + 加 `CliWiringTest` 驱动 `main()`。
⇒ 被验证的东西必须是真正运行的那个东西。

端到端：submit → `--status` 得到 `BRIDGE_TRANSPORT_OK`/DONE；另一次 submit → `--cancel` → STOPPED。
文档：`knowledge/failure_patterns/integration/WORKBUDDY_GATEWAY_CONTRACT.md`、
`docs/WORKBUDDY_BRIDGE.md`、`00_MASTER_RULES.md §10b`、`START_HERE.md`。
第二阶段（V2 暴露 MCP server）只预留 seam，未实现。

---

## 全量测试抓到两处失败

**失败 1（测试写法）**：`test_evidence_integrity.py` 的正则扫字面量，跨行隐式拼接只截到后半段
⇒ 假阳性。**改用 AST**（隐式拼接本就是单个 `Constant`，且不含注释），删掉"跳过自己"那条特例。

**失败 2（我上轮引入的回归）**：`test_training_verifier.py` 第 4 步期望 `SELECT_INFANTRY_CAMP`。
**决定性测量**：09-08 帧（金色环+手指，点击→菜单）`d=0`；09-17 帧（军官头像，无环无手指，
点击→地图）`d=8` —— **两帧都被读成 `HIGHLIGHTED`，而生产阈值正是 8**。
根因：裁剪 `w=0.59 h=0.285` 框的是**建筑**而不是**高亮**，建筑两帧都在 ⇒ 判别力只剩 8 级。
⇒ 上轮把 Stage A/B 建在了**区分不了两个状态的信号**上。**禁止调阈值修**；
真修法 = 把金色圆环单独裁成紧模板 + 用**反例帧**测距离。
处置：保留"不点击"（安全侧），`TRAIN` 仍 **BLOCKED**（`camp_menu_never_drawn`）；
测试改为**更强**断言（第 4 步 `WAIT_FOR_CAMP_MENU` + 新增"两帧不可区分"的可执行断言）。
新卡 `knowledge/failure_patterns/vision/TEMPLATE_CROP_COVERS_THE_VARIABLE.md`（至少第 4 次）。
证据复制到受保护目录 `dataset/truth_audit/training_camp_highlight_ambiguity_20260917/`。

---

## 升级队列（AUTO → Queue → Bridge → WorkBuddy）

新增 3 处最小改动：`winter_agent_v2/escalation_queue.py`（状态机/去重键
`capability|failure_type|skill`/`max_concurrent_jobs=1`/修复预算+冷却/模型阶梯/对账/
`observe_run()` hook）、`winter_agent_v2/runtime_reload.py`（`RUNTIME_RELOAD_REQUIRED`
标记 + 15 分钟上限）、`tools/escalations.py`。hook 接进 `run_live.py`（`--no-escalate` 可关），
面板 `start()` 加延后闸门。**AUTO 永不同步等 WorkBuddy。**

一个台账 `learning/workbuddy_escalations.jsonl`，状态是事件流 fold；`source=bridge`（传输审计，
无 key）与 `source=queue`（生命周期）区分，**fold 忽略无 key 行**。
UI 不可读按**后缀**判（名单版当天就漏了 `TRAINING_PAGE_NOT_PROVEN`）。
补了 `STOP_REASON_WALLS`：`verified_beast_target_not_visible` 每轮结束运行却 0 失败 episode。
`LIVE_VERIFIED` 只认真机 episode（派发后 + recorded_at + verifier_ok + 截图）；
只改代码只能记 `TEST_PASS` 并写明"NOT a verified capability"。

模型阶梯四个 id **实测全部跑通**；`hy4-preview-f` 识别出探针 prompt 自相矛盾并**拒绝**照抄 token。

**无人值守 AUTO 自己触发了**：`queued: gateway unavailable` →
台账 `QUEUED / UNKNOWN_UI / SPEND_STAMINA_ON_BEAST`（同时证明"WorkBuddy 挂掉不阻塞 AUTO"）。

**两个运维事实**：① 面板进程没有 `CODEBUDDY_GATEWAY_PASSWORD` ⇒ 只能建单不能派发；
② 正在跑的 AUTO 仍报 `MAA_IMPORT_FAILED` ⇒ 上轮解释器修复**要重启面板**才生效
（worker 每轮新进程吃到新代码，**面板自身代码是长驻的**）。

测试 `tests/test_escalation_queue.py` 68 项；`check_wiring` 0。

---

## 首次真机升级闭环 + 两处被推翻的结论

**闭环**：无人值守 AUTO 自动 `发现 → 分级 UNKNOWN_UI → 解析 SPEND_STAMINA_ON_BEAST
→ 建单 → QUEUED 继续跑`；带凭据再跑一次即派发成功（job `8fd1dfbf`，
`deepseek-v4.1-flash`）。第二轮自动 `DEDUP_SKIP ... state=WORKING` ⇒ **去重真机成立**。

**推翻一**：`permissionMode` 四个真机 job 实测 —— `dontAsk`/`acceptEdits`/`auto` **全部 DENIED**，
**只有 `bypassPermissions` 执行**（`GIT=19964ec PY=42`）。默认已改。
我先前判"`dontAsk` 能跑 shell"是错的：那 40 位哈希可以用 `Read .git/refs/heads/main` 拿到，
**我验证了输出没验证机制**。agent 后来把机制说清了：`Bash` 拒绝，shell 回退只在只读白名单内。

**推翻二**：对账**不能按时间窗把 tree diff 归给 agent**。第一次记 `TEST_PASS`
（"the tree changed while the agent ran"），而 agent 说 "No code was changed" ——
那是**我自己**的编辑。已修成"事实 + 需 agent 自述佐证"，并加 `--correct` 把历史那行
改为 `NO_IMPROVEMENT`、清掉误产生的 reload 标记。

**工单产出了真实价值**：agent 虽然不能执行，但**读源码的根因是对的**（已逐条核验）：
`TARGET_BEAST_MUSK_OX_9` 只有 **1** 条模板，姊妹语义 **10** 条；
`vision.py:1675`/`brain.py:829`/`verifier.py:699` 全链硬绑 `MUSK_OX`+`level==9`
⇒ 野兽一只也派不出去。记入 #29，并落成 P0 工单 `CAP-Z01-BEAST-TARGET-GENERALISATION`。

`hy4-preview-f` 拒绝照抄探针 token（指出 payload 自相矛盾、形似注入）——它是对的，
也说明我的探针 prompt 形状不规范。
- GUI 与新架构对齐：状态栏改 `V2大脑/MAA/MuMu/游戏/页面/AUTO/WorkBuddy/时间`（删 Qwen/Vision），
  当前决策加「执行后端」并区分*按设计走 ADB*与*真降级*，总览加 8 项 KPI（带来源）+ WorkBuddy 卡，
  「学习」→「自动开发」，状态词汇换成真实语义（未读取≠未知）。
- 实测：Tk 静默裁切（可用 638px vs 请求 1283/1101/912）⇒ 加滚动容器；episodes.jsonl 3.3MB
  全文解析 34ms ⇒ 改单次 mtime 缓存 pass（热态 100 次刷新 104ms）。
- 运维：会话里 `CODEBUDDY_GATEWAY_PASSWORD` 已过期 ⇒ 面板正确报 AUTH_REJECTED；
  冒烟测试不得跑事件循环（`after(1800, self.start)` 无法取消，会拉起真 worker）。
- CAP-A01 关闭：#25 根因**不是**落点偏/弹窗被顶，而是 before 帧本身是「英雄之旅 等级2」却判成
  `INTEL_BEAST_MISSION/BEAST/level10`（`BTN_INTEL_VIEW_TARGET` 在父帧 d=0 且与 OCR「前往查看」中心逐像素重合）。
  特定标题模板裁进了**等级**（等级10 的裁剪在等级2 帧上 d=16>阈值8）⇒ 泛匹配器（gate 54，四帧全 d=28）答了话。
  修法：**文字用 OCR 判型**（`ocr.py::_read_intel_dialog_title`），并顺带修掉硬编码的 `mission_level=10`；
  裁剪方案量过不成立（等级2 d=0 / 等级10 **d=131**）。19 项测试；真机复跑待设备空闲，**未声称 LIVE_VERIFIED**。
- 升级管线首次真实对账走通：DONE→`TEST_PASS`（非 LIVE_VERIFIED）→COOLDOWN→**写出真实 reload 标记**→
  面板 settle 后清标记并在新代码上起 worker。WorkBuddy 自述 **PARTIAL**（诚实）。
- 面板必须**重启**才吃到解释器修复：在跑的 AUTO worker（PID 3552）仍是 codex-runtimes 解释器
  ⇒ 此刻正跑在不能 import maa 的降级路径上。
- GUI 真实接线审计：新增 `tools/verify_panel_wiring.py`（独立重算生产源逐项比对，**16/16 PASS**）+ `docs/PANEL_WIRING.md`。
- **抓到三个真缺陷**：① MAA 在"1.3 小时没执行"时说正常（现在带新鲜度）；② AUTO 读的是死 worker 留下的状态文件
  （改为纯函数，AST 测试禁止它碰 snapshot）；③ `_model_rows` 按不存在的 `Rung.jobs/live_verified` 读，
  战绩文件一有数据就在 Tk 回调里崩（**单元测试看不到，开窗口才看得到**）。
- 另有"沉默当未知"：设备探测失败显示`未探测`而设备其实不在（新验证工具抓到）。
- 真实进程级验证：网关真杀真起（正常→不可用→正常）；生产适配器提交真实待办缺口 → 卡片 排队→**开发中（859e3787）**；
  已完成 job 的真实结果；KPI 与 catalog 相等 + 副本突变测试。
- **明确未执行**：拔真机（AUTO 正占用）、点击开始/停止（会打断操作者的循环）——如实标注，不假装。
- **面板必须重启**才能吃到解释器修复 + 本轮接线修复。

## 23:40 安全生效闭环 RUNTIME_RELOAD_VERIFIED（HEAD c4b5a2f）

- 旧面板（PID 23268，跑在 codex-runtimes 解释器上，日志写 `MAA_IMPORT_FAILED`）于 23:19:40 自行退出：
  `latest.log` 被截成 0 字节、`runtime_snapshot` 仍写 `AUTO_RUNNING / runtime_thread_alive=true`
  ⇒ 外部突然终止、**无崩溃报告**（不是崩溃路径）。原因未查明，如实记录。
- 未提交的 `escalation_queue.py / check_wiring.py / build_capability_catalog.py / test_escalation_queue.py`
  **是 job 859e3787 的在制品**（它自己的会话 jsonl 里 20 次 Edit 命中），不是我的 ⇒ 全程没碰、没提交。
- 新面板以 venv `pythonw` 起、cwd=仓库、凭据只经环境变量注入；worker = venv `python.exe tools\run_live.py`。
  两级 python 进程是 Windows venv 重定向器，不是解释器漂移。
- **生产证据**：重启后 **7/7** 条 episode 的 `capture_backend = MAA_MUMU_EXTRAS`（重启前 0 条）；
  首条 `20260917_233356_830706` / SCAN_MAP_FOR_BEAST / SUCCESS / verifier_ok=true / 15:34:21Z + 有截图。
  `executor_backend` 仍是 ADB 是**对的**（该技能未迁 MAA，MAA 在这里是观测/设备层）。
- 两个真缺陷：① **会话环境里的网关密码是过期的**（第一个随机密码），:8080 用的是后来固定的那个
  ⇒ 面板 reconcile 一直 HTTP 401、队列永远推不动 in-flight job；已把固定值固化到用户环境变量，
  `/api/v1/health` 200，重启后面板**两次成功对账**（15:35:17Z / 15:37:17Z，job=WORKING）。
  ② **工具调用结束会回收整棵进程树**（DETACHED+BREAKAWAY 探针同样死，PowerShell `Start-Process` 同理）
  ⇒ 长驻 GUI 不能当 fire-and-forget 子进程启动。
- 门禁：`check_wiring` problems 0；`verify_panel_wiring` **16/16 PASS**；`RUNTIME_RELOAD_REQUIRED` 已正常结算清除。
- 证据：`dataset/truth_audit/runtime_reload_20260917/RUNTIME_RELOAD_VERIFIED.json`。
- job 859e3787 全程 **RUNNING**（未中断）；随后回 Capability-First：CAP-Z01 巨兽目标泛化。

## 23:47 CAP-Z01 第一片 + 一次实时事故

- CAP-Z01：派遣判据从字面量 `("MUSK_OX", 9)` 改成数据表。新增 `winter_agent_v2/beast_targets.py`，
  `knowledge/game/beasts.json` 的 MUSK_OX 行加 `species` + 显式 `dispatchable:true`（**只有这一行允许派遣** ⇒ 行为等价）；
  改 `brain.py` BEAST_HUNT 判据 + `verifier.py` 三个校验器的身份判据。原样保留为剩余：`vision.py` 识别模板、
  `skills.py` 的 `TAP_SEMANTIC` 名字（两者都要等第二个目标有真机模板）。
- 门禁：新测试 17 passed；beast 组 59 passed / 27 subtests；`check_wiring` problems 0。
- **事故**：实时 AUTO worker import 了半写的 `brain.py` → `NameError: name 'is_dispatchable' is not defined`
  （`latest.log` 有真 traceback）。根因是 **body 改了、import 被外部写入者刷回**，
  而用来挡 AUTO 的 reload 标记**根本没写成功**（守卫脚本 import 名写错，报错后我没确认守卫是否生效）。
  ⇒ **保护动作本身必须被验证**。影响：一个循环在动作前中止，未点击、未花体力、未丢证据。
  补 import → 重跑测试与 check_wiring → 15:44:09Z 新循环干净（SUCCESS / MAA_MUMU_EXTRAS）。
- 证据：`dataset/truth_audit/cap_z01_20260917/CAP_Z01_SLICE_1.json`。
