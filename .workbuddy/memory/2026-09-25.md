# 2026-09-25（宿主会话流水，缓存）

- 按操作者指令**停止 WorkBuddy 自动调用通道**，改用本机 Ollama Qwen 做结构化 UI 动作规划。
- 新增 `local_qwen.py`（唯一知道端点的文件）+ `ui_planner.py`（协议 + Advice 桥接），
  Runtime 只多一个可注入 `advisor=`。旧答案协议、grounding、风控、执行、verifier 全部复用。
- 真机：`STRUCTURED_OUTPUT_PASS` + `MAA_EXECUTION_PASS` 已取得；`GOAL_VERIFIED` 未取得。
- 第一个真实断点：`SEMANTIC_TARGET_IS_A_LABEL_NOT_A_CONTROL` —— 模型选对 `登录好礼`，
  但项目把**标签框**当成了可点控件，点击落在标签上，页面 MAP->MAP 未变。
- 详见 .workbuddy-ai/memory/2026-09-25.md 与 docs/LOCAL_PLANNER_ACCEPTANCE.md。
