

# 2026-09-20

## 09:50–10:20 — 解除 V2 对旧目录 `E:\dongri-mumu-bot` 的依赖（未删除任何东西）

**迁移前的实测依赖（四处 + 一个活进程）**：
`winter_agent_v2/runtime_env.py` 的 `PROJECT_VENV`、`config/v2.json` 的 `ocr.module_path`、
`tools/panel_restart.py` 的 `VENV_PYTHONW`、两个桌面启动器；而**当时活着的 AUTO worker 命令行**
就是 `E:\dongri-mumu-bot\.venv\Scripts\python.exe`。

**新环境**：`E:\无尽冬日智能体\.venv` —— **复制**（非重装）自旧 venv：959 MB / 61 包，
`MaaFw 5.12.3`、`MaaAgentBinary 1.0.1`、`opencv-python`、`onnxruntime`、`rapidocr-onnxruntime` 等原生件齐全；
基底解释器是旧目录**之外**的 `Python312\python.exe`，故副本可自立。选复制而非重装：
为复现可用的原生二进制去重装 61 个包，只是多出若干出错方式而没有收益。`pyvenv.cfg` 已改写，
且新 venv 内部**没有任何**指向旧目录的绝对路径（pth/cfg/txt 已扫）。

**验证（真机，不是断言）**：
| 项 | 证据 |
|---|---|
| 解释器 | `runtime_env.resolve()` → `present=(numpy,PIL,cv2,maa,rapidocr_onnxruntime)`、**`missing=()`** |
| preflight | `VERDICT: PASS`（Runtime / MuMu / WorkBuddy Gateway 全 OK），**候选里已无旧路径** |
| GUI + AUTO | 面板日志：`运行环境预检通过：E:\无尽冬日智能体\.venv\Scripts\python.exe` → `自动运行已启动` → 连续轮次 |
| **活 worker** | `16712|19024` → **`E:\无尽冬日智能体\.venv\Scripts\python.exe run_live.py`**（父进程＝面板） |
| MAA 原生执行 | `available()=True`、`capture_backend=MAA_MUMU_EXTRAS`、**`capture()` 720x1280 用时 11.6 ms** |
| 网关 | preflight + 面板日志均 OK（`http://127.0.0.1:8080`，node 进程在旧目录之外） |

**顺带修掉一个真 bug（它挡住 AUTO 启动）**：面板启动时跑 `preflight.py --json` 并把子进程 stdout
按 **OEM 码页**解码（对控制台工具是对的），而 preflight 当时输出**裸 UTF-8** ⇒ 中文根路径变成乱码、
**最后一个字节吃掉了闭合引号** ⇒ `json.loads` 报 `Invalid \escape` ⇒ 面板显示「预检输出无法解析」并
**拒绝启动 AUTO**。修法：该路径改为 **ASCII 转义 JSON**（任何解码下字节一致）；人读的输出保持可读形式。
已用面板自身的调用路径在修前/修后各复现一次。

**结论**：V2 生产层**已完全解除**对旧目录的依赖。旧目录从 V2 角度**可以安全删除**，两点保留说明在台账。
**未删除、未重命名、未清理旧目录的任何内容。**



## 12:12–12:30 — Soak 启动失败：**诊断与预期不同**，另修掉两个真缺陷

### 一、Soak 并不选择解释器（三向实测）
`_drive_soak` 是**进程内**构造 `GatewaySoak`（`gateway_soak.py` **不创建任何子进程**，它甚至断言 GUI
不额外 spawn 网关）。截图那句来自 `launch_context()`（`control_panel.py:1390`），它打印的是**窗口自己的**
`sys.executable`。三种情形实测：

| 启动窗口的解释器 | `launch_context()` |
|---|---|
| venv 的 python | `('production','运行解释器是生产解释器同目录的 python.exe')` → **Soak 启动** |
| 带 `WINTER_AGENT_LAUNCH_PATH=desktop` | `('production','正式桌面入口')` → **Soak 启动** |
| 其它解释器 | `('development','运行解释器 … 不在生产解释器目录 E:\无尽冬日智能体\.venv\Scripts 内')` ← **截图那句** |

⇒ **Soak 拒绝是正确的**（§三/§九：开发工具启动的窗口不得产出验收证据），**隔离检查必须保留**。
`panel.log` 里今天有 **4 次 `验收 Soak 已启动`**（09:54–09:59，venv 启动的窗口）⇒ 机制正常，
只要窗口跑在生产解释器上。

### 二、真缺陷 A：一个生产子进程**继承调用者的解释器**
`escalation_queue._wiring_problems()` 用 `sys.executable` 派生 `tools/check_wiring.py`，而它由**面板的泵**
驱动 ⇒ 开发工具启动的面板会**用产品不运行的那个解释器**判断产品接线是否合格。
**已改**为走 `runtime_env.resolve()`（项目自己的机制）；解析不到就返回 `None`（"无法运行"，不冒充答案）。
**诚实说明**：本次**没有改变结论**（`check_wiring` 不依赖第三方包，非生产解释器同样输出 `problems: 0`）
—— 这是**修掉一个隐患**，不是修掉报告的症状。

### 三、真缺陷 B：一致性检查的 `NameError`（**无 traceback，靠静态分析定位**）
任何 artifact 里都**没有** NameError 的 traceback（全仓日志/台账只有 2026-09-17 `is_dispatchable`、
2026-09-18 `fold` 两条历史）。按"不得猜"的要求，改用**静态分析**找未定义名字：

```
tools/control_panel.py:4023: undefined name 'view'
```

`_narrate_pump` 里 `development = view.get("current")`，而**该方法从未绑定 `view`**；两个兄弟刷新都写
`view = escalation_view()`，唯独这里丢了这一行。因为整块在 `try` 里、`except` 很宽，**NameError 被吞掉**
⇒ **一致性卡片每次刷新都失败且一言不发** —— 正是这个面板存在的意义所在的那种沉默。
**已补** `view = escalation_view()`。新增 `tests/test_panel_names_are_bound.py`（静态检查被监视的刷新方法
所用名字是否已绑定；**移除那一行 → 2 条测试立刻变红**，已验证）。

### 四、附带观察（未定性）
`learning/CONTROL_PLANE_RELOAD_REQUIRED.json` **只写不删**（`control_plane_reload.py` 有 `control_plane_path`
但没有任何清除函数；对照 `runtime_reload.py` 有 `clear()`）。`panel_reload.log` 里能看到多次
`stopping panel tree at pid …`。**但重载判定本身是 git 派生的**（`needs_reload`/`reload_reason` 用
`changed_paths_since`），所以**陈旧标记是否会再次触发重载，我尚未证明** —— 记为观察项，未定性。


## 12:24–12:30 — Soak **启动成功并正在采样**（正式启动路径验证通过）

以正式启动配置（`WINTER_AGENT_LAUNCH_PATH=desktop` + 生产 venv 解释器）常驻启动面板后：

- 日志：`12:24:23  验收 Soak 已启动（WINTER_AGENT_LAUNCH_PATH=desktop（正式桌面入口））：窗口 900 秒`
- 证据文件 `evidence/gui_workbuddy_loop/latest.json`：
  `launch_context="production"`、`development_env_limitation=""`、`sample_count=7`、
  `samples[0].gui_pid=2272`（活面板）、`gateway_pid=25708`，`generated_at` 持续前进。

⇒ **Soak 不再触发解释器隔离错误**，`launch_context` 是 production。

### 但 Soak 自己的判定是 FAIL —— 一个**新发现**，如实记录
`verdict.overall="FAIL"`，其中 `auto_gameplay.ok=false`，理由：
`窗口内测到 AUTO 未在运行（AUTO 状态可读但为停止）`。

而同一时间面板日志写着 `自动运行已启动`、`连续运行已启用`，`panel_restart --status` 也显示
`runtime: DEGRADED ... updated 1s ago`。⇒ **两处对「AUTO 是否在运行」的读数不一致**，
Soak 采到的那个源说"停止"。**这是 Soak 存在的意义**（它就是为了抓这种矛盾），
但**成因未查明**，下一轮第一件事就是查它读的是哪个字段。

### 12:40–13:05 — 成因查明：`auto_gameplay` FAIL 是**仪器误报**；真 P0 定位到一行视觉代码

**1. Soak 的 `auto_gameplay` 判 FAIL 是误报，不是 AUTO 的错。**
`auto_running` 读的是**每轮全新进程**的线程标志（`control_panel.py:2194-2202`：
`mode=="AUTO" and (runtime_thread_alive or scheduler_loop_alive)`），一轮里只有约 4 s 为真
（~10% duty），而 Soak 采样节拍 ~12.5 s ⇒ **拍频锁相**，系统性采不到。
同一窗口（04:24:24→04:32:49Z）的证据流里有 **13 条已完成的 AUTO 轮次**，2 秒轮询也两次抓到
`AUTO_RUNNING`。**没有放宽判据**，改法是新增"累计完成轮数"这一单调计数（旧信号可在零轮次时为真，
计数器不会），两者取或，两者都无进展仍判 False。`pump_ticking` 同形（唯一过期样本是 t=0 的 589 s）。

**2. 真 P0：邮件收件箱被视觉层误判成 `POPUP/INTEL_REWARD`。**
逐小时流水：`09-20 01:xx` `DISMISS_INTEL_REWARD/FAILURE ×30`，`04:xx` `DISMISS_MAIL_GENERIC_REWARD/FAILURE ×29`。
失败轮次的 `state_before` 一律 `page=POPUP, popup=INTEL_REWARD`，而**我打开 PNG 看到的是一张
干净的邮件收件箱**（标题「邮件」、战争/联盟/系统/报告/收藏、可领取奖励列表、删除所有已读／一键已读&领取）。
在**同一帧**上重跑生产分类器：`PAGE_MAIL d=0.0`、`TAB_MAIL_SYSTEM_ACTIVE d=0.0`、
`BTN_MAIL_TAB_ALLIANCE d=0.0`、`BTN_MAIL_CLAIM_ALL d=4.0`、`STATUS_MAIL_TAB_BADGES d=4.0`、
`BTN_MAIL_TAB_REPORT d=6.0`，而 `POPUP_INTEL_REWARD_TITLE d=20.0 thr=22.0` ⇒ **六个强邮件信号输给一个
踩在 91% 阈值上的弹窗信号**。
缺陷行：`vision.py:1144-1147`，其负向对照只挡地图页（`BTN_RESOURCE_SEARCH_*`），不挡邮件页；
阈值 `vision.py:265` = 22。该分支在 `PAGE_MAIL` 分支（`vision.py:1630-1639`）**之前**求值。
`POPUP_INTEL_REWARD_TITLE` 的 ROI（x .30 w .40 y .215）**横跨邮件列表行**，行内容逐封邮件变化
⇒ 这是同一类 **"裁剪框盖住了会变的内容"**（本项目第 6 次）。
**为什么一直没被抓到**：`tools/probe_reward_popup_gate.py:45` 的 `SIGNALS` **把
`POPUP_INTEL_REWARD_TITLE` 排除了** —— 所以"零误报"的结论从未覆盖真正在触发的那个分支。
这也是 `DISMISS_INTEL_REWARD` 那 35 次失败的同一根因（一个 bug、两个技能名、约 70 次失败）。

**3. 我自己的误判（必须记住的教训）。**
我先只读了 episode 里的 `state_before`，据此断定"调度器用**别的域的**关闭技能去关弹窗"——
**错的**。`state_before` 是 **runtime 的信念**，不是像素。**诊断 UI 类缺陷必须打开 PNG 看**，
否则会把"世界状态是错的"读成"动作是错的"，并去修一个没坏的东西。

**4. 当前状态（如实记录）**：面板**已停止**——12:37:40 因 `CONTROL_PLANE_RELOAD_REQUIRED`
（6e8ba75→55d7746）自行在安全点重启窗口，旧进程退出后**没有活下来**（`panel.pid` 仍是死 pid 2272，
快照停在同一时刻）；网关 pid 25708 正常监听 8080。另：**一个 WorkBuddy 升级 Job 正在跑
`DISMISS_INTEL_REWARD`**（pid 24292/10100）——它基于的正是那个被误读的世界状态。

面板未恢复的**真因已查明**：`control_panel.py:4496` 的重载把重启委托给
`tools/panel_restart.py --restart`（设计正确），但那个窗口是**从开发宿主拉起的**，
重启助手跑在宿主进程围栏里，它删除 `panel.pid` 的动作撞上宿主的安全删除护栏
（`learning/control_panel/panel_reload.log` 里的 `[safe-delete][SAFE_DELETE_BULK_CONFIRM_REQUIRED]`）。
即 `DEVELOPMENT_ENV_LIMITATION`，§九 禁止在产品里绕行。⇒ **开发宿主拉起的窗口连一次 900 秒
Soak 都跑不完（本次只活 13 分钟）**，10 小时攻坚只能在 `Start-Winter-Agent-V2.cmd` 上跑。

### 13:10–13:35 — 项目「已知失败清单」是错的（至少 11 项，不是 9 项）

`soak-fix` 把两个 `test_control_panel.py` 的 `AssertionError: 'BACK' != 'SAFE_STOP'`
归为「同事未提交的改动」，**未做 A/B 就下结论**。我按「不得无证据判定无关」重查：

- **方法**：`git worktree add --detach <tmp> HEAD` —— 在 HEAD 上开一棵**干净工作树**，
  跑目标测试，再 `git worktree remove`。**不碰** live tree（当时有 worker 正在改文件）。
- **结果**：干净 HEAD 上同样 2 个测试失败（`2 failed, 45 passed`）⇒ **是 main 上的既存失败**，
  与脏工作树、与当时的 worker 改动都无关。
- **判定为测试陈旧而非代码回归**（有证据，不是猜）：`brain.py:202-228` 的
  `_leave_foreign_page_once` 是**有意新增**的行为，注释里写着 2026-09-19 的真机测量
  （某轮以 `goal_page_mismatch` 停下，而客户端还停在这个 goal 自己的面板上，下一轮跳不回家）。
  实测决策：`MAIL + RESOURCE_DETAIL → BACK / mail_goal_leaves_a_panel_it_does_not_own`，
  **同一 brain 第二次调用 → `SAFE_STOP`**（一次性），`MAIL + MARCH → SAFE_STOP` 不变。
  ⇒ 测试守的「不得继承采集动作」这个不变量仍然成立，只是断言写的是旧的 `SAFE_STOP`。
- **修法**：不弱化断言，反而**更严**——连 `reason` 一起钉住，并记录 BACK 是一次性的
  （诚实停止仍会跟着来，不会变成循环）。`tests/test_control_panel.py` **47 passed**（原 2 failed / 45 passed）。
  提交 `6f8e8b6`，已推送。
- **第三个同类陈旧测试**（同样在干净 HEAD 上失败）：
  `tests/test_reward_popup_source.py::…::test_without_goal_context_it_stops_rather_than_guessing`
  期望 `SAFE_STOP`，实得 `CLOSE_POPUP`（brain 后来加了通用 `CLOSE_POPUP` 兜底，`brain.py:509-510`）。
  **未动**——该文件当时正被 `vision-fix` 编辑，并发写同一文件是禁止的，留待其落地后再处理。
- ⚠ **方法学陷阱（下次别再踩）**：worktree **不含被 gitignore 的 `dataset/`**，
  所以 worktree 只能跑**不依赖 dataset 的测试**；想取真正的全量基线必须在 live tree（且工作树干净）上跑。

⇒ **结论**：`docs/`/handoff 里那张「已知 9 项失败」清单**不可用于判定「无关」**，
它至少少了 3 项；`04_OPEN_ISSUES.md` 需要在下一次刷新 handoff 时按干净 HEAD 重derive。

### 14:00–14:45 — 配额 429 打断全部四个 worker；接管并完成其中一个的未提交改动

**事故**：`vision-fix` / `stall-breaker` / `vision-verify` / `soak-fix` **全部**被
`429 使用量已超出频率限制` 打断（额度 **2026-09-21 12:29:50 GMT+8** 重置）⇒ 本窗口内**无法再派活**，
我是唯一执行者。**未把它们的未完成工作当成已完成来汇报。**

**死前留下的状态（逐项核对，不假设）**：
- `vision-fix` **已提交** `7b712d8`（第三个陈旧测试），质量好：先读 `brain.py:397-411` 确认行为
  **有据可依**（首次训练清扫真机运行以 `generic_reward_without_goal_context` 停下、卡在奖励弹窗上，
  因为训练/研究没有自己的 dismiss 技能；而 `CLOSE_POPUP` 已注册、VERIFIED、低风险、绑了
  `verify_popup_closed`，关掉挡路弹窗是 goal-neutral），再**实测**四种 goal 的决策而不是从分支名猜，
  断言**改严**了（skill+reason 都钉住）。它还主动交底一个**未修缺口**：真机那次跑的是
  `KEEP_TRAINING_PRODUCTIVE`，该 goal **今天仍然停下**，分支只覆盖两个字面名。
- `stall-breaker` 改动写进了 `runtime.py` + `capability_gate.py`（语法 OK），**但一次测试都没跑、没提交**。
- `vision-verify` **什么都没产出**（`learning/verify_vision` 不存在）⇒ `9ee62cc` 的独立语料复核**没有做**。
- `soak-fix` 的返工**尚未开始写**。

**接管过程（不采信 diff，逐项验证）**：
1. `_streak_and_last` 只有**一个**调用者，两个读取点都走 `_streak_record` ⇒ 记录加宽是收敛的。
2. 8 个行为用例直跑改动后的函数：空转形状给出正确原因；老形状措辞保持不变；legacy 三字段记录
   **不编造原因**；all-`None` 仍 streak 0（旧 bug 未复活）；2 轮仍不 defer。
3. A/B 排回归：`test_live_runtime.py` **带改动 2 failed / 6 passed，把改动 stash 掉同样 2 failed / 6 passed**
   ⇒ 不是它造成的。
4. **⚠ 我自己的测试差点是废物**：第一版只直接调 `_no_progress_reason()`，于是"把**调用点**改回旧措辞"后
   **10 个测试全绿**——**咬合验证失败**。原因是 `_no_progress_reason` 本身没动，变的是网关的**调用点**。
   改成走 `CapabilityGate._no_progress_deferral`（生产真正用的路径）后，再改回调用点 ⇒
   **2 failed / 12 passed**，报错原文正是那个 bug：
   `'issued no action at all' not found in '3 consecutive production episodes passed their verifier
   and advanced no part of this goal'`。
5. 提交 `0cb30cc`（`check_wiring` 0 problems；工作树 `.py` 已清空）。**未声称 LIVE_VERIFIED**——
   面板没在跑，真机没有观测到"停滞被 defer"。

**教训（比这次修复更值钱）**：**咬合验证要"把调用点改回去"，不能只改被调的纯函数。**
只测 helper 的测试无法发现"生产已经不再调用它"——那正是 green-but-useless 的形态。

**另一个实测结论**：`test_live_runtime.py` 在**干净 worktree 上 8 passed**，在 live tree 上 2 failed
⇒ 那 2 个失败来自**未提交的 knowledge/goals/policy JSON**（runtime 会读它们），与代码无关。
**工作树脏时 runtime/goal 类测试不能当回归信号，只有干净 worktree 的 A/B 可信。**

### 13:35–14:20 — 生产级全面体检（操作者新指令），报告已落盘并推送

**报告**：`docs/V2_PRODUCTION_HEALTH_20260920.md`（提交 `d145242`）。**零项可标"真机验证通过"**——
面板没在跑，本轮没有任何一项获得"真实客户端 + 真实动作 + 真实状态变化 + Verifier PASS"。

**环境实测（§一）**：工作空间 / `HEAD==origin==0cb30cc` / 无未提交 `.py` / 生产解释器＝新 venv
（`PROJECT_VENV=E:\无尽冬日智能体\.venv`，无旧目录引用）/ Gateway pid 25708 监听 8080 /
**MuMu 已连接 `127.0.0.1:7555 device`** / MAA 执行器在位 / 设备租约已释放无泄漏 —— 全部正常。
**但：面板未运行（pid 2272 死）、AUTO 未运行（snapshot 过期 57.6 分钟）。**

**本轮报出的两个核心事实**：
1. **面板的健康读数与真实情况不一致**：12:24–12:32 那次生产窗口 `gateway_reachable 40/40`、
   端口/单网关/黑窗全过，而 `auto_running` **40/40 全 False**，同一窗口证据流里却有 **13 个真实轮次**。
2. **更要紧：健康 ≠ 有进展**。那 13 个轮次**全部失败**、`last_success_time=null`、没有任何真实状态变化，
   而网关/GUI/端口全部显示"正常"。**这就是"心跳正常 ≠ 游戏任务正常执行"的实例，当天真实发生过。**

**新定位的结构性问题（此前从未报过）**：
- **阶段链只认成功态交棒**（`tools/control_panel.py:4713` 等）：`stop_reason == "mail_all_clear"`
  才进入下一阶段 ⇒ **一个失败阶段永久占位**，Intel / 野怪（真正消耗体力的两个阶段）**永远轮不到**。
  这是"任务长期饥饿"的机制，也是 01:00–04:37 烧掉约 2 小时的路径。
- **打野只扫不派**：`SCAN_MAP_FOR_BEAST` **396 成**、`BEAST_HUNT` **0 成 1 败**——整天扫地图从不派兵。
- **免费体力领不到**：`CLAIM_FREE_STAMINA` **10 成 / 235 败**。
- **体力四道阻断叠加**：链到不了 MAP → 到了也只扫不派 → 观测值 527 自 09-19 15:50 已过期不复用
  → 免费体力 235 败。**不是"忘了消耗"。**
- **旧链 `_run_worker`（`tools/control_panel.py:4726`）无任何生产调用者**，是**不可达的重复实现**
  （与现役链**不混用**）；唯一引用是 `tests/test_runtime_interpreter.py:183` 断言其源码文本。
- 竞技场 / 巨熊 / 当前活动：**0 实现、0 尝试、0 可调度技能**（属能力缺口，非 bug）。

**证据可信度问题（§五）**：
- 文档"已知 9 项失败"是错的 ⇒ **至少 11 项**（干净 worktree A/B 证明）。
- **Soak 证据单槽无归档**：13:21 的 1 样本窗口**覆盖**了 12:24 的 40 样本生产窗口；而
  `gateway_soak.py` 注释却写"旧文件留作历史"——**与文件系统相反**。已列工单。
- 13:21 有面板进程 12320 启动 soak 并写证据，但 `panel.log` **在 13:21 没有任何记录**；
  已核实 `GatewaySoak` 唯一生产构造点是 `tools/control_panel.py:2269`，且 soak 测试全部显式传
  `tmp_path` 证据路径 ⇒ **不是测试污染生产证据**，成因 `UNKNOWN`（未解释，不当结论）。
- 仓库内遗留 **1010 个未跟踪测试临时文件 / 20.1 MB**（`tools/_pt_*`、`learning/pytest_tmp_nav`），
  **每个含一份 `winter_agent_v2/**` 副本** ⇒ `grep` 结果约九成是噪声（本轮实测）。

**顺手纠正了自己的一个错误**：探针报 `runtime_env 无 runtime_python_path`——是我的探针写错了名字，
该函数在 `tools/control_panel.py:169`。**产品无此问题，所以没有写进报告。**

**未完成（如实）**：`9ee62cc` 的独立复核（worker 被 429 打断两次，额度 2026-09-21 12:29:50 GMT+8 恢复）；
Soak 条件 7 的返工未开始；全量测试基线仍在后台跑；§4.3 / §4.6 / §4.7 三项为 `UNKNOWN`。

### 14:25 — 全量测试基线：我第一次的做法被宿主护栏截断，而项目早就有工具

- 单次 `pytest tests/ -q > out_suite_baseline.txt`：跑到 `[100%]` 之后**没有 summary 行**，
  被宿主的批量删除护栏截断：`[safe-delete][SAFE_DELETE_BULK_CONFIRM_REQUIRED] {"count":742,"threshold":50,...}`。
  ⇒ **`grep -c "^FAILED"` 得 0 什么都不能证明**——这次运行**没有判决**（既不是通过，也不是失败）。
  我差点把它读成"全绿"。
- **正确工具是项目自带的 `tools/run_tests_batched.py`**，它的 docstring **逐字记录了我刚踩的这个坑**：
  "reaches 100% and then dies before printing its summary ... the run therefore has **no verdict** --
  not a failure, and not a pass either. Betting on one long run again just burns half an hour for
  another non-answer." 做法＝按文件切批 + 每批一个 fresh basetemp（不让 pytest 攒出触发护栏的垃圾），
  无 summary 的批记 `INCONCLUSIVE` 而不是重掷骰子；exit code 只有在每批都有 summary 且都通过时才为 0。
- ⇒ **环境铁律应补一条**：本机**不要**单次跑全量 pytest，用 `tools/run_tests_batched.py`。
  `START_HERE.md` 的「环境铁律」目前没有这一条（只有"无 coreutils / PowerShell 无 stdout / 必须用项目 venv / MuMu 启动命令"）。
- 教训的同构性：这与本轮"测试必须覆盖生产调用路径""咬合验证要改调用点"是同一类错误——
  **一个没有判决的结果，被当成判决来读。**

### 15:30–16:15 — 独立复核 `9ee62cc`（部分完成）＋ 免费体力领取的落点被证伪

**一、`9ee62cc` 的复核：claim 1 我已独立复现（我自己跑，不采信作者）**

从 `6f8e8b6` 取出修复前的 `vision.py`（写到 **仓库外** `%TEMP%\winter_vision_ab\`，用
`importlib` + `__package__="winter_agent_v2"` 让相对导入解析到活包），同帧同参数跑两个分类器：

```
PRE  (6f8e8b6): page=Page.POPUP popup=INTEL_REWARD mail={} intel={'claim_feedback': True}
POST (HEAD)  : page=Page.MAIL  popup=None         mail={'status':'CLAIMABLE','active_tab':'SYSTEM'}
```

⇒ **核心声明成立**（邮件帧不再被读成奖励弹窗）。语料全量差分（`dataset/raw`+`truth_audit`+`verified`
= **6747 帧**，与作者同一总体，便于逐数对比）已启动，仍在跑，未出结论——**不预判**。

**二、`CLAIM_FREE_STAMINA`（10 成 / 235 败）：我先把最可能的假设证伪了**

- 技能定义：`skills.py:266-275`，`SkillState.CANDIDATE`，动作 `TAP_SEMANTIC BTN_CLAIM_FREE_STAMINA`，
  verifier `FREE_STAMINA_CLAIMED`，失败签名 `FREE_STAMINA_CLAIM_NOT_PROVEN`。
- 245 条 episode **全部** `state_before/after = POPUP/GET_MORE_STAMINA`，且 `stamina=None`
  （该弹窗上看不到 HUD 体力表）。
- 打开失败帧：**体力 547 / 上限 200**（"体力积压"从推断变成照片上的数字），
  行「丰盛的招待…帕特里克会按时在**仓库**备好补充体力的大餐」旁边就是一个绿色**「领取」**按钮
  ⇒ **不是"没有可领的东西"**。同一弹窗还有三处付费行（购买并使用 300💎／超值月卡／礼包购买）。
- `after_refresh_2` 帧与 before **完全一致**（「领取」原地不动）⇒ 这次尝试**没有任何可见效果**。
- **假设证伪**：我原以为与情报入口 #51/#52 同类（ROI 对着空地）。实测 `BTN_CLAIM_FREE_STAMINA`
  只有 1 条 manifest 记录，ROI box **(540,355)-(622,411)**、中心 (0.807,0.299)；
  把该 ROI 裁出来放大 4 倍看，**就是「领取」两个字**
  （`%TEMP%\winter_roi\BTN_CLAIM_FREE_STAMINA_0.png`）⇒ **落点是对的，不是瞄错。**
- **仍未区分的两个候选**（需要真机，不能靠离线猜）：① 点击送出但游戏未受理；
  ② verifier 要求体力数字上升，而该奖励是**进仓库的大餐**、本身不会让体力变
  ⇒ 若是②，则 verifier 的期望与该奖励类型不符，**属"验证器判断错误"而非动作错误**。
  该 episode（09-19）**早于** `tap_point`（`6b0a6dd`）落地，所以**拿不到当时的落点像素**；
  下一次真机失败会自带落点，届时可直接区分。**本轮不下结论。**

### 17:00–17:45 — `9ee62cc` 独立复核**完成**：安全，且作者的爆炸半径声明被独立复现

**最终仪器（第三次换代才有判决）**：漏斗 + 真标签。
① 漏斗：只保留"被删掉的信号命中 ≤22、而存活的整弹窗模板没命中 ≤6、且两条负向对照都不命中"的帧；
② 在候选集上跑**修复前/后两个真分类器**，取**真标签**。
（前两代为什么不行：全 `observe()` 差分 **1.3 s/帧 ⇒ 2.5 h**；六距离谓词扫描**量的是我重建的谓词**，
不是真标签——见下。）

**结果**（语料 6747 帧 = `dataset/raw`+`truth_audit`+`verified`，与作者同一总体）：
```
candidates inspected : 267
labels that CHANGED  : 80
...of those, chrome-carrying (= REGRESSION): 0     ← 预先声明过的判定线，通过
transition breakdown:
   70  (POPUP, INTEL_REWARD) -> (MAIL, '')
    5  -> (UNKNOWN, '')      3 -> (INTEL, '')      1 -> (HOME, '')      1 -> (MAP, '')
changed & chrome-free & on the MAIL page : 70      ← 与作者「70 邮件」一致
changed & chrome-free & NOT mail page    : 10      ← 与作者「10 地图/城池」一致
```
**我独立核对的额外一条**：那 70 帧**我自己测的** `PAGE_MAIL ≤ 6`（我的 `mail` 标记）⇒ 变成 `MAIL` 是对的，
不是只听作者说。10 帧非邮件页只是"不再被错标成 INTEL_REWARD"；它们的新标签**我没有做地面真值核对**。

**重叠核对（我测的一侧）**：邮件页 title 原始距离 **n=304, min=20, p50=30, max=40**（下限恰好 20）
⇒ 任何想排除邮件页的门必须 ≤19。**作者测的一侧**（真弹窗 0..26、其中 85/217 ≥19）我没有独立重测
⇒ "没有阈值可分"这个**组合**结论成立，但**我的仪器只覆盖了邮件侧**，这一点必须讲清楚。

**我自己的方法错误（本轮第二次，也是最重要的一次自我纠错）**：
第二代仪器跑出 `chrome=42`，看起来像"42 张真弹窗丢了标签"。**是我错了**——我重建的谓词忽略了
`vision.py:1136` 的外壳分支**在它之前**执行，带外壳的帧早已返回 `GENERIC_REWARD`、**根本到不了**被删的谓词。
⇒ **重建谓词不能当证据，必须取真标签。** 换成第三代仪器后，同一问题给出 `chrome-carrying = 0`。
教训与前面几条同源：**"我自己算出来的中间量"也不是事实。**

**结论**：`9ee62cc` 在 6747 帧上**未发现回归**，可以在等真机验证的同时继续保留。**仍未真机验证。**
候选清单已落盘 `learning/verify_vision_candidates.json`（267 条，可复查）。

### 18:05–18:20 — P0「重复性任务必须执行到真实完成」：**先纠正我自己报告里的一个错**，再取现场证据

**① 我上一份体检报告的 §2.1 措辞与 §2.3 归因是错的，已在文档里加「更正」块。**

- 我原写"正式 AUTO 是阶段链、每阶段一个 run_live 子进程"，并把"任务饥饿"归因于阶段交棒闸门
  （`control_panel.py:4713` 的 `stop_reason == "mail_all_clear"`）。
- **那段阶梯在 `_run_worker`（`:4726` 起）里，而它没有任何生产调用者**；被线程启动的是
  `_run_unified_worker`（`:4409` → `:4644`），只发**一条**命令
  `run_live.py --max-actions 24 --capture-dir runtime_auto/<stamp>`（无 `--goal`、无 `--stop-after`），
  **Goal 由运行时内 Scheduler 选** ⇒ **面板层没有第二次 Goal 选择。**
- **用产物独立确认（不是读代码推断）**：`dataset/raw/control_panel/` 下 `runtime_auto` **2217** 个子目录、
  最新就是当下；阶梯自己的目录 `runtime_mail` 最后写入 **09-18 07:29**、`runtime_training` **09-18 13:36**、
  `runtime` **空**，且**根本没有 `runtime_intel` / `runtime_beast` 目录** ⇒ 阶梯的 Intel/野怪 阶段
  **从未产出一张截图**。⇒ **`--stop-after DISPATCH_INTEL_BEAST` 之类不是本 P0 的原因**（我在改之前拦住了自己）。
- 连带：工单 **T3**（阶段失败预算）作废。

**② 面板已被启动，AUTO 正在跑**（不是我启的）：`panel.pid=19612` 存在；活 worker
`run_live.py --max-actions 24 --capture-dir ...\runtime_auto\20260920_181355_...`；
`runtime_snapshot` 5.5 s 前更新，`mode=AUTO`、`agent_state=GOAL_RUNNING`、
`goal=ALLIANCE_ROUTINE`、`skill=OPEN_ALLIANCE`。⇒ **§七 真机验收现在可以做**（但仍需更长的观察窗口）。

**③ 现场证据（25 分钟 / 10 轮）**：
- **10 轮里 5 轮"只做一个动作而且失败"**：`CLOSE_POPUP:FAILURE` **连续 3 轮**（10:04:40 / 10:05:18 / 10:05:54）、
  `DISMISS_MAIL_GENERIC_REWARD:FAILURE`、`DISMISS_DAILY_GENERIC_REWARD:FAILURE`。
- **窗口内没有任何 DISPATCH / MARCH / START_GATHER 类 episode** ⇒ **情报、打野、采集一次都没派出**。
- **但"同一轮内重复"是可行的**：`10:07:56` 那轮 `DISMISS_DAILY_GENERIC_REWARD:SUCCESS` 出现**两次**
  （6 步：BACK→OPEN_DAILY→DAILY_CLAIM_REWARDS→DISMISS×2→BACK）⇒ 机制本身在，问题是**走不到需要重复的 Goal**。
- **观察存储里的体力是 `8`，取自一个 `%TEMP%\tmpsf009pq_\...` 临时帧**（4 小时前）
  ⇒ **生产状态文件被非生产帧写入**（测试/探针污染），且早已过期。**不得用它下结论。**

### 18:20 — P0「执行完当前全部可执行任务才进入下一轮」：**定位到唯一的那一行并已修（`fd3f153`）**

**机制就在 `tools/control_panel.py:5112-5119`（`_apply_complete` 尾部）**：每个子进程结束后
无条件 `self.root.after(delay_ms, self.start)`，`delay_ms = 600000 if full_queue else 30000`，
而这个判断**只看上一轮的 stop reason，从不看工作周期还有没有可执行工作**。⇒ **A（子进程结束）被当成 D（工作周期结束）**，
`--max-actions 24` 用满也只等 30 秒重开一个新进程。

**改动（最小、有界）**：`_apply_complete` 现在区分两种"还要继续"：
`SEMANTIC_TARGET_NOT_VERIFIED`（目标解析不出＝这个 Goal 现在做不了，应立即让位给别的 Goal）或
`MAX_ACTIONS_REACHED` 且 `summary["executed"] > 0`（预算花在真动作上＝周期还有活）⇒ **`delay_ms = 0`，立即继续**；
其余保持原样（满队列 10 分钟、其它 30 秒）⇒ 环境性失败**不会**变成紧循环。
有界性来自 `0cb30cc` 刚接上的 no-progress deferral（连续 3 次无进展即让该 Goal 下台）。
`check_wiring` **problems: 0**；`test_control_panel.py` + `test_runtime_interpreter.py` **71 passed**。

⚠ **面板正在运行**：该改动一落地，运行中的窗口会因 `CONTROL_PLANE_RELOAD_REQUIRED` 在安全点自行重载
（这次窗口是操作员启动的，不同于上次我从开发宿主拉起的那次）。**§六 六个场景仍未被观察**，需要更长窗口。

**另记一次差点误判**：我第一条命令链返回 exit 1 且 `check_wiring` 无输出，我差点当成"改动把接线检查弄坏了"；
单独重跑后是 `problems: 0` / `rc=0` —— **是我的命令链（`tail -3` 与顺序）造成的，不是产品**。
又一次"把管道产物当成事实"。

### 18:40–20:50 — 真机验收结果、控制面自杀式重载的根因、以及"点任意位置退出"的实测

**① `fd3f153` 与 `0cb30cc` 已在真机上报效（今天第一次有"我改的东西生效了"的证据）**
- 日志：`20:29:13 ✓ 本轮结束：MAX ACTIONS REACHED` → `20:29:14 连续运行已启用，**立即**后进入下一轮。
  （子进程结束不等于工作周期结束，立即继续）` → `20:29:15 自动运行已启动` ⇒ **预算用满后没有等 30 秒**。
- `CLEAR_INTEL` 已 `DEFERRED`，理由串是新的分解版：*(3 passed their verifier, 1 were rejected by it, 0 issued no action)*。
- **轮次规模从 n=1~6 变成 n=23 / n=14**；`last_success_time` 从长期 `null` 变成 20:33:15 的真实成功。
- 窗口内真实动作：`INTEL_HERO_DISPATCH:SUCCESS`×1、`DISPATCH_INTEL_BEAST:SUCCESS`×1、`CLAIM_FREE_STAMINA:SUCCESS`×1、
  `OPEN_INTEL`×5（每次之后重新读列表）。Episode `20260920_202422_459803`(n=23)、`20260920_202915_466807`(n=14)。
- ⚠ 仍未验证：`337bdc4` 的失败让位**没有直接证据**（无 `yield …` 行）；`DISPATCH_BEAST`（普通野怪）**仍为 0**；
  **体力消耗没有真实读数**（拒绝使用取自 `%TEMP%` 临时帧的旧值）。

**② 控制面自杀式重载：根因确认（`0792cb8` 停用了它）**
`panel_restart.py:290` 的 `taskkill /PID <panel> /T /F` **跟随父进程关系**，而重启助手是**面板自己派生的**子进程
⇒ 助手在 tree kill 中自杀，`cmd_start()`（`:391`）**从未执行**。证据是助手自己的日志：
`panel_reload.log` 在 `stopping panel tree at pid 19612` 处**断掉**；它本应接着打印 `:294` 的
`workers left after the kill: N`，再下一行才是 `cmd_start()`。两次（pid 2272 / pid 19612）同形。
**项目早知此机制**（`_ensure_gateway_after_stop` 的 docstring 明写 `/T` follows parentage、DETACHED_PROCESS
不改变 parentage，2026-09-18 实测），**只给网关处理了**。
⇒ **我撤回**先前把 12:37 那次归因于 `DEVELOPMENT_ENV_LIMITATION` 的结论：18:18 那次是**操作员从桌面入口**启动的，
同样没回来，所以这是产品缺陷。`0792cb8` 已把自动重载**停用**（改为提示人工重启），仅停自我 kill，
STOP/PAUSE、设备租约、生产隔离未动。**这不是重载已修**。
⚠ **运行中的面板（pid 20092，20:22 启动）加载的是 0792cb8 之前的版本** ⇒ 在它被重启前，
**不要再提交控制面（`tools/control_panel.py`）改动**，否则会再次自杀。

**③ 当前真机阻塞：一张自称"点击任意位置退出"的奖励弹窗**
`runtime_snapshot`：`current_goal=AUTO_DISCOVERY`、`current_skill=SAFE_STOP`、`page=POPUP`、
`stop_reason=generic_reward_without_goal_context`。帧：
`dataset/raw/control_panel/runtime_auto/20260920_204143_843357/…_step_001_before_20260920T124146365715.png`
（「获得奖励」+ 三份奖励 + 页脚「点击任意位置退出」）。

**实测（`SemanticWorldVision(max_distance=999)` + `find`，即 #51/#52 方法）**：
| 语义 | 距离 | 门 |
|---|---|---|
| `POPUP_GENERIC_REWARD_HEADER` | **16** | 16（恰好卡门） |
| `BTN_CLOSE`（= `CLOSE_POPUP` 的目标） | **28** | 6 ⇒ **解析不出** |
| `BTN_DISMISS_INTEL_REWARD` | **2** | — （roi x0.36 w0.28 **y0.90** h0.07 = 页脚那条） |

**两个关键结构事实**：
1. **五个 `DISMISS_*_GENERIC_REWARD` 的动作目标完全相同**（都是 `POPUP_GENERIC_REWARD_HEADER`，
   `skills.py:169-174`）⇒ `brain.py:397` 那个 `TRAIN`/`RESEARCH` 白名单**在机械上什么也没保护**，
   只是限制了这**一个**行为在什么 goal 下被允许。**拦住弹窗的不是"怕点错"，是名义上的域名区分。**
2. **执行器能传坐标**：`skills.py:426` 的 `Action("SWIPE", "0.50,0.62,0.50,0.30", …)` 就是字面归一化坐标，
   且已在生产跑通。⇒ 项目笔记那句"执行器只支持 TAP_SEMANTIC（无绝对坐标动作）"**对点击成立、对坐标输入不成立**。

**操作者已同意的最小修法（三步，尚未实施）**：
1. 加一个 **goal 中立、点固定归一化点**的动作种类（复用 SWIPE 的坐标通路），落点取页脚退出带 **y≈0.90**、避开三个奖励图标；
2. 把 `GENERIC_REWARD` 的兜底从 `SAFE_STOP`（`brain.py:412`）指到它——**仍不猜域名技能**，所以
   "不许猜域"的守卫意图保留，只是不再把弹窗留在屏幕上；
3. 同步更新两处守卫并写清理由：`check_wiring` 的 **`OK brain: reward popup without goal context stops rather than guessing`**
   与 `tests/test_reward_popup_source.py::…::test_without_goal_context_it_stops_rather_than_guessing`。
   **理由**：关掉一个自称"点任意位置退出"的弹窗不是猜，是**它自己声明的退出方式**。
   ⚠ 只做前两步会留下 `check_wiring` 红 ⇒ **必须三步一起做完再提交**。

**仍未完成**：情报/体力的**真实剩余数与体力读数**（需把情报列表与 MAP 两张画面调出来读）；普通打野出征；
`337bdc4` 的失败让位现场；工作周期的巡视器与角色切换；以及**重载的真正修复**（接替者必须由
**不在待关闭进程树内**的机制驱动，如 WMI `Win32_Process.Create` 或计划任务）。

## 21:30 会话：按 #64 续办入口实施（奖励弹窗落点）

**做了**（commit `e8efa2d` → `d2ba7b6` → `48d4640`，已 push）：

1. 五个 `DISMISS_*_GENERIC_REWARD` + 新增 goal 中立技能 `DISMISS_SHARED_REWARD`
   一律改点**页脚退出带** `BTN_DISMISS_INTEL_REWARD`（落点 (360,1197)）；
   绑 `verify_popup_closed`、进 `VERIFIED_ATOMIC`。
2. `brain.py`：goal 说不清页面的兜底由 `SAFE_STOP` 改成该技能；`TRAIN`/`RESEARCH` 的
   `CLOSE_POPUP`（目标 `BTN_CLOSE` 实测 28/门 6，打不中）一并换掉。
3. 守卫三条 + 两个测试文件同提交改完（`check_wiring` problems: 0）。

**真机已试**（轮次 `20260920_211101_334048`、`20260920_211621_887798`）：
`DISMISS_INTEL_GENERIC_REWARD` 用新目标连续 4 次 verifier OK、after=INTEL 0.98。
体力本轮 513→501→491，**确实在消耗**。

**关键更正（重要，勿再引用旧说法）**：#64 原文/补充写的"横幅点了无效"**是错的**。
第一手帧（`dataset/truth_audit/reward_popup_exit_20260920/key/03_*`）显示点完横幅后
「获得奖励」**已经关掉**，露出的是**第二个**弹窗「探照灯升级」。
**真主因是"识别用 `横幅 or 页脚`、点击只用横幅"**：横幅 14/16/18/20/26 骑门（门 16），
页脚 0~2（门 8）⇒ 页脚认出弹窗、点击却解析不到横幅 ⇒ `SEMANTIC_TARGET_NOT_VERIFIED`
（旧目标 51 次失败里 45 次是这种"点都没点出去"）。

**新开未修**：#65 第二个弹窗（探照灯升级，横幅 26/页脚 22，识别不出 ⇒ 整轮结束，
**这是 13:10:26 失败的真因**）；#66 横幅解析成功也不保证关得掉；#67 在非弹窗页面上跑弹窗技能；
#68 `intel_pin_centers` 只认紫/蓝/橙，情报帧上返回 4 而板上有 9（灰/绿 pin 全漏）。

**两个读数已按口径取到**（抓帧 + 看图）：体力 **491**（地图帧 21:21:36）；
情报页 下次刷新 02:40:54 / 右上 513 / 等级 7 / 5/90 / 板上 9 个 pin。
P0「体力降到 30 以下」仍未达成。

**教训（本会话自己犯的）**：先按 #64 的叙述写完提交（含"横幅点了无效"），
**看帧之后才发现那句话是错的**，只能再用一个提交更正。
⇒ **引用任何"某控件无效"的说法前，先看那一次的 after 帧**；handoff 里的因果句同样要当假设。

## 22:30 会话续：#68 情报 pin 计数 + 体力路线台账

**#68 已修**（`winter_agent_v2/intel_pins.py`）：三个颜色掩码 → **五个**（加 GREEN、加中性灰/银），
再加**宽度上界 130**（真 pin w 51–107 vs 页标题「情报」w 165–166）与**中性类专属位置下界 `BOARD_TOP=200`**
（语料里真 pin 最小 y=261；中性假阳性是标题 y 73–85 与左上 HUD y 38–57）。
**下界只作用于中性类**——加到所有颜色会冒"上移板面的真彩色 pin 被顶掉"的风险，那正是本 issue 要防的漏计。
判据淘汰过程（别再重做）：饱和度 50/40/30/25 都留标题、20 连真 pin 一起去掉；橙色底环丢一个真灰 pin。

**真机闭环**：回查发现 2026-09-20T14:03:18Z 那一帧被读成 `available_count: 0`
⇒ `NOT_AVAILABLE` ⇒ `CLEAR_INTEL=COMPLETE`。该帧**旧判据 0 / 新判据 5**（4 绿 + 1 灰）。
已归档发布 + 钉进测试。全语料 435 帧：读成 0 的帧 **1 → 0**，板外录取 **0**，找回 **397 绿 + 247 灰**。

**体力路线台账**（新 `knowledge/strategy/stamina_routes.json`，已登记 _index）：
采集实测 **0 体力(54/54)**；情报三路 10/10/12；**打野显示 10 实际 7**（最便宜）。
每次成功的体力 = 消耗 ÷ 真机成功率 ⇒ 打野 **11.3**(88.9%, n=54) < 英雄之旅 **14.7**(68%, n=25)
< 营救幸存者 **22.5**(53.3%, n=15)。**但冰原巨兽未实现**（START_RALLY/JOIN_RALLY 无 VERIFIED_ATOMIC 条目
⇒ 永不调度）、**打野被识别层卡死**（#41/#46）⇒ "做完情报用剩余体力去打野"今天落不了地，
**先修打野识别**才是唯一路径。**未算奖励量级**，别当价值已证。

**新发现 #69**：操作者的体力顺序在代码里写了两遍 —— `operations_policy.choose_stamina_goal()` **无任何生产调用者**，
`runtime.py:876` 的内联 route 映射才是生效的那处。同文件 `choose_troop_rotation`/`choose_shield`/`choose_healing`/
`reward_candidates` 同样无调用者。

## 22:45 会话续：通用 UI 语义盘点（操作者一次性许可）

**没建第二套系统**（操作者明确禁止）。做的是**盘点 + 真机取证 + 只补被量出来的缺口**。

**语料**：`dataset/truth_audit/ui_semantics_corpus/` 244 帧，**页面标签由生产自己打**
（`episodes.jsonl` 的 `state.page`，conf≥0.9），14 页各 20 帧；工具 `tools/ui_semantic_evidence.py`
（全帧 OCR → `knowledge/ui/semantic_evidence.json`）。

**数字**：模板 403 条/230 语义；技能 95/绑定 79；被技能点名的语义 71（59 有模板，12 派生解析器）；
171 个模板无技能使用；**3 个语义被 ≥2 个技能共用**（`BTN_DISMISS_INTEL_REWARD` 7 个、
`BTN_INTEL_VIEW_TARGET` 3 个、`BTN_BEAST_START_MARCH` 2 个）；词典 96 候选 → **57 有视觉证据 / 39 纯候选**
（而 39 恰是通用那一类：`BTN_BACK`/`BTN_CLAIM`/`BTN_CONFIRM`/`STATUS_*`/`RED_DOT_*`）；
45 候选在 244 帧上被 OCR 确认；**133 个跨页字符串词典完全没命名**（底部导航、`奖励`、`前往`、`2/6`）。

**两条"不要做"**：
- **`前往` 不要合并成通用按钮**（4 页出现、指向各异）——项目现在按页+对象作用域处理是对的。
- 不要把词典做成第二份事实源；`tools/ui_semantic_ledger.py` 是**派生**的（字段缺失一律 UNKNOWN）。

**新守卫 + 新缺口 #71**：`check_wiring` 把"可调度技能点名了视觉层解析不出来的目标"钉成**精确集合**
（新增会红、修好也会红直到删条目）。全仓只有一条：`RESEARCH -> BTN_START_RESEARCH`。
**不是缺裁剪**：科技研究路线落到**科技树**（发展/经济/战斗 + `1/3` 徽标节点，整页无「研究」按钮），
该按钮只在选中节点后出现，而路线**没有"选节点"这一步**。`RESEARCH` 全史只跑 1 次且被 `QUEUE_BUSY` 挡住 ⇒ 缺口潜伏中。

**#68 真机复验完成**：22:41:39 那轮读 `{"status":"AVAILABLE","pins":3}`；同帧**旧判据 0 / 新判据 3**
（2 绿 + 1 灰），生产记录的 3 与新判据一致 ⇒ 跑的就是新检测器。修好前满板读 0，修好后读 3。

**AUTO 全程未阻塞**：14:00Z 后 106 个 episode，95 成功 / 11 失败（90%），当前仍在 `CLEAR_INTEL`。

## 23:55 会话：操作者 P0「情报已清完 → 转入剩余体力消耗」

**两个 P0 阻塞（都不是"路线没写"）**，用项目自己的 API 读出来的：

1. **僵尸开发作业 `17f1c743`**（`SPEND_STAMINA_ON_BEAST|NO_GOAL_PROGRESS|SELECT_INTEL_PIN`，14:39:05Z 提交）
   挂 `WORKING` 但 `detail="Idle background coding session with no task"`，把 `SPEND_STAMINA_ON_BEAST`
   钉在 `DEVELOPMENT_PENDING` ⇒ `AVOID_STAMINA_WASTE` **永不入调度**，还占住唯一并发槽让另外 5 个升级 `CONCURRENCY_WAIT`。
   实测进度钟冻结 **34.6 min**（阈值 20）⇒ **项目自带的 45 分钟超时规则在 15:24:15Z 自动 cancel 它**
   （15:24:18 由等待中的另一个作业接替），**没有手工改任何状态**。
   作业落定后 `_capability_deferral` 给 `RUNNABLE` ⇒ 门禁自动放行 ⇒ **真机在 15:25:36Z 自动转入了打野路线**（27 步）。
2. **情报这一轮不是"做完了"，是"打不动了"**：板上剩 1 个 pin，开出来是 `BLOCKED/MASTER_BOUNTY/lv20`
   （recommended 189M vs 本角色 1.6M）。`untried_pins=1` 意为"本轮没点过"不是"打得过"，
   而 pin 在任务消耗后**不消失** ⇒ `goal_progress` 恒 false。已落进 `knowledge/game/intel.json`
   的 `round_state`（`ROUND_CLEARED_AWAITING_REFRESH` + 下次刷新 ~09-21 00:00 GMT+8）。
   **有界性已实测**：`DEFERRED` 走 `no_progress_probe_minutes=30` 探测窗，+5 分钟即放行 ⇒ 不是永久标完成。

**第三处（本次真正改的）**：路线到不了目标。`SCAN_MAP_FOR_BEAST` 是无收敛条件的平移
（全史 420 次、`goal_progress` 恒 false），而能到目标的两种手段**都是逐物种的**
（麝牛9 / 猛犸象5 的精灵模板）。患者帧上：客户端**印着** `霜鳞避役`（0.89）+ 徽标 `20`，
`lookup_by_name` 命中 —— 然后 `is_dispatchable=False`（该行 `dispatchable:false`/`UNVERIFIED`）。
⇒ **识别全通，卡的是一道手写的逐物种预批准开关。**

**改法（操作者："不要只针对某个物种，只要打得过就可以打"）**：判定换成**客户端自己的判定**——
`may_evaluate`（能不能点/开卡，身份够了就行）vs `is_dispatchable`（能不能花体力，
要帧上有 `本次出征胜券在握`）。实测拒绝（雪豹29 红判定）**比改前更严**（连点都不点）。
新增 `SELECT_BEAST_TARGET_LABELLED`（`TAP_SEMANTIC: BEAST_ON_MAP`，落点由名字标签框实测得出），
绑 `verify_beast_card_opened`；后面（卡面攻击 → 阵型页 → DISPATCH_BEAST）**全部是既有的、物种无关的**。

**同一读取上的另外三个缺陷**（第一片修完后真机跑了 27 步、平移 5 次、`beast` 全空，
而**同期帧证明野兽就在画面里**，于是查出来）：

| # | 缺陷 | 实测 | 修法 |
|---|---|---|---|
| 1 | 标签带**把整只野兽裁掉**（旧 `x .05-.50, y .45-.80`） | 40 帧整帧 OCR：只有 2 帧读到兽名，**两帧都在带外**（`霜鳞避役`(0.516,0.283)、`冰霜古猿`(0.724,0.243)）| 放宽到可玩区 `y .12-.80`；闸仍是**兽名白名单** |
| 2 | 等级是身份键的一半 | 该兽徽标是 **19**，表里只有 `level 20` ⇒ `lookup_by_name(name,19)` 返回 None ⇒ **名字读准也没目标** | 身份改为**按名字解析**；等级降级为数据（`level`/`table_level`/`level_matches_table`）|
| 3 | **单字误读**杀死整次读取 | OCR 读成 `霜解避役` **0.78**（< 0.80 门槛，且与 `霜鳞避役` 互不为子串）⇒ 两道闸同时拒 | 新增"**恰好一字之差且唯一**"模糊匹配（字差 2 不算、两个候选算 None），该路径下限 0.75 |

**⚠ 又撞到同一类错误**：`tests/test_beast_label_recognition.py` 的 `NEGATIVES` 里有两帧被当作
"没有野兽"，放宽带子后两帧都读出了兽名。**裁出来看：冰蓝色兽体和名牌就画在那里**（`key/04_*`）。
⇒ 那是**被旧带子挣来的标签，不是帧的事实** —— 与"把满板情报读成空卡"同一类。
已挪进正例，并把"沉默"属性改成用**打桩文字**钉（不依赖任何一张"某人以为空"的图）。

**仍未做 / 不许当完成**：`SELECT_BEAST_TARGET_LABELLED` **真机 0 次**（点标签→开卡→攻击→阵型→出征
一次都没跑过），状态 **CANDIDATE**。`冰霜古猿`（0.98）**没注册**（等级/是否普通野兽/代价全未观测，
没有替它编字段）。提交 `c9d5c53` → `6ad132b`。

## ⚠ 更正（同一晚，真机回来后）：这一跳**真机跑过 1 次**，而且**目标类别错了**

真机 1 次尝试（`2026-09-20T16:10:04Z`，rev `82cdf49`，goal `AVOID_STAMINA_WASTE`）：

```
before  MAP  beast={label_text:霜鳞避役, level:20, source:BEAST_LABEL, tap_norm:[0.5181,0.7778]}
action  TAP_SEMANTIC BEAST_ON_MAP
after   MAP  beast={label_text:等级7霜鳞避役, tap_norm:[0.5021,0.3946]}   ← 卡面已开
result  FAILURE / BEAST_TARGET_SELECTION_NOT_PROVEN
```

**打开 after 帧看图**（`key/07_*`）：卡面写 `等级7 霜鳞避役` · **推荐实力 683,100,000** · **`[集结]` 25**。
⇒ **`霜鳞避役` 是集结目标（冰原巨兽一类），不是普通野兽**（普通野兽卡面给的是 `攻击`、代价 10 体力）。

⇒ 这次尝试**一半成、一半败**，必须分开说：
- ✅ 标签读数在真机上有效（conf 0.875，落点 (373,995) 与整帧 OCR 一致）；
- ✅ **点击真的发出去了、卡面真的开了** —— 这是这一跳的 LIVE 证据；
- ❌ **目标类别错了**：开出 rally 卡，普通打野链（`ATTACK_BEAST_CARD` 要 `BTN_BEAST_CARD_ATTACK`）消费不了，
  所以 **`BEAST_TARGET_SELECTION_NOT_PROVEN` 是验证器"判对了"**，不是 bug。

**新增未修缺口 #72**：地图上"站着且印名字"的兽**可能是集结目标**，光靠名字区分类别。
区分办法只有两条：① 卡面自己画的控件（`攻击` vs `集结`）；② **操作者指的那条路 —— 搜索面板的
`野兽` 页签按等级搜**（`beast_5_found.png` 人肉验证过；失败模式文件规则 3 也写了"客户端自带导航优先"）。
两条都不该无真机校准就实现。另：`START_RALLY`/`JOIN_RALLY` 仍无 `VERIFIED_ATOMIC` ⇒ 集结目标无处派发。

**教训**：**"读到了名字 + 点开了卡"不等于"找到了对的那类目标"。**
在把一条链接上去之前，必须先确认**目标的类别**（这次的卡面控件就是类别证据），
否则接出来的是一条把路线引到另一条路上的链（本次：从"打野"引到了"集结"）。
