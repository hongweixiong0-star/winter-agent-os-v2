# 2026-09-27

## 凌晨会话四（巨熊时间落定 + BTN_DISPATCH L4，HEAD 30e9ea1）
- **巨熊正式时间（客户端直读）**：常规活动→巨熊行动「预约自动开启：2026-09-28 20:00」。reserved_start=2026-09-28T20:00:00+08:00，source=CLIENT_DIRECT_SCHEDULE，confidence=CONFIRMED，经 event_schedule.record_reservation/save 写入。旧值 2026-09-27T12:59:58+08:00（冷却推算）标 INVALIDATED/COOLDOWN_END_DERIVED，记入 calendar_corrections(kind=RESERVATION_INVALIDATED)。wake 阶梯实测：T30=19:30(40) T15=19:45(200) T5=19:55(2000) T1=19:59(2000) OPEN=20:00(4000)；readiness 12/12 READY。证据 knowledge/events/bear_hunt_time_20260928.json；T-30 须重读巨熊行动页核对（核对≠重置为 UNKNOWN）。
- **BTN_DISPATCH → L4（真机）**：新工具 tools/device_dispatch_l4.py：野外视图→资源搜索(模板0.845)→提交(1.0)→采集(1.0)→编队页→**生产 resolver** tap(551,1213)→MAA 点击→离页，地图见 行军 1/4 + 17公里 真实行军+倒计时。formation.py 真帧验证（出征确认、3兵种行、3英雄槽）。DISPATCH_READY=LIVE_VERIFIED。无体力消耗；采集行军保持生产（1/4 队列），巨熊 T-5 前如需队列可撤回。
- **SEARCH_RESOURCE 第一链节补上**：BTN_OPEN_RESOURCE_SEARCH TEMPLATE 节点注册（BTN_OPEN_RESOURCE_SEARCH.png，thr .78，1/1 正 0/3 负）。
- **诚实拒绝**：PAGE_MAP 城镇/野外 chip RapidOCR 6 帧只读出 3 帧→REJECTED_VALIDATION，路由条目撤销（避免 MAA 接管 OPEN_MAP 造成回归），改判 两态 TEMPLATE 待做。
- **runtime.py 精确入库**：用 difflib 生成仅含两条本会话 hunk 的最小 patch（OPEN_TECH_TREE verifier 绑定 + BTN_LAB_RESEARCH 当前帧 resolver）`git apply --cached` 入库 dbfbc56；Codex 其余 706 行未提交改动原样保留。注意：Path.write_text 会把 \n 写成 CRLF，生成 patch/重写 JSON 必须用 write_bytes；timed_event_readiness.json 曾被 indent=1 全文件重写，已修复为 indent=2 并 amend。
- AUTO 全程 GOAL_RUNNING/AUTO_RUNNING 未中断（当前在训练营地）；科研队列 IDLE，KEEP_RESEARCH_PRODUCTIVE L5 等 OPEN_TECH_TREE 被 worker 加载后自然达成（control plane 已发 RELOAD_REQUIRED）。
