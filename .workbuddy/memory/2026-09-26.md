# 2026-09-26（宿主会话路径 · 仅当日流水）

> 本目录是 WorkBuddy 宿主注入的会话工作路径，**不是长期事实源**。
> 今日完整流水与结论见：`.workbuddy-ai/memory/2026-09-26.md`

本日事项：安装并审计 MuMu 官方 `mumu-control` skill（结论 Benign，9 文件与源字节一致），写入 `knowledge/tooling/tool_registry.json` 的 `MUMU_CLI` 条目（状态 CANDIDATE / READY_FOR_LIVE_VERIFY，未完成真机验证），并给 `.gitignore` 增加 `.codebuddy/`、`.agents/` 以保护公开镜像。

待操作者裁决：是否授予 `DEVELOPMENT_VALIDATION_DEVICE_LEASE`，以便在 V2 让出设备后做一次真机截图 + 一次低风险点击。

### 桌面 V2 启动失败排障（同日凌晨）

根因**不是代码**，是三个前置条件缺失：`tools/preflight.py` 的判定式为
`ok = ADB已连接 AND 分辨率==720x1280 AND 前台包==com.gof.china`，三项缺一即 FAIL 并主动阻止启动（设计如此，拒绝降级运行）。

实测修复顺序：实例未启动 → `mumu-cli control -v 0 launch` → `adb connect 127.0.0.1:7555` → 前台是桌面 `app.lawnchair` → `monkey` 拉起 com.gof.china → preflight 由 FAIL 转 **PASS**。

三个可复用结论：
1. **`mumu-cli info` 的 `is_android_started`/`adb_port` 不可信**：实例已在线且 adb 可连，info 仍持续报 `false`/`None`。判断实例是否就绪只能以 `adb devices` 为准。
2. **实例选型**：`mumu-cli info -v all` 显示 index 0 =「MuMu安卓设备」(Android 12)、index 1 =「部落冲突安卓15」。V2 的 serial `127.0.0.1:7555` 与 index 0 对应。**严禁按厂商公式 `16384+index*2` 猜端口。**
3. **开发工具无法留住 GUI**：先后三次由本会话启动面板（`cmd //c start`、`PowerShell Start-Process`、项目自带 `tools/_launch_panel_detached.py` 的 `DETACHED_PROCESS`），均在调用结束后被回收，PID 15480/23016 归零。项目早有记录（2026-09-18 panels 24936/25408）。**结论：持久 GUI 只能由操作者从桌面路径双击 `Start-Winter-Agent-V2.cmd` 启动，AI 侧只负责把前置条件修到 PASS。**

差评点自查：过程中一度看到 Python312 路径的 pythonw 便怀疑是 2026-09-17 那个"错误解释器"P0 复发，查 ParentProcessId 后证实是 venv `pythonw.exe` stub 派生真实解释器的**正常行为**，已收回该判断。

租约：本轮以 `WORKBUDDY_DESKTOP_BOOT` 正常 acquire/release（result=ok），已归还，state 回到 `V2控制中`。

### V2 极速完善轮（凌晨，测试小号 1171757165 连续真机）

沿用 NativeISTER: 巨熊 P0 修通 —— 查出根因是 `learning/timed_event_schedule.json` 的 `roles` 为空数组导致无到点唤醒，沿 联盟→联盟领地→特殊建筑→狩猎陷阱 读到活冷却并建立真实预约 `2026-09-27T12:59:58Z`（北京 09-27 20:59:58），提交 `1e9b2ef` 并推送。真实放飞灯笼 1 次（剩余 50→49，真消耗）；发现全新页面联盟商店与已开放活动「明月的盛典」。详见 `.workbuddy-ai/memory/2026-09-26.md` 与交接 `12_WORKBUDDY_HANDOFF_2026-09-26.md`。

三条可复用教训：① 联盟页连按返回 ×3 会触发退出游戏对话框，须走底部导航；② 本环境启动的 GUI 必被回收，持久 AUTO 只能人工双击启动；③ `semantic_dictionary.json` 的 `records` 是 list 不是 dict，写过一次的破坏已精确还原。

**AUTO 当前未运行**，设备空闲 —— 需操作者桌面双击 `Start-Winter-Agent-V2.cmd` 恢复（preflight 已是 PASS）。

---

## 接续：批量补全缺失 Pipeline 与运行时自主生成（提交 `3905e55`，已推送）

闭合「缺失节点只能等人手写」：运行中发现 unresolved 控件 → `pipeline_autogen` 派生节点 → 写回路由表 → 下次解析命中。
已提交 9 文件 / +1833 行：`pipeline_autogen.py`、`executor_router.py`（按 kind 分派 OCR/模板 + 修 `save()` 删 `not_migrated` 的严重 bug）、
`runtime.py` 的 `_maybe_autogen_node` 钩子、`backend_routing.json` 5 个 `promoted:false`/P3 节点、
`tools/pipeline_coverage.py`、`tools/autogen_harvest.py`、`tools/autogen_closed_loop.py`、2 个测试文件（27 项全通过）。

**关键操作教训（可复用）**
- 他人未提交改动与自己的改动混在同一文件时，用 `git hash-object -w --path=<path>` + `git update-index --cacheinfo` 把
  「只含自己改动的 blob」放进索引提交，工作区保留完整版 —— 既按范围提交又不毁掉别人的工作。
- 模板必须按**裁剪到屏幕后的同一 ROI** 切，否则 MAA 报 `templ size is too large`；PIL `crop` 用 `(l,t,r,b)`。
- 底部导航真实 y=1250；进子页采帧前必须先返回。
- 生成的节点必须标 `promoted:false` 且不得 P0：采集验证 ≠ 语料 A/B 测量。
- `tests/test_live_runtime.py` 的 4 项失败在 HEAD 上同样存在（基线），不归本轮。

覆盖现状：137 技能仅 4 个真 MAA 节点，16 个 goal 0 个 A 类。巨熊预约仍在，**尚未在真实窗口参加**，不得虚报。

---

## 接续（07:46 段）：巨熊开窗前离线验证收口（提交 `6f415a3`）

- Git 检查：Codex 已推进 5 个提交（autogen 执行链 45d5f1c/8a42332、巨熊 START/JOIN 经 MAA 接线 b5c2c51 等），本地与远端同步。
- AUTO 状态：操作者已启动（pythonw PID 16992，operator_intent=RUNNING），今日运行 127 步成功，失败集中在 BEAST_DISPATCH/SAFE_BACK 未证明（已知缺口）。
- 核实巨熊门禁：陷阱详情页用**显式「距开始」标签**写 SCHEDULED/seconds_to_start（ocr.py:966），冷却文本不会误判 ACTIVE；`bear_phase` 相位分层成立。
- 唤醒链路：`event_schedule.load()` 读到 role 1171757165|BEAR_HUNT 预约，`seconds_until_next_transition()`=36.64h → 09-27 20:59:58 北京。
- 离线回放（交接缺口 #2 的离线半）：09-09 真实集结列表帧上 `read_rally_list` 解析出目标/发起者/倒计时/容量/**行自己的加入按钮**，有可加入行 → JOIN_RALLY；满员/不可读目标/无行 → 如实拒绝。固化为 `tests/test_bear_join_decision_replay.py`（帧 LOCAL_ONLY，克隆上自动跳过），3 项通过。
- 仍待真机窗口：START_RALLY/JOIN_RALLY 的真实执行、集结列表滚动、开放判据实读。未碰设备，AUTO 全程持租约。

---

## 接续（07:56 段）：接管 Codex + 采集器修复 + 批量接线（提交 `03b49fd`）

**闭环已证（引用证据，不虚报）**：`OPEN_ALLIANCE_TECH_FROM_HOME` 节点 trigger=`runtime unresolved after 1 attempt(s)`（AUTO 运行中真实触发生成），今日 06:41–07:09 共 6 步经 MAA 执行且 Verifier 通过（failure_type=None）。发现→生成→注册→执行→验证 全链活体闭环。

**采集器两处根因修复（tools/autogen_harvest.py）**：
1. `declared_labels()` 用复合 cn 名（领取挂机收益）匹配 OCR token，永远 0 命中 → 改为按字典 `ocr` 词表逐词。
2. 泛词错上下文：地图帧的「联盟」被接线成邮件页签（3 帧全含→无负样本）；探险页「领取」被当成联盟宝箱领取。加两道闸：**无负帧不接线**；**命中页必须在语义声明的 pages 内**。两处错接线均已回滚。

**导航教训**：主视图（主城/地图）按 BACK 必弹退出对话框并污染后续点击；采集路线改用常驻底部导航，全屏子页才用 BACK。

**本轮新接线（均 promoted:false/P3，harvest 验证）**：`DAILY_CLAIM_REWARDS`（每日任务·一键领取）、`EXPLORATION_IDLE_CLAIM`（探险·领取）。MAA 真节点 4→12→14。

**新页面发现（DISCOVERED，未注册 CANDIDATE）**：地心探险（大地之心，15:33:10 后开启，地下科研所/装备锻炉/商店/排行榜）——对应台账 D 类 `LABYRINTH_DAILY`。帧存 `dataset/raw/autogen/20260926_002647_help.png`。

**AUTO 停转根因（重要）**：运行中面板加载 `8a42332`，被 `b5c2c51+` 取代后面板于 07:29 自置位 `RUNTIME_RELOAD_REQUIRED` 并暂停（learning/CONTROL_PLANE_RELOAD_REQUIRED.json）。**需操作者双击 Start-Winter-Agent-V2.cmd 重载**（AI 代启会被回收）。另有多例「连续 3 轮无进展」DEFERRED（READ_MAIL_TABS/READ_DAILY_PROGRESS/CLAIM_IDLE_INCOME，签名含 OPEN_ALLIANCE_GIFTS 疑似误路由）——新接线节点重载后应直接改善。

---

## 接续（08:47 段）：桌面双击重启失败排查与恢复

- 根因链：MuMu 实例 0 未启动（is_process_started=false）→ ADB 无设备 → preflight FAIL（core blocker: device）→ 启动脚本按设计阻止启动（无窗口停留时操作者感知为"无反应"）。
- 恢复步骤：MuMuManager control -v 0 launch → adb connect 127.0.0.1:7555 → monkey 启动 com.gof.china 到前台 → preflight PASS。
- 旧面板 PID 16992（07:29 自暂停等待重载）仍占单实例 → 已终止；顺带发现并核对一对 launch_panel_logged 启动，desktop_startup.log 证实 venv 解释器，无错误解释器进生产。
- 08:52 AUTO 恢复步进（CLAIM_OFFLINE_REWARDS 成功，OPEN_ALLIANCE_TECH_FROM_HOME autogen 节点经 MAA 执行通过）；RUNTIME_RELOAD_REQUIRED 标记已被消费。
- 教训：双击启动"无反应"优先怀疑 preflight 的 device 段——MuMu 主程序开着 ≠ Android 实例已启动。

---

## 接续（13:14 段）：81 缺口收敛——AutoGen=0 根因修复 + AutoRepair 落地（提交 `963929b`）

**AutoGen=0 根因（证据定位，非猜测）**：钩子的 cn 文本守卫读语义字典，而字典（BTN_/PAGE_/STATUS_ 等 UI 元素 id）**不声明任何 81 个缺口语义**（实测 18 个采样全 miss）→ `_semantic_cn_text` 返回空 → 钩子在生成器前退出。修复：回退到 gap queue 的 `visible_words`（`declared_gap_words()`，31/81 有 CJK 声明词）。

**AutoRepair 从 0 变为真实路径**：`_maybe_repair_node`——节点存在但定位失败 → 有界（每语义每轮一次）重裁模板 → **必须有负帧证明裁剪页面专属**（无负帧只记候选不接线）。EXPLORATION_IDLE_CLAIM 今日 3 次失败都在代码生效前，下次失败将触发首次真实修复闭环。

**L2→L4 证据**：INTEL_CLAIM_REWARDS 今日 5 步经 MAA 且 Verifier 全过（情报领奖=L4）；OPEN_INTEL 27 次成功、OPEN_DAILY/OPEN_MAIL 成功。

**采集改进**：harvest 工具标签源并入 gap queue；发现与 AUTO 赛跑问题（tap 后 AUTO 立即导航走），改用「自持租约导航采帧 + `--frame` 离线接线」。多跳会话脚本 /tmp/wb_nav_session.py。

**Git 分诊**：untracked 实测 858；根目录 `.probe_*`/`.fail_*`/`.full_*` 与 `tools/_*.py` 探针垃圾入 .gitignore（-77 项）。大头待分：LEARNING 222、KNOWLEDGE/perception 75、DATASET 126。

**GUI**：runtime.py 是 worker 文件——每 cycle 新子进程从磁盘 import，**修完下 cycle 即生效，无需操作者重启**；CONTROL_PLANE_RELOAD 仅 control_panel.py 变更才需要。

## 14:28-14:50 WorkBuddy 接管段（P0 收敛轮）
- **P0-2 完成**：50 个 no-CJK 缺口按真实 UI 特征分类 OCR16/TEMPLATE21/COLOR1/STRUCTURE5/LIST_DYNAMIC7（tools/classify_gap_recognition.py，规则表 v1，写回 queue 的 recognition.method）。pipeline_autogen 增 gap_recognition_method + ColorMatch 节点形状；autogen_harvest 增 --template-rect 裁剪路径（真实 matcher 验证）+ OCR 法 live-frame CJK token 发现；COLOR/STRUCTURE/LIST_DYNAMIC 候选-only。commit 848b0f1。
- **P0-3 进展**：AUTO 06:37 UTC 实跑邮件链 SELECT_MAIL_ALLIANCE_TAB → MAIL_CLAIM_REWARDS SUCCESS verifier_ok=True，after-state mail.status=CLAIMED badge_count=0 notification_cleared=True —— 邮件 L4 实证，L5 证据=领取后收件箱红点清零。
- **P0-4 部分完成**：queue 增 12 条巨熊集结 P0 条目（RALLY_LIST_OPEN/ROW_JOIN/FULL/START_RALLY/JOIN_RALLY_CONFIRM/HERO_SELECT/TROOP_SELECT/DISPATCH_CONFIRM/JOINED_STATE/BATTLE_REPORT/EVENT_ACTIVE_STATE/RALLY_LIST_ROW_TARGET），recognition.method 待活动 UI 实帧。Scheduler 唤醒机制已有（event_schedule.reserved_start → bounded_poll wake），待客户端实读 reserved_start。commit d269870。
- **P0-1 进行中**：租约 06:31 UTC 过期后 AUTO 恢复 GOAL_RUNNING；EXPLORATION_IDLE_CLAIM 自 03:05 UTC 后未再被调度，后台监控挂 20 分钟窗口。
- 租约竞态注意：本段早期 PIPELINE_AUTOGEN 租约压住 AUTO 至 06:31 UTC 才恢复。
- **关键发现**：EXPLORATION_IDLE_CLAIM 03:05Z 失败的 control `BTN_EXPLORATION_IDLE_CLAIM` 不在字典也不在 queue → 修复回退拿不到词会静默跳过。已用同面板兄弟控件 BTN_EXPLORATION_IDLE_CONFIRM 的实读词（领取/挂机收益/挂机时间）补进 queue（71f3af9），declared_gap_words 验证通过。下一轮该语义失败即触发首个真实 AutoRepair。后台 watcher XJMs8X 挂 80 分钟。

## 15:10-15:30 第二轮
- 修复可观察性：record_repair_event 写 learning/autogen_repair_events.jsonl（ENTER/SKIP/WIRED/ERROR + skip_reason）；runtime.py 最小改动在工作区（未提交，保护 Codex）。
- 实读改判：r2_alliance 帧印出「联盟战争/联盟宝箱」→ BTN_ALLIANCE_WAR/BTN_OPEN_ALLIANCE_GIFTS 改 OCR 回填词，harvest 验证接线 2 节点（OPEN_BEAR_RALLY_LIST/OPEN_ALLIANCE_GIFTS）= 集结备战第 1 条 L1/L2。
- MAIL_ROUTINE 判游戏侧 L5（learning/goal_l5_evidence.jsonl）：06:37Z 全链 SUCCESS+verifier，终态 CLAIMED/badge 0/红点清。Goal lifecycle 自动 COMPLETE 改造未做（brain.py 目标机需单独谨慎改，勿在本轮混入 runtime.py）。
- 巨熊预告 r2-r12 十一轮未实读到：事件中心页签=日历/冻土之王/全军参战/'!'(小游戏)；日历滚到底=堡垒争夺(09-29)；两行 09/30 起的图标行名字在屏外，横滑会重置列表。下轮路线：主页→联盟→联盟战争页内找巨熊入口；或等 AUTO 自然碰到。
- commit 1d95410。

## 15:52 关键事件：首个真实 AutoRepair 触发 + 根因修复
- 07:50:12Z EXPLORATION_IDLE_CLAIM 生产失败（SEMANTIC_TARGET_NOT_VERIFIED）→ repair ENTER → 声明词「领取」命中帧 → 候选生成 → **SKIP REJECTED_NO_NEGATIVE_OR_MISMATCH**。完整证据在 autogen_repair_events.jsonl：validation.errors=["NO_ADAPTER"]——validate 传了 adapter=None，门禁永远不可能过。
- 修复：repair 改用 self.maa_adapter（executor 同一实例）。mine-only blob 提交 e7a88c1（workspace 6524 行 Codex 工作原样保留）。
- 观察性已在生产验证：4 条真实事件（2 ENTER/2 SKIP 含 skip_reason、validation 报告、负帧清单）。
- watcher jMlieM 挂 90 分钟：等 repair WIRED 或 EXPLORATION 重新执行。

## 16:05 08:01Z 修复 0 命中根因离线破案
- 离线端到端（生产同款 MAA adapter + 08:01Z 生产修复帧）：BTN_EXPLORATION_IDLE_CLAIM 修复节点 validate **正帧 1 命中/负帧 0 命中** —— matcher 链路本身是好的。
- 生产 0 命中且 errors=[] 的原因：validate() 丢弃了 _try_match 的错误字符串（如 adapter 忙/未就绪的 MAA_RECO_FAILED/MAA_UNAVAILABLE），与「模板不在帧内」不可区分。已修：frame_errors 逐帧记录（负帧 NO_MATCH 不算错），commit 201de22。
- 下次生产修复带 frame_errors，可直接定位真阻断；watcher vhDklP 90 分钟。
- 注意坑：np.asarray(Path) 返回 object 标量会令 MAA 崩 IndexError（探针自身踩过）；正确读帧用 pipeline_autogen._load_rgb。

## 16:35 修复 0 命中真正根因：TEMPLATE 零余量 ROI
- frame_errors 观察性立功：08:22Z 修复事件显示正帧错误=干净 NO_MATCH（非瞬态故障）。看帧：领取按钮明明在，但比 08:01Z 下移约 30px（宝箱光效动画推动布局），而模板节点 ROI=裁剪原位 [564,827,107,80] 零余量——按钮漂出 ROI 即必 NO_MATCH。
- 修复：TEMPLATE 节点 ROI 一律外扩 48px（TEMPLATE_ROI_EXPAND），负帧验证守卫额外区域。跨帧实证：08:01 模板对 08:01+08:22 两帧 positive_hits=2/2、负帧 0（443d85f）。
- 三层修复链完整：adapter=None→live adapter（e7a88c1）；错误吞噬→frame_errors（201de22）；零余量 ROI→48px margin（443d85f）。下次生产修复预期直接 WIRED。watcher rwmxzd。
