# 2026-09-21

## 00:20 会话：导航与快捷入口全面发现（操作者一次性许可）

**没建第二套导航**（操作者明确禁止）。做法是**先派生、再在真机帧上量缺口**。
新增 `tools/ui_navigation_matrix.py` → `knowledge/ui/navigation_matrix.json`：把 96 技能 /
80 绑定 / 40 导航类入口 / 230 个有模板语义 / **171 个有模板却没任何技能点名**，
以及操作者点名的 12 类功能各有几条可走路径，全部**投影**出来（不手写）。

**逐条覆盖（可复跑）**：情报/灯塔 18 条路；邮件 6；城镇/野外快捷面板 4；地图搜索栏 4；活动 4；
兵营 3；科技 2；回城 2；体力HUD 2；采集点 1；**联盟捐献 0**；**巨兽/自动加入 0（连模板都没有）**。

### 三处更正（都有帧，别引用旧说法）

1. **"城镇快捷面板"不是路线用的那个**。`OPEN_POWER_OVERVIEW → BTN_OPEN_POWER_DETAILS → 提升`
   打开的是 **`加成总览`**（部队/建筑/科技/领主装备/英雄实力 + 战绩，各带数值）再进
   **`实力详情`**（五行进度条 + `提升`）——**这是属性统计链**。操作者描述的"带进度/倒计时/可用次数、
   带城镇-野外标签的快捷功能面板"**项目里零记录**，需要真机发现。
   帧：`runtime_auto/20260921_001508_805754/…_step_003_before`（加成总览）、`…_step_004_before`（实力详情）。
2. **训练路线不是被教学手指挡住的——那个理由是错的**。代码注释与 `verify_camp_menu_reobserved`
   都写"手指压着兵营、点了会跳地图"，依据只有 **2026-09-17 一次尝试**。**证伪**：
   `runtime_auto/20260920_093256_036374/…_step_005_before` 上**手指在、菜单也正常开着**
   （`16 盾兵营` + 径向菜单 `详情/立即完成/加速/训练`，手指此时指向 `训练`）⇒ **手指是引导**。
   真正差别是状态：stage A ＝金环亮无菜单（`TARGET_INFANTRY_CAMP_HIGHLIGHTED` d=2），
   菜单态 ＝`BUILDING_INFANTRY_CAMP`（d=8），**两者落点都在 (340,690)**。
   ⇒ 唯一未决问题：**stage A 该点哪里**（环中心试过→跳地图；手指尖下的兵营图标在 ≈(350,545)，
   **高 137 px，从未试过**）。**不猜**，本轮不改落点。
   **代价已量**：`KEEP_TRAINING_PRODUCTIVE` 127 步里 **45 步（35%）在 `WAIT_FOR_CAMP_MENU`**，
   真正到训练页只有 4 次；`MAX_CAMP_MENU_WAITS=2` ⇒ 每轮浪费 2 步 + 一整轮，已持续 3.25 天。
3. **快捷面板内容完全没读进 WorldState**：`POWER_DETAILS` 帧上 `observe` 返回
   `training={}` / `research={}` ⇒ 操作者 §四 要的"先从面板读可靠状态"**今天做不到**。

### 正面证据

- **同一个面板被两个目标复用**：`16:05:11 NAVIGATE_INFANTRY_CAMP`（训练用 `部队实力→提升`）与
  `16:15:55 NAVIGATE_RESEARCH_LAB`（科研用 `科技实力→提升`）是**同一条三跳链**，两条都 SUCCESS。
- 底部导航六个入口（`探险/英雄/背包/商店/联盟/野外`）真机可见；右侧八个活动入口 + 邮件/设置可见；
  左侧有 `通关探险第80关 (0/1)` 任务卡。**但这些里项目只覆盖 `OPEN_EXPLORATION`/`OPEN_ALLIANCE`/
  `OPEN_MAP`/`OPEN_MAIL`/`OPEN_DAILY`，其余零语义。**

### 守卫

`check_wiring` 新增一组：矩阵必须等于**重新派生**的结果（陈旧即红）、每个技能恰好一行、
**曾经有路的功能力不能掉到 0**（改名必须变红而不是读成"从来没有"）、已知缺失的功能必须仍被报告。
测试 `tests/test_navigation_matrix.py` 8 条。首个 bug 也钉住了：**`RESEARCH` 含 `SEARCH`**，
子串匹配把全部科研目标归进了"地图搜索栏" ⇒ 改用正则 `(?<!RE)SEARCH`。

**本轮未做**：没有减少任何导航跳数（那是导航行为改动，需要真机校准）；没有实现兵营页签切换
（另外两个兵营**零帧**，且 `TAB_TRAINING_LANCER`/`MARKSMAN` 都裁自蓝底格子、**d 都是 2，区分不了彼此**）。
提交 `35c9a24`。

## 01:00 会话：两件真机缺陷（联盟页整轮停机 + 训练 Stage A 落点）

### 一、联盟页整轮停机（P0，已修 `ecb0014`）

真机 16:32:37 现场：`agent_state=DEGRADED`、**一轮只有 1 步**、`stop_reason=alliance_state_unknown`，
goal `AUTO_DISCOVERY`、page `ALLIANCE`、`{"section":"HOME","tab":"VICTORY_LOOT","status":"UNKNOWN"}`。
而**那帧上明明画着 `联盟科技` 带 25 角标、`联盟互助` 带 6 角标、`联盟商店` 红点**。

**两个独立的错**：
1. **分支**：goal 驱动的联盟路线对 section HOME 有出口（进礼物页），但**页面驱动分支**
   （`world.page is Page.ALLIANCE`）**完全没有出口** ⇒ 别的 goal 站在这里直接落到兜底。
   历史计数：`section=HOME` **56 次**、`GIFTS/UNKNOWN` 14、`TECHNOLOGY status=None` 6、`HELP` 2。
2. **停机的代价**：`SAFE_STOP` 在 runtime 里**不是**"跳过这个观察"，而是记 DEGRADED 并 `return finish`
   ⇒ **一个没人能操作的页面结束了整轮**；而且没有任何动作把客户端挪走 ⇒ **下一轮开局还在同一屏**。

**修法**：用项目自己的 `_leave_foreign_page_once`（有界、绑 `verify_safe_back`）。
**没有**用"顺手点礼物"这条更显然的修法——那在本文件里已被证伪（研究 goal 下点了 12 次
`OPEN_ALLIANCE_GIFTS`，研究页一次都没读到）。

**⚠ 我第一版改错了，被两个既有测试挡住，它们是对的。** 它们的注释写着：DAILY 面板在 2026-09-16
拿到**同一个 Back**，理由是"unknown panel **stranded the client for every following run**"，
而 "**Alliance is untouched**"。⇒ 我做的不是新行为，是**把联盟页对齐到 DAILY 已跑了 5 天的形状**。
测试还钉住：**没有 goal 时是调度器在探测**，那里 Back 会看起来像在工作并挤掉真正有活的观察。
⇒ 范围收窄为**命名 goal**（`_leave_or_stop` 自己写的规则："leave ... if a NAMED goal ... else stop"）。

真机：`ecb0014b` 已跑 22 步**未停机**，但 `leaves_a_panel` **出现 0 次** ⇒ **该分支尚未真机触发**。

### 二、训练 Stage A（#74 结案，已修 `6dbc609`）

`KEEP_TRAINING_PRODUCTIVE` 127 步里 **45 步在 `WAIT_FOR_CAMP_MENU`**，到训练页 4 次。
"不点"的理由是**教学手指遮挡**（依据 09-17 唯一一次尝试）。**证伪 + 找到真因**：

**46 帧真机 stage A（5 个独立时段）**：模板落点 **46/46 = (346,682)**；金环中心 (313,585)（sd 4.3/4.0）。
环的 y 范围约 520–645 ⇒ **落点在环下方 37 px** ⇒ **落在建筑之间的空地** ⇒ 客户端当**地图点击**。
**手指从未覆盖那个点**（它覆盖的是环；`20260920_013343` 那帧**手指在、菜单也开着** ⇒ 手指是引导）。

### ⭐ 最有价值的一条：判别器是"环的尺寸"，而旧结论量错了量

项目有一对人工验证帧（文件名就是结论）：
`camp_with_gold_ring__click_opens_menu__20260908` vs `camp_with_officer_badge__click_jumps_to_map__20260917`。
旧测试据此断言"**信号区分不了两态**"，因为**模板距离 0.0 vs 8.0，而 8.0 正好压在门上**。

**实测**：开菜单那帧有 **205×112 的完整金环**；跳地图那帧只有 **7×15 的金色碎片**（军官徽章边缘）。
⇒ **差两个数量级。旧结论没错，但它量的是模板距离——换个量就分开了。**

**落地**：新 `winter_agent_v2/camp_ring.py`，在**匹配到的模板自己的 ROI 内**找环
（ROI 由调用方给 ⇒ **无写死坐标**；第一版全帧找金色返回了右侧活动栏的装饰，故必须加窗）；
**最小尺寸判据**；读不出 ⇒ **None 不兜底**（⇒ 路线等待，安全侧）。
接入：`vision` 写 `camp_tap_norm` → `runtime` 派生解析器 `TRAINING_CAMP_IN_RING`（带 HOME 守卫）
→ `skills` 把 `SELECT_INFANTRY_CAMP` 的目标改为它（**识别与点击分离**）
→ `brain` **先点一次**再走原有 2 次等待与让位。

**离线**：46/46 零退化；**开菜单帧给落点、跳地图帧被拒**；守卫 +6，`problems: 0`；28 条训练测试全过。
**⚠ 未验证**：真机从未点过环心。**09-08 不能当"环内有效"的对照**——该帧几何与 09-17
**相同**（tap 到环心 102 vs 103 px），它的"成功"很可能是按顺序推断而非实测。
真正的差别是**环存在与否**。状态 **CANDIDATE**。

### 另一条：`assertRegex` 的第三个参数是 msg 不是 flags

我写 `self.assertRegex(source, pattern, re.DOTALL)` ⇒ flags 被当消息丢掉 ⇒ `.` 不跨行 ⇒
测试静默失败。改用 `re.compile(pattern, re.DOTALL)`。

## 07:20 会话（重启后接手）：训练 Stage A 真机验证**失败**，已回退（`a6d1944`）

### 真机结果（决定性）

修复上线后 AUTO **真机跑了 1 次**该跳（`repo_rev 2f616406`）：

```
skill  SELECT_INFANTRY_CAMP   action  TAP_SEMANTIC: TRAINING_CAMP_IN_RING
before.training.camp_tap_norm = [0.441, 0.4551]   ← (317,583) = 环中心
action_backend = ADB                              ← 点击真的发出去了
result  FAILURE / INFANTRY_CAMP_MENU_NOT_PROVEN
after.page HOME, after.training = {}              ← 无菜单，且高亮环消失
```

**after 帧显示：菜单没出现，金色环消失** ⇒ 客户端把这次点击当"点空"。

**⇒ stage A 四次真机点击、四次全败**：三次环外 (346,682)（两次菜单没开、一次跳地图）+ **一次环内 (317,583)**。
**"瞄得更准"不是答案** ⇒ 撤回点击、回到等待；保留 `camp_ring.py` 与 `camp_tap_norm`（环的测量可靠：46/46 帧）。

### ⭐ 我昨晚错在哪（重要）

我把**几何推理**当成了**充分条件**：46 帧证明"落点在环外 103px"，于是改成"点环心"。
**真机一次就否掉了** —— **落点在环内 ≠ 客户端会响应**。

⇒ **新纪律**：改落点之前，先确认**"那个位置本来就有可交互目标"**，而不只是"落点在范围内"。
本次环内其实是 **5 个亮色块**（264/98/60/60/44 px），我点的位置紧邻其中**最小的一个**；
**教学手指指向的是环右侧那个带 `2` 角标的图标（测得 (379,592)）**——
但"5 选 1"**没有客观唯一判据**，所以**没有据此再改坐标**（那会是第 5 次盲试）。

### #82（新，未决）：那个状态到底是什么？

**假设（非结论）**：那不是**圆形**的建筑选中框，而是**椭圆**（rx=102, ry=56），画在**大型多部件建筑**上、
带 `2` 角标、**教学手指指着角标** ⇒ 更像**引导式步骤**。
**若成立** ⇒ `verify_infantry_camp_highlighted` 接受它就是**假到达**，训练路线前半段一直在报**从未获得的成功**。
**可判定的下一步（不需盲点坐标）**：① OCR 环内/附近建筑名，与 `knowledge/game/buildings.json`
（13 条，含 `INFANTRY_CAMP` 但**无位置/外观字段**）比对；② 查有无**教程/引导态**证据（项目**零 tutorial 语义**）；
③ 若确为引导态 ⇒ 记 `DEFER`，停止找点击点。

### 正面进展：用户 P0「消耗剩余体力」**真的在发生**

`AVOID_STAMINA_WASTE` 从昨晚 4 步 → **51 步（最活跃 goal）**，体力 **367 → 265（-102）**：

```
17:10:43 357 → 17:11:56 347 → 17:15:20 327 → 17:18:44 305 → 17:23:29 285 → 17:29:20 265
```

**真实消耗路径 = `DISPATCH_INTEL_BEAST`（情报野兽任务，每次 -10）**，
另有 `EXECUTE_INTEL_RESCUE_SURVIVORS`（-12）。**不是**"世界地图普通打野"。

⚠ **17:29 之后体力停在 265 已 6 小时**：情报板打完 + 世界地图打野仍被 **#72（集结目标混淆）**卡住
⇒ **目标 <30 远未达成**，且"情报耗尽后"的续航路径**仍然缺失**。

本会话总计（17:20Z 后）：**139 步 / 127 成功 / 12 失败（91.4%）**，覆盖 11 个 goal。
