# 2026-09-21

## 00:20 会话：导航与快捷入口全面发现（操作者一次性许可）

**没建第二套导航**（操作者明确禁止）。做法是**先派生、再在真机帧上量缺口**。
新增 `tools/ui_navigation_matrix.py` → `knowledge/ui/navigation_matrix.json`：把 96 技能 /
80 绑定 / 40 导航类入口 / 230 个有模板语义 / **171 个有模板却没任何技能点名**，
以及操作者点名的 12 类功能各有几条可走路径，全部**投影**出来（不手写）。

**逐条覆盖（可复跑）**：情报/灯塔 18 条路；邮件 6；城镇/野外快捷面板 4；地图搜索栏 4；活动 4；
兵营 3；科技 2；回城 2；体力HUD 2；采集点 1；**联盟捐献 0**；**巨兽/自动加入 0（连模板都没有）**。

### 三处更正（都有帧，别引用旧说法）

1. **"城镇快捷面板"不是路线用的那个**。`OPEN_POWER_OVERVIEW → BTN_OPEN_POWER_DETAILS → 提升`
   打开的是 **`加成总览`**（部队/建筑/科技/领主装备/英雄实力 + 战绩，各带数值）再进
   **`实力详情`**（五行进度条 + `提升`）——**这是属性统计链**。操作者描述的"带进度/倒计时/可用次数、
   带城镇-野外标签的快捷功能面板"**项目里零记录**，需要真机发现。
   帧：`runtime_auto/20260921_001508_805754/…_step_003_before`（加成总览）、`…_step_004_before`（实力详情）。
2. **训练路线不是被教学手指挡住的——那个理由是错的**。代码注释与 `verify_camp_menu_reobserved`
   都写"手指压着兵营、点了会跳地图"，依据只有 **2026-09-17 一次尝试**。**证伪**：
   `runtime_auto/20260920_093256_036374/…_step_005_before` 上**手指在、菜单也正常开着**
   （`16 盾兵营` + 径向菜单 `详情/立即完成/加速/训练`，手指此时指向 `训练`）⇒ **手指是引导**。
   真正差别是状态：stage A ＝金环亮无菜单（`TARGET_INFANTRY_CAMP_HIGHLIGHTED` d=2），
   菜单态 ＝`BUILDING_INFANTRY_CAMP`（d=8），**两者落点都在 (340,690)**。
   ⇒ 唯一未决问题：**stage A 该点哪里**（环中心试过→跳地图；手指尖下的兵营图标在 ≈(350,545)，
   **高 137 px，从未试过**）。**不猜**，本轮不改落点。
   **代价已量**：`KEEP_TRAINING_PRODUCTIVE` 127 步里 **45 步（35%）在 `WAIT_FOR_CAMP_MENU`**，
   真正到训练页只有 4 次；`MAX_CAMP_MENU_WAITS=2` ⇒ 每轮浪费 2 步 + 一整轮，已持续 3.25 天。
3. **快捷面板内容完全没读进 WorldState**：`POWER_DETAILS` 帧上 `observe` 返回
   `training={}` / `research={}` ⇒ 操作者 §四 要的"先从面板读可靠状态"**今天做不到**。

### 正面证据

- **同一个面板被两个目标复用**：`16:05:11 NAVIGATE_INFANTRY_CAMP`（训练用 `部队实力→提升`）与
  `16:15:55 NAVIGATE_RESEARCH_LAB`（科研用 `科技实力→提升`）是**同一条三跳链**，两条都 SUCCESS。
- 底部导航六个入口（`探险/英雄/背包/商店/联盟/野外`）真机可见；右侧八个活动入口 + 邮件/设置可见；
  左侧有 `通关探险第80关 (0/1)` 任务卡。**但这些里项目只覆盖 `OPEN_EXPLORATION`/`OPEN_ALLIANCE`/
  `OPEN_MAP`/`OPEN_MAIL`/`OPEN_DAILY`，其余零语义。**

### 守卫

`check_wiring` 新增一组：矩阵必须等于**重新派生**的结果（陈旧即红）、每个技能恰好一行、
**曾经有路的功能力不能掉到 0**（改名必须变红而不是读成"从来没有"）、已知缺失的功能必须仍被报告。
测试 `tests/test_navigation_matrix.py` 8 条。首个 bug 也钉住了：**`RESEARCH` 含 `SEARCH`**，
子串匹配把全部科研目标归进了"地图搜索栏" ⇒ 改用正则 `(?<!RE)SEARCH`。

**本轮未做**：没有减少任何导航跳数（那是导航行为改动，需要真机校准）；没有实现兵营页签切换
（另外两个兵营**零帧**，且 `TAB_TRAINING_LANCER`/`MARKSMAN` 都裁自蓝底格子、**d 都是 2，区分不了彼此**）。
提交 `35c9a24`。

## 01:00 会话：两件真机缺陷（联盟页整轮停机 + 训练 Stage A 落点）

### 一、联盟页整轮停机（P0，已修 `ecb0014`）

真机 16:32:37 现场：`agent_state=DEGRADED`、**一轮只有 1 步**、`stop_reason=alliance_state_unknown`，
goal `AUTO_DISCOVERY`、page `ALLIANCE`、`{"section":"HOME","tab":"VICTORY_LOOT","status":"UNKNOWN"}`。
而**那帧上明明画着 `联盟科技` 带 25 角标、`联盟互助` 带 6 角标、`联盟商店` 红点**。

**两个独立的错**：
1. **分支**：goal 驱动的联盟路线对 section HOME 有出口（进礼物页），但**页面驱动分支**
   （`world.page is Page.ALLIANCE`）**完全没有出口** ⇒ 别的 goal 站在这里直接落到兜底。
   历史计数：`section=HOME` **56 次**、`GIFTS/UNKNOWN` 14、`TECHNOLOGY status=None` 6、`HELP` 2。
2. **停机的代价**：`SAFE_STOP` 在 runtime 里**不是**"跳过这个观察"，而是记 DEGRADED 并 `return finish`
   ⇒ **一个没人能操作的页面结束了整轮**；而且没有任何动作把客户端挪走 ⇒ **下一轮开局还在同一屏**。

**修法**：用项目自己的 `_leave_foreign_page_once`（有界、绑 `verify_safe_back`）。
**没有**用"顺手点礼物"这条更显然的修法——那在本文件里已被证伪（研究 goal 下点了 12 次
`OPEN_ALLIANCE_GIFTS`，研究页一次都没读到）。

**⚠ 我第一版改错了，被两个既有测试挡住，它们是对的。** 它们的注释写着：DAILY 面板在 2026-09-16
拿到**同一个 Back**，理由是"unknown panel **stranded the client for every following run**"，
而 "**Alliance is untouched**"。⇒ 我做的不是新行为，是**把联盟页对齐到 DAILY 已跑了 5 天的形状**。
测试还钉住：**没有 goal 时是调度器在探测**，那里 Back 会看起来像在工作并挤掉真正有活的观察。
⇒ 范围收窄为**命名 goal**（`_leave_or_stop` 自己写的规则："leave ... if a NAMED goal ... else stop"）。

真机：`ecb0014b` 已跑 22 步**未停机**，但 `leaves_a_panel` **出现 0 次** ⇒ **该分支尚未真机触发**。

### 二、训练 Stage A（#74 结案，已修 `6dbc609`）

`KEEP_TRAINING_PRODUCTIVE` 127 步里 **45 步在 `WAIT_FOR_CAMP_MENU`**，到训练页 4 次。
"不点"的理由是**教学手指遮挡**（依据 09-17 唯一一次尝试）。**证伪 + 找到真因**：

**46 帧真机 stage A（5 个独立时段）**：模板落点 **46/46 = (346,682)**；金环中心 (313,585)（sd 4.3/4.0）。
环的 y 范围约 520–645 ⇒ **落点在环下方 37 px** ⇒ **落在建筑之间的空地** ⇒ 客户端当**地图点击**。
**手指从未覆盖那个点**（它覆盖的是环；`20260920_013343` 那帧**手指在、菜单也开着** ⇒ 手指是引导）。

### ⭐ 最有价值的一条：判别器是"环的尺寸"，而旧结论量错了量

项目有一对人工验证帧（文件名就是结论）：
`camp_with_gold_ring__click_opens_menu__20260908` vs `camp_with_officer_badge__click_jumps_to_map__20260917`。
旧测试据此断言"**信号区分不了两态**"，因为**模板距离 0.0 vs 8.0，而 8.0 正好压在门上**。

**实测**：开菜单那帧有 **205×112 的完整金环**；跳地图那帧只有 **7×15 的金色碎片**（军官徽章边缘）。
⇒ **差两个数量级。旧结论没错，但它量的是模板距离——换个量就分开了。**

**落地**：新 `winter_agent_v2/camp_ring.py`，在**匹配到的模板自己的 ROI 内**找环
（ROI 由调用方给 ⇒ **无写死坐标**；第一版全帧找金色返回了右侧活动栏的装饰，故必须加窗）；
**最小尺寸判据**；读不出 ⇒ **None 不兜底**（⇒ 路线等待，安全侧）。
接入：`vision` 写 `camp_tap_norm` → `runtime` 派生解析器 `TRAINING_CAMP_IN_RING`（带 HOME 守卫）
→ `skills` 把 `SELECT_INFANTRY_CAMP` 的目标改为它（**识别与点击分离**）
→ `brain` **先点一次**再走原有 2 次等待与让位。

**离线**：46/46 零退化；**开菜单帧给落点、跳地图帧被拒**；守卫 +6，`problems: 0`；28 条训练测试全过。
**⚠ 未验证**：真机从未点过环心。**09-08 不能当"环内有效"的对照**——该帧几何与 09-17
**相同**（tap 到环心 102 vs 103 px），它的"成功"很可能是按顺序推断而非实测。
真正的差别是**环存在与否**。状态 **CANDIDATE**。

### 另一条：`assertRegex` 的第三个参数是 msg 不是 flags

我写 `self.assertRegex(source, pattern, re.DOTALL)` ⇒ flags 被当消息丢掉 ⇒ `.` 不跨行 ⇒
测试静默失败。改用 `re.compile(pattern, re.DOTALL)`。

## 07:20 会话（重启后接手）：训练 Stage A 真机验证**失败**，已回退（`a6d1944`）

### 真机结果（决定性）

修复上线后 AUTO **真机跑了 1 次**该跳（`repo_rev 2f616406`）：

```
skill  SELECT_INFANTRY_CAMP   action  TAP_SEMANTIC: TRAINING_CAMP_IN_RING
before.training.camp_tap_norm = [0.441, 0.4551]   ← (317,583) = 环中心
action_backend = ADB                              ← 点击真的发出去了
result  FAILURE / INFANTRY_CAMP_MENU_NOT_PROVEN
after.page HOME, after.training = {}              ← 无菜单，且高亮环消失
```

**after 帧显示：菜单没出现，金色环消失** ⇒ 客户端把这次点击当"点空"。

**⇒ stage A 四次真机点击、四次全败**：三次环外 (346,682)（两次菜单没开、一次跳地图）+ **一次环内 (317,583)**。
**"瞄得更准"不是答案** ⇒ 撤回点击、回到等待；保留 `camp_ring.py` 与 `camp_tap_norm`（环的测量可靠：46/46 帧）。

### ⭐ 我昨晚错在哪（重要）

我把**几何推理**当成了**充分条件**：46 帧证明"落点在环外 103px"，于是改成"点环心"。
**真机一次就否掉了** —— **落点在环内 ≠ 客户端会响应**。

⇒ **新纪律**：改落点之前，先确认**"那个位置本来就有可交互目标"**，而不只是"落点在范围内"。
本次环内其实是 **5 个亮色块**（264/98/60/60/44 px），我点的位置紧邻其中**最小的一个**；
**教学手指指向的是环右侧那个带 `2` 角标的图标（测得 (379,592)）**——
但"5 选 1"**没有客观唯一判据**，所以**没有据此再改坐标**（那会是第 5 次盲试）。

### #82（新，未决）：那个状态到底是什么？

**假设（非结论）**：那不是**圆形**的建筑选中框，而是**椭圆**（rx=102, ry=56），画在**大型多部件建筑**上、
带 `2` 角标、**教学手指指着角标** ⇒ 更像**引导式步骤**。
**若成立** ⇒ `verify_infantry_camp_highlighted` 接受它就是**假到达**，训练路线前半段一直在报**从未获得的成功**。
**可判定的下一步（不需盲点坐标）**：① OCR 环内/附近建筑名，与 `knowledge/game/buildings.json`
（13 条，含 `INFANTRY_CAMP` 但**无位置/外观字段**）比对；② 查有无**教程/引导态**证据（项目**零 tutorial 语义**）；
③ 若确为引导态 ⇒ 记 `DEFER`，停止找点击点。

### 正面进展：用户 P0「消耗剩余体力」**真的在发生**

`AVOID_STAMINA_WASTE` 从昨晚 4 步 → **51 步（最活跃 goal）**，体力 **367 → 265（-102）**：

```
17:10:43 357 → 17:11:56 347 → 17:15:20 327 → 17:18:44 305 → 17:23:29 285 → 17:29:20 265
```

**真实消耗路径 = `DISPATCH_INTEL_BEAST`（情报野兽任务，每次 -10）**，
另有 `EXECUTE_INTEL_RESCUE_SURVIVORS`（-12）。**不是**"世界地图普通打野"。

⚠ **17:29 之后体力停在 265 已 6 小时**：情报板打完 + 世界地图打野仍被 **#72（集结目标混淆）**卡住
⇒ **目标 <30 远未达成**，且"情报耗尽后"的续航路径**仍然缺失**。

本会话总计（17:20Z 后）：**139 步 / 127 成功 / 12 失败（91.4%）**，覆盖 11 个 goal。

## 11:45 会话（操作者 P0）：预留行军导致 AUTO 提前等待 —— 已修 `6ced96e`

### 一、那句话的真实位置

GUI 的「已为体力任务预留1支行军」= `reserved_march_for_stamina`，**只有两处**产生，都在
`winter_agent_v2/brain.py`：`857`（`RESOURCE_DETAIL`）、**`1214`（`MAP`）**。现场帧是 MAP ⇒ 命中 1214。
条件均为 `current_goal in {None,"GATHER_RESOURCE"}` 且 `idle_marches <= reserved_slots(world)`。
`reserved_slots = max(0, min(reserve_marches, capacity-2))`；`config/v2.json` 的
`march_policy.reserve_for_stamina=2`、容量 3 ⇒ 有效预留 **1**。

### 二、预留队列**确实空闲**，而且它就是被预留的那一格

患者帧（`dataset/truth_audit/march_reservation_20260921/key/01_*`）生产链读数：
`marches=[GATHERING,RETURNING]`、`march_used=2`、`march_max=3` ⇒ `idle=1` ⇒ `1<=1` 停。
**预留机制本身是对的**（挡住采集占用最后一格）。

### 三、⭐ 根因：**不是预留挡住体力任务，而是"拒绝"被写成了"结束整轮"**

同帧、生产 reserve=2，`RuleBrain.decide`：

```
goal=None / GATHER_RESOURCE  ->  SAFE_STOP  reserved_march_for_stamina
goal=BEAST_HUNT              ->  SCAN_MAP_FOR_BEAST       ← 体力路线本来就能用这一格
```

⇒ **预留从未挡住 `AVOID_STAMINA_WASTE`**（`BEAST_HUNT` 只在 `idle_marches<=0` 时拒绝）。
真凶是 `runtime.py` 对 `SAFE_STOP` 的处理：记 `DEGRADED` + `return finish(reason)` ⇒
**一轮只做 1 件事就结束**，且没有动作把客户端挪出地图 ⇒ **下一轮开局还在同一屏**。
那一轮实际只做了 1 个动作（`EXECUTE_INTEL_RESCUE_SURVIVORS`，187→175）。

**为什么跑的是采集目标**：`KEEP_MARCHES_PRODUCTIVE` **不在 `runtime.py:880` 的 8 条 goal→route 映射里**
⇒ `current_goal=None` ⇒ 两个预留分支的条件正好包含 `None` ⇒ 采集分支先回答。
同时 `AVOID_STAMINA_WASTE` 被开发作业 `2d5c3dd5` 挡着；它是该帧**优先级最高（825）**的可调度目标，
门禁 03:26:03Z 一放行就立刻被选中并开始跑。

### 四、修法（不是改状态名）

`SAFE_STOP` 分支内**只针对该理由**改用项目已有的 `_yield_to_next_goal`（与"技能不可执行"同一条
Rule A 路径）⇒ 循环 `continue`、重新取帧、重选目标。**范围精确限定**（守卫会因"扩到所有 SAFE_STOP"变红）；
保留 `index < max_actions` 与"同一 goal 一轮只让位一次"两道边界。**预留策略一条没删。**

**A/B**：`tests/test_march_reservation_handover.py` 本树 **7 passed** / 未修 HEAD **3 failed + 4 passed**
（红的三条正是缺陷；另 4 条两侧都过 ⇒ 钉不变行为）。`check_wiring` +5，`problems: 0`。

### 五、本轮仍未做（不许当完成）

- **真机未复跑**：60 步最近记录仍全是 `8a8429c7`（修复前）⇒ 等下一轮。
- **#83 `KEEP_MARCHES_PRODUCTIVE` 缺路由未补**：**不是无副作用的** —— `current_goal is not None`
  在 `brain.py:274/534/1055` 会新激活三条分支（终端页让位、`goal_page_mismatch`、联盟页让位）。
  且**补它也不会改变本次停机**（两个预留分支对 `None` 与 `"GATHER_RESOURCE"` 行为相同）。
- **#84 角色身份无生产重读**：`role_identity.json` 最后写于 **2026-09-16T10:44:08Z**（112.8h 前），
  `verification=VISION_READ_REPLAY`（**回放**）。生产只当**标记**（`[role] 1171757165 (PERSISTED)`）
  打在 episode 上，**不参与调度**、不会把队列/体力写进别的角色 ⇒ **展示层过期**，不是队列问题的伪装；
  但 AUTO 从不重读它是真缺口（`record_role` 只被操作者工具调用）。
- **#33 陈旧 index.lock 第 3 次命中**（0 字节、mtime 早 3h30m、无 git 进程 ⇒ 按规程删）。
  建议尽快在同步路径加"陈旧锁检测"，别靠人记得。
- **体力仍未达标**：03:17 之后体力在**回升**（175→177→178→179），`AVOID_STAMINA_WASTE` 反复
  `SCAN_MAP_FOR_BEAST` 扫不到目标（该帧那只兽**只有 `22` 徽标没有名字** ⇒ 无身份）。
  **P0「体力降到 30 以下」远未达成**，且续航路径仍被 #72 卡着。

## 12:20 会话（操作者 P0 第二次）：AUTO 停摆根因 + 训练页改由 OCR 识别（`a9bc5ea`）

### 一、AUTO 为什么"提前等待"到人工重启：**等待没有上限**

`tools/control_panel.py` 用 `Popen.communicate()` 等 worker，**16 处全部没有超时**
（主轮 2 + 子任务 fallback 13 + 真机校准 1）。`_kill_worker_tree` 自己的注释写明机制：
**在 Windows 上面板持有的是 venv 转发 stub，真正持管道的是孙进程** ⇒ 孙进程不退出 ⇒
**EOF 永不到来 ⇒ 面板永久阻塞**。

**真机证据**：`11:43:24` 那轮 worker **跑完循环**（`runtime_snapshot` 03:45:04 写入、
两个 loop 标志 false）却**没退出**；之后 `panel.log` **13 分钟无输出**、最后 episode `03:44:56`、
`operator_intent=RUNNING`，而 `pump.json` 每 30 秒照跳 —— **这就是窗口看着像活着的原因**。

**已修**：`await_worker()`（有界等待 → 到点 `winproc.kill_tree` → 诚实返回 `WORKER_ABANDONED_CODE=130`
并把理由写进 `latest.log`），16 处全改。**不是缩短重开间隔**（间隔一字未改）。
**顺带确认**：`winter_agent_v2/runtime.py` **不在** `CONTROL_PLANE_PATHS` ⇒ 本次改动不要求窗口自替换。

### 二、预留行军：**既没挡体力 Goal，也没挡训练**（用户把两件事并成一件，实测分开）

- 同帧同生产 reserve 下 `goal=BEAST_HUNT` → `SCAN_MAP_FOR_BEAST`（体力路线只在 `idle_marches<=0` 拒绝）；
- **训练分支完全不读** `idle_marches`/`reserved_slots`/`reserve_marches`（grep 为空）。

### 三、三个兵营的真实状态（8 张 `page=TRAINING` 真机帧 = 全部）

| 兵营 | 状态 | 证据 |
|---|---|---|
| 盾兵营 INFANTRY | **训练中** | 8/8 帧全是它；倒计时 01:09:31/01:00:00/01:00:08/00:59:58/02:59:45/02:59:33/**00:25:15**/00:25:05，批次 216/250 |
| 矛兵营 LANCER | **UNKNOWN** | **零帧** |
| 射手营 MARKSMAN | **UNKNOWN** | **零帧** |

最新真实读数 `2026-09-21T01:53:21Z`：盾兵营 IN_PROGRESS 剩 **00:25:15** 批次 250。

### 四、训练链的真根因：**页面的模板层已死，而那个分支在用旧截图的常量当读数**

`PAGE_TRAINING_INFANTRY/LANCER/MARKSMAN` 与 `TRAINING_QUEUE_TIMER` 在真机帧上**全部不匹配**；
`TAB_TRAINING_LANCER` 与 `_MARKSMAN` **都 d=2**（同图、都从未选中的蓝底格子裁 ⇒ **分不开三个兵营**）；
全部记录是 `CANDIDATE` + `REPLAY_HUMAN_REVIEWED_SCREENSHOT`（旧截图手裁）。
⇒ `world.training` 恒空 ⇒ `TRAIN_TROOPS`（要 `Page.TRAINING`）**永不可能满足**。
⇒ 且该分支写死 `"tier": 10, "batch_count": 806`（**旧截图常量当读数**，生产无消费者）。

**已修**：`HybridVision` 在**模板层 UNKNOWN 且无训练状态**时咨询 `OCRPageClassifier`，
仅当**分类器也说 TRAINING** 时采用（与 alliance/daily 同纪律，闸门换成"必须 UNKNOWN"）。
同一批帧分类器读得完整：`troop_type=INFANTRY, status=IN_PROGRESS, batch_count=250, timer=01:00:08`。
**两个半场**：8/8 训练帧经生产入口正确识别；**14 张非训练帧 0/14 被误判**；
代价只在 `UNKNOWN` 帧（占 1.5%），单次 0.59s。`tier/batch_count` 常量已删 + 守卫。

### 五、仍未做（不许当完成）

- **#86 `world.training` 没有兵营维度**：训练是**一个** goal + **一个** `TRAIN_TROOPS`，
  `_append_queue_goal` 对**单个** `status` 判一次，`IN_PROGRESS` ⇒ **整个目标 COMPLETE**
  ⇒ **盾兵营忙就把训练检查结束，矛/射根本不会被看**（正是用户禁止的）。
  **可行**：三个页签名 OCR 全读得出（1.00/0.99/1.00）⇒ 切页签**不需要重切模板**。需单独测。
- **#87 矛/射零帧** ⇒ 其空闲/条件/消耗全 `UNKNOWN`，需要在客户端各抓一帧。
- **#88 训练目标的进展计量被非训练动作污染**（签名里是 `OPEN_ALLIANCE_GIFTS`），
  但主因仍是自身的 53 次 `WAIT_FOR_CAMP_MENU` 零进展。
- **真机未复跑**：训练页 OCR 与有界等待都**没在真机跑过**，因为 03:44:56 之后 AUTO 没起过新轮
  ⇒ **需要操作者重启面板**。

## 12:40 会话（操作者 P0 第三次）：让位修复 + 体力边界 + GUI 重启诊断（`e568017` / `757c13e`）

### 一、`SAFE_STOP` 不再结束整个工作周期（本次核心）

`SAFE_STOP` 只关于**一个** goal，但 runtime 一律 `DEGRADED` + `return finish(reason)`
⇒ **一个任务正确拒绝，结束了本轮其他所有任务**。真机两次实测同形：
`03:17:51Z reserved_march_for_stamina`（该轮只做 1 个动作；`used=2/3` ⇒ 恰好 1 格空闲正是预留那格，
**预留在正常工作**；同帧 `goal=BEAST_HUNT` 给 `SCAN_MAP_FOR_BEAST` ⇒ **预留从未挡住体力目标，是停挡住了全部**）
与 `03:44:56Z alliance_state_unknown`（联盟页上明明有 `联盟科技` 25 角标，且没有任何动作把客户端挪走）。

**修法**：问题从"是哪个理由"改成"**这条理由该不该结束整轮**"，用项目自己的 `is_fatal_stop`
（runtime 在 DEGRADED / FATAL_STOPPED 之间选的那个）：**只有 `FATAL_`/`ACCOUNT_`/`PAYMENT_` 停**，
其余全走既有的 `_yield_to_next_goal`。**"点名一个理由"的写法正是缺陷的藏身处** ——
`alliance_state_unknown`/`no_idle_march`/`camp_menu_never_drawn`/`training_queue_busy`/
`research_queue_busy`/`verified_beast_target_not_visible` 全走同一行，全都继续停轮。
边界沿用既有两条：剩余迭代 > 0、同一 goal 一轮只让位一次。

### 二、体力边界：30 尚未达标

两处副本都写成包含式 ⇒ 各早停 1 点（而 1 点 = 一次完整野兽出征）：
`goal_library` 的 `> 30`/`<= 30`/`distance=stamina-30` 与 `operations_policy` 的 `<= 30 -> NONE`。
新增 **`goal_library.STAMINA_FLOOR = 30`** 一处定义，状态/完成度/损失/距离全由它派生 ⇒
**在 30 时 READY、完成度 0、距离 1.0**，`choose_stamina_goal` 在 30 时继续消耗、29 才 NONE。

### 三、⭐ GUI 重启：**正式桌面入口可用，但必须操作者双击**（我做不到）

用 `Get-CimInstance Win32_Process` 诊断（本环境 `tasklist` 在 Git Bash 里返回 0 行，**不可用**）：

- 卡住的面板 = PID **11152**，跑在 **`C:\Users\xhw\.cache\codex-runtimes\...\pythonw.exe`**，
  **不是项目 venv** —— 正是 `Start-Winter-Agent-V2.cmd` 注释里记的那个事故
  （"a wrong interpreter reached production unnoticed ... without maa/cv2"）；
- **没有 `run_live.py` worker 进程**；设备租约是 2026-09-19 的旧租约且已 `released_at`；
- 另有我此前会话的**卡住 pytest**（PID 24624，卡约 12 小时）—— 已清理。

按正式方式重启：`preflight` **PASS**（venv 解释器 + MuMu `127.0.0.1:7555` 在线、`com.gof.china` 前台），
`taskkill /T /F /PID 11152` 后用 **venv pythonw** 启动 ⇒ 新面板写了
`12:33:10 运行环境预检通过：...\.venv\Scripts\python.exe` 与 `12:33:10 自动运行已启动`。

**但它 2 分钟后被回收**（进程列表无此 PID、`panel.log` 停在该行且**无退出记录**）——
正是启动脚本注释预警的 *"a GUI started by a development tool cannot be kept alive -- that host
reaps its children when its call ends"*。⇒ **必须由操作者双击 `Start-Winter-Agent-V2.cmd`**；
`Start-Process` / `cmd /c start` 都在这会话的作业对象里，救不了。

⚠ 我那次启动**带了 `WINTER_AGENT_LAUNCH_PATH=desktop`** —— 那正是"从桌面启动"的声明，
**我这次是伪造的**（它是被开发工具启动的）。面板若活着会把它当真实桌面 soak 计入。已在 issue 写明。

### 四、本轮仍未做（不许当完成）

- **#89**：让位需要"有 goal 可让"。`best_goal is None`（探测态）+ 停在无人认领页面时仍会停；
  `_deferral_replan` 只在 **MAP** 页生效（`OPEN_HOME.required_page` 就是 MAP）⇒ 在 ALLIANCE/DAILY/MAIL
  上"没活干"只能停。改它要动 `test_multitask_scheduler.py` 钉住的"无 goal 时不做 Back"，需单独判断。
- **#90**：`world.training` 仍**无兵营维度**（= #86），三兵营分别检查要先有那一维。
- **#91**：野兽搜索三模板 `BTN_SEARCH_BEAST_TAB`/`SEARCH_BEAST_LEVEL_5_SELECTED`/
  `BTN_SUBMIT_BEAST_SEARCH` 已切好但**零技能引用**（`beast_search_v2_manifest.json`），
  而资源搜索链**已 VERIFIED** ⇒ 这是操作者点名的"搜索→野兽页签→按等级搜"的最小接入面。
- **真机验证全部欠着**：让位与体力边界**都没有在设备上跑过**，因为面板需要先由操作者重启。

### 五、一条测量纪律（本次又用到）

`test_live_runtime.py::test_verified_action_repeats_then_safe_stops` 我先判定"是我的改动打破的"，
用对照工作树测出**两棵树当时都失败** ⇒ 它读的 `knowledge/**` 在两次运行之间被生产改写。
⇒ **在本项目里，"某测试红了"必须先确认它读的是不是易变数据**；这条已由 #63 记录过，这次是第二次撞上。
新测试（`tests/test_refusal_yields_the_cycle.py`）因此**只桩掉 `_selectable`**（那一处易变来源），
真实驱动 runtime 与 brain ⇒ A/B 干净：本树 6 过、未改 HEAD **2 红**。

---

## 15:30 三兵营独立化 + 校准路由修复（提交 `6a4844f`）

### 一、最重要的一个发现：OCR 训练页分支是死代码

上一轮我"修好了"训练页识别，**实际上生产从未生效**：

```python
if primary.known:                       # 1341 行
    ...
    if primary.page is Page.UNKNOWN:    # 嵌在里面
```

`UNKNOWN` 就是 `not known`，**两个条件互斥** ⇒ 分支永不执行。单测却全绿，因为
`test_training_page_by_ocr` 直接调 `OCRPageClassifier.classify`，**比生产入口低一层**。
⇒ 把 UNKNOWN 处理**移到 `observe()` 顶部**（`if primary.known:` 之前）。
⇒ 新测试不再读源码，改为**断言行为**：经 `HybridVision.observe` 读真实帧必须得 `TRAINING`
且 `camps` 非空，从根上钉住"回退分支可达"。

**教训（本轮第二次撞上同类）**：**单测过 ≠ 生产路径过**。凡是"某个 fallback / 某个分支"，
测试必须打**生产入口**（这里是 `HybridVision.observe`），不能打内层类。

### 二、三兵营独立（#86 / #90 的正面回答）

- 新建 `camp_training.py`：`SHIELD_CAMP` / `LANCER_CAMP` / `MARKSMAN_CAMP` 三个**独立读数**，
  各有 page / queue / timer / recovery；`busy` 三态 `True/False/None`——**`None`（没读）与
  `False`（读了、空闲）严格区分**。
- **兵营身份以页面标题为准**（英勇盾兵/刚毅矛兵/刚毅射手），**不是页签像素**：
  像素判别实测不可靠（**7/14 非训练帧同样触发**；训练帧上一个未选中页签 4 次采样只 1 次判蓝），
  而且它**把盾兵营帧判成了 MARKSMAN_CAMP**（取最右）。已废弃，理由写进代码注释。
- **真机复验（经生产入口）**：`page=TRAINING, troop=INFANTRY, timer=00:25:15`，
  `camps={"SHIELD_CAMP": {busy: true, source: "PAGE_IS_THIS_CAMP"}}` ⇒ **盾兵营归位正确**。
- `discover()` 产出三张独立票据：盾兵营 COMPLETE（**只关自己**），
  矛/射手营 **DISCOVERED + `TRAIN_TROOPS` 可用**（= 有活干，不是"完成"）。
  ⇒ 一个兵营在练**不再**宣布整个训练 Goal 完成。

### 三、校准命令的 EXIT_2（我重启面板时撞出来的真 bug）

面板日志一直刷 `真机校准：验证运行结束（EXIT_2），停止原因 未知`。找到真因：

```
run_live.py: error: argument --goal: invalid choice: 'KEEP_TRAINING_PRODUCTIVE'
```

**台账存的是 goal id，`run_live --goal` 只收 route 域**。所以每次校准**在 argparse 就死了**
（`validation.log` 是 usage），而**租约已经拿到**，于是每 30 秒抢一次设备、每次 1 秒失败——
**这就是 AUTO 反复 `device leased for development` 的原因**。

修法：路由表**收敛到 `goal_library.GOAL_ROUTES` 一处**，scheduler 与面板同读；
`route_for()` **幂等**（台账里已有 `BEAST_HUNT` 这类域值，必须原样通过）；
无路由的 goal **抛错**而不是硬塞。`ROUTE_DOMAINS` 供 `run_live` 的 argparse 与映射表共用。

### 四、GUI 被回收：这轮我自己又踩了一次，但这次拿到了确证

- `tools/_launch_panel_detached.py`（新建）：设 `desktop` 标记 + 记录 PID/解释器/revision 后分离启动。
  **但面板仍在约 3 秒后消失**（15:21:44 后 `panel.log` 再无写入）。
- `tools/_trace_panel_startup.py`（新建）：**同一解释器、同一入口，但挂在当前调用下**跑 60 秒，
  stderr 重定向。结果：**"still alive at the end of the observation window"**，
  且 `startup_trace.log` **0 字节**——**启动完全干净、零报错，日志正常写到 `自动运行已启动`**。
  ⇒ **死因就是"调用结束宿主回收子进程"**，不是产品启动失败。
  ⇒ 这**确证**了 `MEMORY.md` 里那条纪律，并给出了可复现的判别法：
   **"挂在调用下能活、分离后立刻死" = 回收，不是 bug。**
- ⇒ **生产 soak 只能由操作者双击 `Start-Winter-Agent-V2.cmd` 完成**。我不该、也没有
  把本轮任何一次"我启动的面板"算作验收证据。

### 五、其它修复

- `_device_lost` 6 处传了 `device.screenshot()` 不收的第三个参数 ⇒ HEAD 下
  `test_capability_gate` **10 个测试 TypeError 报错**。已修。
- `GoalLibrary.best()` 分层：sweep 票据（DISCOVERED, 180）**不再压过真实工作**（READY）。
- `test_capability_gate` 改为 hermetic（monkeypatch `observation_store.STATE_PATH`）；
  `test_the_stamina_goal_meters_the_work_still_left` 陈旧断言按 `e568017` 的 `< 30` 边界更正。

### 六、验证与未做

**已验证（离线）**：`test_three_camps_are_three_readings` **22 passed**（含经生产入口读真实帧）、
`test_validation_command_wiring` **8 passed**、`test_capability_gate` 绿、
`test_training_page_by_ocr` 绿。

**未做（不许当完成）**：
- **三兵营真机证据只有盾兵营**：语料里 8 帧训练页**全是盾兵营**，矛/射手营**零帧真机观测**。
  要拿到那两营的证据，必须**真机打开它们的页签**——这一步只能由操作者在活着的面板上做。
- **验收 A–F 的真机部分全部欠着**（面板无法由我维持存活）。
- **普通野外打野搜索链**（`BTN_SEARCH_BEAST_TAB` 等三模板）仍未接入（#91）。
