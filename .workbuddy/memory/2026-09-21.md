# 2026-09-21

## 00:20 会话：导航与快捷入口全面发现（操作者一次性许可）

**没建第二套导航**（操作者明确禁止）。做法是**先派生、再在真机帧上量缺口**。
新增 `tools/ui_navigation_matrix.py` → `knowledge/ui/navigation_matrix.json`：把 96 技能 /
80 绑定 / 40 导航类入口 / 230 个有模板语义 / **171 个有模板却没任何技能点名**，
以及操作者点名的 12 类功能各有几条可走路径，全部**投影**出来（不手写）。

**逐条覆盖（可复跑）**：情报/灯塔 18 条路；邮件 6；城镇/野外快捷面板 4；地图搜索栏 4；活动 4；
兵营 3；科技 2；回城 2；体力HUD 2；采集点 1；**联盟捐献 0**；**巨兽/自动加入 0（连模板都没有）**。

### 三处更正（都有帧，别引用旧说法）

1. **"城镇快捷面板"不是路线用的那个**。`OPEN_POWER_OVERVIEW → BTN_OPEN_POWER_DETAILS → 提升`
   打开的是 **`加成总览`**（部队/建筑/科技/领主装备/英雄实力 + 战绩，各带数值）再进
   **`实力详情`**（五行进度条 + `提升`）——**这是属性统计链**。操作者描述的"带进度/倒计时/可用次数、
   带城镇-野外标签的快捷功能面板"**项目里零记录**，需要真机发现。
   帧：`runtime_auto/20260921_001508_805754/…_step_003_before`（加成总览）、`…_step_004_before`（实力详情）。
2. **训练路线不是被教学手指挡住的——那个理由是错的**。代码注释与 `verify_camp_menu_reobserved`
   都写"手指压着兵营、点了会跳地图"，依据只有 **2026-09-17 一次尝试**。**证伪**：
   `runtime_auto/20260920_093256_036374/…_step_005_before` 上**手指在、菜单也正常开着**
   （`16 盾兵营` + 径向菜单 `详情/立即完成/加速/训练`，手指此时指向 `训练`）⇒ **手指是引导**。
   真正差别是状态：stage A ＝金环亮无菜单（`TARGET_INFANTRY_CAMP_HIGHLIGHTED` d=2），
   菜单态 ＝`BUILDING_INFANTRY_CAMP`（d=8），**两者落点都在 (340,690)**。
   ⇒ 唯一未决问题：**stage A 该点哪里**（环中心试过→跳地图；手指尖下的兵营图标在 ≈(350,545)，
   **高 137 px，从未试过**）。**不猜**，本轮不改落点。
   **代价已量**：`KEEP_TRAINING_PRODUCTIVE` 127 步里 **45 步（35%）在 `WAIT_FOR_CAMP_MENU`**，
   真正到训练页只有 4 次；`MAX_CAMP_MENU_WAITS=2` ⇒ 每轮浪费 2 步 + 一整轮，已持续 3.25 天。
3. **快捷面板内容完全没读进 WorldState**：`POWER_DETAILS` 帧上 `observe` 返回
   `training={}` / `research={}` ⇒ 操作者 §四 要的"先从面板读可靠状态"**今天做不到**。

### 正面证据

- **同一个面板被两个目标复用**：`16:05:11 NAVIGATE_INFANTRY_CAMP`（训练用 `部队实力→提升`）与
  `16:15:55 NAVIGATE_RESEARCH_LAB`（科研用 `科技实力→提升`）是**同一条三跳链**，两条都 SUCCESS。
- 底部导航六个入口（`探险/英雄/背包/商店/联盟/野外`）真机可见；右侧八个活动入口 + 邮件/设置可见；
  左侧有 `通关探险第80关 (0/1)` 任务卡。**但这些里项目只覆盖 `OPEN_EXPLORATION`/`OPEN_ALLIANCE`/
  `OPEN_MAP`/`OPEN_MAIL`/`OPEN_DAILY`，其余零语义。**

### 守卫

`check_wiring` 新增一组：矩阵必须等于**重新派生**的结果（陈旧即红）、每个技能恰好一行、
**曾经有路的功能力不能掉到 0**（改名必须变红而不是读成"从来没有"）、已知缺失的功能必须仍被报告。
测试 `tests/test_navigation_matrix.py` 8 条。首个 bug 也钉住了：**`RESEARCH` 含 `SEARCH`**，
子串匹配把全部科研目标归进了"地图搜索栏" ⇒ 改用正则 `(?<!RE)SEARCH`。

**本轮未做**：没有减少任何导航跳数（那是导航行为改动，需要真机校准）；没有实现兵营页签切换
（另外两个兵营**零帧**，且 `TAB_TRAINING_LANCER`/`MARKSMAN` 都裁自蓝底格子、**d 都是 2，区分不了彼此**）。
提交 `35c9a24`。
