# 2026-09-24（宿主工作路径）

长期事实源仍为 `E:/无尽冬日智能体/.workbuddy-ai/memory/2026-09-24.md`（本轮完整记录）。

本轮要点（三行）：

1. UNKNOWN 作答 worker 启动即崩的根因 = 网关缺 `CODEBUDDY_FORCE_HEADLESS_BUNDLE`，
   `bin/codebuddy:205` `MODULE_NOT_FOUND`；已在 `gateway_service.build_plan()` 补上（commit `ffb4502`）。
2. 其后暴露本项目自身污染：网关从 agent shell 继承了 `CODEBUDDY_TOOL_CALL_ID` /
   `CODEBUDDY_SAFE_DELETE_*`，武装了批量删除保护 → `POST /jobs` 500；
   已加 `service_environment()` 净化（commit `d6d1a47`）。
3. `unknown_dispatch` 增加失败分类、两套独立重试预算、DEGRADED 健康告警、
   帧缺失不占槽位、记账行不重置重试时钟；冷启动答案仍在等模型，执行链尚无真机证据。
