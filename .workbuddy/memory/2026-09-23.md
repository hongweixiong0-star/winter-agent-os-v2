
## 2026-09-23 01:36–02:05 — 面板第二种控件：绿勾（已完成）

- 实测三张真帧：`已完成`行在 x 0.533–0.590 有 ~41×31 px 绿块（1265/1268/1276 px），全空闲帧没有。
  ⇒ 绿勾与蓝箭头共用同一控件槽。
- 两层接好：`rows[].control`（ARROW/DONE/NONE）+ `done_norm`；`QUICK_PANEL_ROW_<KEY>_DONE` 解析到绿勾；
  4 条词典记录 + 4 个领取技能 + `verify_panel_row_done_collected`。提交 `efc0ed5`。
- **真机负面结果**：按绿勾中心 (404,556) 只把面板关掉，无奖励弹窗；重新读面板时盾兵行**仍是 已完成**。
  ⇒ 验证器旧判据"绿勾消失"在面板消失时也成立（等于用"不再看"换通过），已收紧为要求 after 仍可读；
  ⇒ 领取路径暂时清空（附理由：每轮保证失败 + 十轮无进展会把整个训练 Goal 延后）。提交 `f9d2d3f`。

详见 `.workbuddy-ai/memory/2026-09-23.md`（事实源，含逐像素数据与下一步候选）。

## 2026-09-23 02:05–02:40 — 金环是客户端自己画的（推翻上一段的因果）

- 口径：只取"城市可见 + 面板关闭"的 217 帧（16:08–18:00），逐帧匹配金环：**62 段交替**，
  段长 1–6 帧、静默 5–23 分钟，页面不变时 1–2 秒内即翻转；24 帧落在与训练无关的步骤上。
- ⇒ 上一段"按绿勾把营房高亮出来"**撤回**；绿勾仍然什么也没领到（结论不变）。
- 由此修 `verify_camp_menu_reobserved`：不再要求"环还在"，只看页面；环只进 evidence。
  新增测试 `test_the_ring_pulsing_off_is_not_a_failed_wait`；MAP 等 guard 未动。
- 另撤回两处：金环归属（经 `BUILDING_INFANTRY_CAMP` 模板复核 0.649 最高 ⇒ 仍是盾兵营）；
  "4 次 tap 失败是因为环消失"这句解释不成立（环自己会消失）。
- 未做：`WAIT_FOR_CAMP_MENU` 的 35% 浪费（耦合 3 个测试 + 接线表，需单独一轮）。登记 issue **#92**。

详见 `.workbuddy-ai/memory/2026-09-23.md`（事实源）。

## 2026-09-23 02:20–02:45 — 落地 #92 + 更正上一段的计数

- **更正**：上一段的"217 帧 / 62 段交替 / 24 帧落在无关步骤上"与"最紧的一对 17:51:16/17:51:35"**撤回**——
  那对帧标签与真实状态相反，且 `17:51:16` 实际是**世界地图**帧，却被读成 `HOME` 且环模板命中（假阳性）。
- 结论不变，依据换成亲核的一对：`17:50:32` 无环 → `17:51:00` 同一视野有环+手（28 秒，面板状态未变）。
- **实施 #92**：brain 的 Stage A 不再等（删除 `MAX_CAMP_MENU_WAITS`/计数器），首次看见即同名 blocker；
  3 个测试文件 + `check_wiring.py` 三处断言改为钉"立刻停"；并把 `QUICK_PANEL_ROW_*` 补进派生目标族
  ⇒ 接线检查红项 10 → 9。
- **新开 #93**：读取层在地图帧上 `page=HOME` 与"有环"双假阳性（有帧），不得凭自选阈值收窄。
- 回归：15 个测试文件全绿、无 FAILED。

详见 `.workbuddy-ai/memory/2026-09-23.md`（事实源）。

## 2026-09-23 02:32–03:00 — #93 撤回；帧↔记录配对规则固化

- #93 撤回：那帧只被 `17:51:35 OPEN_HOME` 引用，其 `state_before.page` 就是 `MAP`，环也没命中——
  是我按时间戳取错了文件（三轮同种错误的第三次）。
- 按路径配对的正确计数：119 帧城市可见中 **71 有环 / 48 无环**（比例与原判一致），
  **交替段数 22**（原写 62 是并列造成的假交替）；#92 的结论与依据成立。
- 新增 `tools/frame_owner.py`（查帧的归属与环读数）；长期记忆
  `.workbuddy-ai/memory/MEMORY.md` 写入"帧↔记录必须按路径配对"的铁律。

详见 `.workbuddy-ai/memory/2026-09-23.md`（事实源）。

## 2026-09-23 02:38–04:10 — 操作者指令：红点驱动触发（§一 落地）

- 实测五个入口角标：邮件 9/26（中心 0.960/0.794 稳定）、探险 9/26（恒 197px）、每日 26/26、
  联盟 26/26（计数变化）、英雄 26/26 **几何完全不变 ⇒ 疑似美术，标 SUSPECT_ARTWORK 不读**。
- 落地：`knowledge/ui/entry_badges.json`（唯一事实源）+ `winter_agent_v2/entry_badges.py`
  （三态、表外不读、面板关闭=UNKNOWN）+ `WorldState.red_dots` + `HybridVision.observe` 包装。
- §五 接一处：MAIL 目标在 HOME 读到邮件入口 ABSENT 时不进邮件页（非 fatal）。
- 词典 8 条 `RED_DOT_*`：3 条绑定实测入口，5 条改 `UNBOUND`（读它们就是数任意红像素）。
- 登记 issue **#94**：§二/§三/§四/§六 未接、§七 七项真机验收未做。

详见 `.workbuddy-ai/memory/2026-09-23.md`（事实源）。

## 2026-09-23 02:49–05:05 — 大指令：审计 + 一次真机验收 + 修一个真机复现的缺陷

- 审计两处：**§九 面板共用性不成立**（四条路由已接、未打开时会请求普通控件点把手，真机验过）；
  **§六 AI 通道真实且自动**（gateway up、已自动作答 1 条并转成 MAA 坐标 (635,372)），
  但 episodes 无 advice 字段 ⇒ "答案被执行"缺证据，E 项不能算完成。
- 真机定向 `--goal MAIL`：`red_dots` 落地（每步 12 条）、三态在线；邮件 **PRESENT 290px → OPEN_MAIL →
  MAIL_CLAIM_REWARDS SUCCESS**；验收 D 的跳过未被触发（MAIL_ROUTINE 被既有开发作业占着）。
- **真机复现并修**：18:54:27 已完成行（`control=DONE` 绿勾）被 TRAIN 分支当"进入"目标点了
  → FAILURE/`PANEL_ROW_TASK_BAR_NOT_PROVEN`；已补 `control==ARROW` + `arrow_basis==SCAN` 两个守卫，
  并钉住"只有估算点的行不再被点"。50 项测试通过。
- 登记 issue **#95**、**#96**；审计件 `learning/red_dot_audit_20260923.json`。

详见 `.workbuddy-ai/memory/2026-09-23.md`（事实源）。

## 2026-09-23 05:05–05:40 — 验收 C 成立；修可审计性断点

- **C 真机成立**：`18:55:10` 面板行 `OPEN_TASK_FROM_QUICK_PANEL_RESEARCH` SUCCESS，after 里
  `research.menu_open=true`（行自己的任务条）→ `18:55:28 OPEN_RESEARCH` 进到科研页 → `BACK` 返回。
- **修**：episode 只有 `verifier_ok`、6603 条里仅 29 条带验证证据 ⇒ 加 `Episode.verifier_evidence`
  并在写入处填上（每个验证器本就返回该字段）。
- 既存红项用 `git stash` A/B 判定（改动前后同样失败），不引用静态清单（issue #63 的规矩）。

详见 `.workbuddy-ai/memory/2026-09-23.md`（事实源）。

## 2026-09-23 03:15–03:35 — 营地工具条两种布局（正式链断点，已修）+ 白气泡负面结果

- 发现路径：同一次点击、同一前置状态，16:33 开、16:42 不开；看图证实**面板明明开着**，是读取器没读到。
- 逐词实测：忙布局无 `升级`、多 `立即完成/加速`，`详情/训练` 上移到 y 0.683–0.685（**低于 y 带下沿 0.69**）。
- 两处耦合缺陷同源（只在空闲布局量过）：y 带 `(0.69,0.76)`→`(0.66,0.78)`；门模板（升级图标）→ 增一条
  **详情图标**记录（两布局 d≤2）。`--check` 0 miss / 0 假阳性；`16:42:30` 由 `{}` 变为读出 `menu_open`。
- **没把** `立即完成/加速` 加进可读词表（花钱控件，166 钻）。
- 负面结果：白气泡是**通用建筑提示框**（位置固定、内容随对象变）⇒ 40 帧实测漏 8 / 误报 13，
  装入的是士兵头像或金色雕像 ⇒ 不能当营地判据（与 #92 金环同类）。
- 顺带修：两个清单写入工具 `indent=2` → `indent=1`（否则加一条记录重排 408 条 / 7499 行）。
- 新开 **#97**（#95 补充）：`18:54:27` 那种「金环光晕+气泡」渲染项目零判据，B 项卡在这里。
- 真机：AUTO 连续运行中（走地图线），设备被 GAMEPLAY 持有 ⇒ 本轮**无真机证据**，修复是离线在生产帧上验的。
- 回归：11 个文件 413 项全绿。提交 `7c38955`。

详见 `.workbuddy-ai/memory/2026-09-23.md`（事实源）。

## 2026-09-23 03:33–04:05 — 真机链走通（C/F）；B 收窄；#97 因果撤回；新开 #98

- **真机（03:32-03:35，正式 AUTO）**：`OPEN_TASK_FROM_QUICK_PANEL_SHIELD` → after
  `menu_open=true / camp=SHIELD_CAMP / train_tap_norm=[0.6736,0.7152]`；`OPEN_INFANTRY_TRAINING`
  → `HOME->TRAINING`；`TRAIN_TROOPS` → `AVAILABLE->IN_PROGRESS, timer 03:28:51, batch_count 282`。
  ⇒ **面板行→营地菜单→训练页→开始训练**全链在设备上走通、各自通过验证器（正是 16:42/18:54 死掉那条）。
  注：本次是**空闲布局**（0.7152），所以忙布局修复**未被顺带证明**。
- **B 收窄**：训练页两种状态都无领取控件（进行中=立即完成⊖337/加速；空闲=数量滑条+立即完成⊖2,786+训练⊖03:28:53），
  兵种数量直接显示；122 次读数分 91/18/12。⇒ 兵营 `已完成` 极可能是"批次完毕、部队自动入库"的通知。
- **#97 因果撤回**：气泡下方是**科研所**（模板 0.654 vs 兵营 0.257），且同标记在无关步骤帧上也出现；
  `盾兵` 行后来回到 `空闲中` ⇒ 瞬时通知。
- **#98 新开**：`QUICK_PANEL_ROW_*` 的 `arrow_norm.x` 被扫描带（右沿 0.80=576px）外的城市蓝色拉偏；
  按钮实为 x 388-425。账本对照：用 arrow_norm 的 tap 三处失败，落在按钮内（404）与行标签（225）的成功。
  两条修法都试过但**均无依据**（宽度在各批帧上不一致 41px vs 76-105px）⇒ **不改取值规则**，先做不依赖色带的按钮定位器。

详见 `.workbuddy-ai/memory/2026-09-23.md`（事实源）。

## 2026-09-23 04:05–04:35 — 修 #98：面板行按钮改由自身的白箭头定位

- 定位器实测：10 帧 × 4 行 = 40 次，**37 次命中且每次都是 x 396-416（宽 20px，中心 0.5639，spread 0.0000）**；
  未命中 3 次恰是 已完成 行（画绿勾）⇒ "没有雪佛龙 = 客户端没画进入箭头"。
- 成因：扫描带右沿 x576 伸进面板外城市 155px + `min..max` + "取最宽一行" ⇒ 优先选污染行；
  按钮本体青色在 x 384-425，读取器却报 0.5979-0.6667；三次用它的 tap 全失败、落在按钮内的成功。
- 修法：改用按钮内白箭头定位 + 蓝色只在该邻域读；徽标窗口改到按钮**右三分之一**
  （红点实测 412-420 是右端，旧注释"左端"来自被污染的框）；删掉旧常量与死变量。
- 验证：8 帧复读 `arrow_x` 全回 0.5618、徽标状态 0 变化；已完成行自动落到估算回退（进入路径拒绝）。
- 新测试 3 项 + 7 子测试；证据帧入库 `dataset/truth_audit/quick_panel_row_20260923/`（需 -f）。

详见 `.workbuddy-ai/memory/2026-09-23.md`（事实源）。

## 2026-09-23 04:00–04:30 — 真机验证 #98/#97 + 点已完成行体的真实后果

- AUTO 轮间窗口（04:02-04:08）跑了两次定向验证，车辆 `tools/live_panel_arrow_check.py`
  （复用探针的 build/租约，**坐标全部取自当帧**）。
- **#98 真机成立**：把手定位 (0.0181,0.4301)→开面板；四行 `arrow_norm.x` 全 **0.5618**、`w_norm` 全 0.0569；
  点 (404,546) → 营地面板打开（`menu_open:true, camp:SHIELD_CAMP`）。修复前用 `arrow_norm` 的 tap 全失败。
- **#97 忙布局真机成立**：`train_tap_norm=[0.7312,0.6844]` = 离线量出的忙布局高度。
- **点「已完成」行体**（仓库补给 (225,822)）= 面板关闭 + 客户端画出"手+白气泡+金环"，**不是领取**；
  与兵营同形态。受控对比同时**更正 #92 的解释**（标记是客户端为指示未处理项而画、并持续存在），
  结论（不能用它证明到达）不变。
- 气泡定位器**判定不成立**（五帧：0 / 0 / 270px=把手 / 891px 碎片 / 733px），故 `--measure-callout`
  只测量不点。新开 issue **#99**：面板行的 `label` 可以取自面板之外的文字（`梦境寻忆` @ x 0.769）。
- 未做：气泡点击、联盟捐献/英雄招募两条真机可执行行。

详见 `.workbuddy-ai/memory/2026-09-23.md`（事实源）。

## 2026-09-23 04:12–04:30 — #99 修好（面板行只能由面板自己画的 token 组装）

- **机制**：`rows_between` 只按 y 取 token、不判位置；城市活动栏的 `梦境寻忆`（y 0.2977, **x 0.769**）
  夹在 `X科技研究` 表头（0.2898）与真行（0.3211）之间，成了该节第一行 ⇒ `key=RESEARCH label=梦境寻忆
  status=IN_PROGRESS control=NONE`，真正的空闲科研行没被报出。
- **修法**：`drawn_by_the_panel(token)`（左缘 ≤ 既有常量 `QUICK_PANEL_COLUMN_MAX_X_NORM` 0.55），
  用于 `rows_between` / `state_below` / 面板级兵营行三处。
- **验证**：改后该帧读 `科技研究/空闲中/ARROW/PRESENT @0.3211`，与真机 20:03 的 0.3207 一致；
  端到端：旧读数 → `OPEN_POWER_OVERVIEW`，真实读数 → `OPEN_TASK_FROM_QUICK_PANEL_RESEARCH`；
  新测试 2 项；11 文件回归全绿。
- **新开 #100**：`可捐献25/25` / `免费招募` 这类"以自身词表状态"的行被通用选择器的 `status==IDLE` 拒掉
  （实测 `goal ALLIANCE -> OPEN_ALLIANCE`、`goal HERO_RECRUIT -> OPEN_MAP`），且英雄招募没有路由域、
  `DAILY_HERO_RECRUIT` 无 live-loop 验证器。本轮只登记。
- 未做：#99 的真机验证（AUTO 04:19:41 那一轮已在修复后的代码上跑，未走到科研行）。

详见 `.workbuddy-ai/memory/2026-09-23.md`（事实源）。

## 2026-09-23 04:25–04:40 — 气泡语料推翻自己的阈值；搜索面板压住城镇门（#101）

- **语料 1413 帧 / 81 种形状**：上一轮打算用的"约 400px 尺寸间隙"**被推翻**（非气泡最高 1069px，
  被手遮住的气泡只有 395-520px）。成立的判据是**形状**：气泡**高 58-76px**，其余白块都矮（字形 20-23、
  把手 29、名称条 21-23）⇒ 取 **≥55px 高**；抽查两侧 60 帧：真阳性 30/40、**假阳性 0/20**。
  气泡身份＝白色圆角方块+小尾巴，里面是**被高亮建筑的图标**（显微镜/士兵/物资箱），是"去这里看"的指示。
  已实现 `CALLOUT_MIN_HEIGHT_PX` + `--tap-callout`；**点它会发生什么仍未测**。
- **新缺陷 #101**：`OPEN_HOME` 323 步里失败 **44 次（13.6%）**，全部 `page_before=MAP`，其中 37 次
  `SEMANTIC_TARGET_NOT_VERIFIED`；成串发生，最近一次 20:26:44（当晚运行轮内）。受控对比：失败帧＝
  资源搜索面板在场(d=0)、门 absent；成功帧＝无面板、门 present(d=2) ⇒ 面板盖住右下角的门，
  而页面仍读作 MAP。**客户端如何关该面板未测出**（该帧上放大镜不在场）。
- 我的探针在同一视图按设计中止（不猜）——同一处空白。

详见 `.workbuddy-ai/memory/2026-09-23.md`（事实源）。

- **#101 补测**：两侧各 30 帧——成功侧 面板 0/30、门 30/30；失败侧 面板在场且门缺席 22/30，
  门在场但仍失败 8/30（① 4 次 `SEMANTIC_TARGET_NOT_VERIFIED` 而模板 d=2，属解析器层不一致；
  ② 4 次 `OPEN_HOME_NOT_PROVEN`，3 次 after=POPUP）。本轮不改代码。

- **#101 主机制已修**：七个 `OPEN_HOME` 发出点里只有 3 处先关搜索面板，补齐另 4 处
  （EXPLORATION/DAILY/ALLIANCE/兜底 HOME），测试扩到全部 7 个目标；A/B 确认剩余 2 个失败是既存红项。
  解析器层不一致与 `OPEN_HOME_NOT_PROVEN` 两种成因未修；真机验收未做。

## 红点 → 优先级信号（离线受控 A/B，AUTO 正在真机跑，未抢设备）

- 新增 `entry_badges.dot_varies` / `dots_pointing_at`（只认 PRESENT × 被量到会消失的角标 × 绑定到 Goal）+
  `goal_utility.RED_DOT_BONUS = 60`（界算出来：未读探访 180 + 60 = 240 < 一次可领取 250）。
- 修掉一个绑定缺陷：面板「科技研究」行的红点原本绑到路由域 `"RESEARCH"` 而不是 Goal id
  `KEEP_RESEARCH_PRODUCTIVE`，而这一行是面板红点信号的主体（12 帧 9 PRESENT、唯一有区分度）。
- 证据：`tools/replay_red_dot_priority.py` 12 张生产帧，同帧同代码只切 `red_dots` ⇒ 处理组 8/8 换板首，
  负向对照 0/4；归档 `dataset/truth_audit/red_dot_priority_20260923/`。
- 刻意不做：§二①「红点唤醒 Goal」——量出该盲窗在语料里一次都没出现（唯一一次在 1841 分钟后）。
- 测量工具：`tools/measure_mail_dot_blind_window.py`、`tools/measure_panel_row_badges.py`
  （都统计并报告跳过的半行：`episodes.jsonl` 是被并发追加的）。
- 登记 issue #102，并把 #96 的状态改为"§一/§二② 已做"。

## 假失败理由（issue #103）：TRY_ORDINARY_CONTROL 32 次失败里 21 次理由为假

- 近 12h 失败榜第一是 `SEMANTIC_TARGET_NOT_VERIFIED`（54），其中该技能 29 次；全量 69 次尝试 38S/32F。
- 定性：失败帧上**画着把手**（`quick_panel.handle` COLLAPSED、面板关闭、无白名单词）；用生产解析器在同帧
  重跑，只切"本 run 是否已用过该控件"一个变量：空→有落点，有→None。第 1 次尝试 50 次里 13 失败，
  第 2 次及以后 19 次里 18 失败。
- 根因：声明控件层四个闸门都只 `return None`，`executor.py:78` 压成"帧上没有这个控件"；连带把假候选
  按 unlocated 入库、并被分类成 `UNKNOWN_UI`（会让开发 agent 去修"读不懂"的问题，而没有东西读不懂）。
- 修法只改"怎么记"（`_ordinary_declined` 具名 + `_failure_type_from()`），**点击行为零变化**；
  假理由 21 → 0，剩 11 条全部 `handle=False`（是真的）。
- 新工具：`tools/probe_ordinary_control_failures.py`、`tools/probe_ordinary_control_resolution.py`；
  证据 `dataset/truth_audit/ordinary_control_failures_20260923/`；测试 `tests/test_ordinary_control_decline.py`。

## 守卫有八个发出点，只护了七个（issue #101 第二轮）

- `OPEN_HOME` 守卫落地 40 分钟后同类失败又成串 8 次（21:22–21:27Z），**七次是一步长的运行** ⇒ 六分钟死循环，
  直到一次无关 SCAN_MAP_FOR_BEAST 平移地图才关掉面板。
- 根因：第八个发出点在**运行时**——`LiveRuntime._deferral_replan`（runtime.py:651），它只在"地图上无事可做"
  时触发，而那正是上一轮留下面板的场景。已加同形状三行（BACK + `close_resource_search_before_the_deferral_hop`）。
- 教训：**加守卫要按 skill id grep 全包**，决策不止大脑一个发出者。
- 顺带补证据缺口：`Episode.decision_reason`（此前理由只进 worker stdout，随日志滚动消失 ⇒ 爆发串只能靠排除法定性）。
- 工具 `tools/probe_open_home_fallback.py`；证据 `dataset/truth_audit/open_home_fallback_20260923/`。

- 2026-09-23 第二轮（issue #104）：**24% 的运行（18/75，自 16:00Z）是纯空转** `OPEN_MAP`→`OPEN_HOME`→`OPEN_MAP`。
  链条由 `tools/replay_run_decisions.py`（生产视觉 + 生产大脑，逐帧喂进同一个 run 实例）**3/3 精确复现**：
  大脑的无目标兜底（`first_ready_p0_skill`，HOME→MAP）与运行时兜底跳转（`_deferral_replan`，MAP→HOME）
  互为反向、互不知情。`_replan_attempted` 自称防的就是这个环 —— **它只给环的一侧上界**。
- 修法：`LiveRuntime._stop_instead_of_looking_again` + `_barren_pages` + 类常量
  `PAGE_HOPS_THAT_ONLY_LOOK_FOR_GOALS` ⇒ 目的地已是荒页则 `SAFE_STOP` / `every_page_this_run_was_fruitless`。
- 受控 A/B（真实帧，只切守卫）：**36 步 → 27 步**，7 个运行提前诚实停止。
- 真机 Live 证据（上一轮改动）：`ORDINARY_CONTROL_ALREADY_USED_THIS_RUN` 已落在
  `20260923_060723_868796` step 18（`09-22T22:15:28Z`）⇒ #103 命名那一半 Live。注意它记的
  `repo_revision=de55d6a` 是**未提交**的工作树版本 —— 提交号也会和时间对不上。
- 教训：写记忆/文件**不要用 shell 双引号包反引号**（会被命令替换吃掉）；本轮用 Write 写临时文件再二进制追加。

## 红点触发机制的进度与卡点（一次量化核对，2026-09-23）

用 `learning/episodes.jsonl`（6928 步）直接量，不凭记忆：

| 环节 | 实测 |
|---|---|
| 带红点读数的帧 | **337**（红点层 09-22 才落地） |
| 其中至少一个 PRESENT | **98** |
| 能落到某个 Goal（`dots_pointing_at` 非空） | **46** |
| 落点 | `MAIL_ROUTINE` 34、**`RESEARCH` 9（路由域，旧记录里的错绑定）**、`KEEP_RESEARCH_PRODUCTIVE` 7、`SHIELD_CAMP_TRAINING` 2 |
| 98 个 PRESENT 帧里的大多数 | `BTN_OPEN_DAILY` 98 / `TAB_ALLIANCE` 98 —— 两者 `varies=False`（恒亮计数角标），**刻意排除**；这是正确的，但也意味着多数"红点"按设计不可用 |
| 三个面板行 | `ALLIANCE_DONATION`/`HERO_RECRUIT`/`MY_REWARDS` **336/336 UNKNOWN**（#100） |
| 邮件分支（唯一行为消费点） | HOME 上 132 次 `OPEN_MAIL` 里只有 **3** 次带角标读数（都 PRESENT），129 次无读数 |
| `goal_id` 与红点指向"一致"的步骤 | **10 / 6928**，其中 **9 次是假一致**（见下） |

### 卡点（据代码位置）

1. **红点不是触发器，只是排序项。** 全部消费点：`goal_utility.red_dot_term`（+60，作用于**已发现**的目标）与 `brain.py:809` 邮件分支。`goal_library.py` 里 **`red_dots` / `dots_pointing_at` 各出现 0 次** ⇒ 一个红点**无法把一个未被发现的目标带进板子**。directive 的"红点变化接入现有 Goal"目前只做到"影响已发现 Goal 的次序"。
2. **信号可用率低**：98 个 PRESENT 帧里剩 46 能落到 Goal，其中 9 次还是错的绑定（旧记录）。
3. **词汇表撞名（新发现）**：`runtime._step_goal` 在 `best_goal is None` 时返回 `brain.current_goal`，**那是路由域**（`RESEARCH`/`TRAIN`/`MAIL`），于是 episode 的 `goal_id` 字段**有时是路由域、有时是 Goal id**。而红点曾绑到 `"RESEARCH"` 这个**路由域字符串** ⇒ 分析时两者"对上"了，但那不是同一个概念。已登记 #105。
4. **真机 LIVE 证据仍缺**：没有任何生产 episode 能证明"红点改变了选择"——因为(2)+(3)使可排序的红点极少出现，且当时绑定是错的。这是最硬的一条。

### 仍未做（directive 侧）

§二③ 保护"已到最后一步的 Goal"（只有 60 分界间接保证）、④ 未知红点有界探索与恢复条件、
⑤ 已完成待领取的优先级（受 #95 B「领取控件未找到」制约）、⑥ 真机计时验收；
§三 其它入口分别判断（联盟捐献/英雄招募行读不出来）、§四 五态结算词汇；#95 的验收 A/B/C/E/F/G。

### 更正（同日，几分钟后的第二次测量）：上面第 1 条**不是当前瓶颈**

上一条把"发现层不读红点"列为 #1 卡点。再用**裸帧**量了一次 `GoalLibrary.discover(WorldState(page=HOME/MAP))`：
它**本来就产出** 8 个目标 —— `ALLIANCE_ROUTINE` / `CLAIM_EXPLORATION_IDLE` / `CLEAR_INTEL` /
`DAILY_ACTIVITY_TARGET` / `KEEP_MARCHES_PRODUCTIVE` / `KEEP_RESEARCH_PRODUCTIVE` /
`KEEP_TRAINING_PRODUCTIVE` / `MAIL_ROUTINE`。

红点表 12 个入口里，**10 个**指向的目标 discovery 本来就会产出（7 个裸帧就有，3 个来自兵营读数）；
剩下 **2 个**指向 `HERO_RECRUIT`，而它**不是 `GOAL_ROUTES` 里的目标**（#100）。
⇒ 对**现在这张表**，"+60 有没有对象可加"从来不是问题，"红点能不能触发发现"也不是瓶颈；
缺的那条边只有在**将来接一个发现不了的入口**时才成为瓶颈。

**真正的瓶颈按硬度重排**：① 可用率（343 → 98 → **46**，且 98 里大多数是恒亮计数角标、按设计排除；
面板三行 336/336 UNKNOWN）；② **真机 LIVE 证据 0 条**；③ 英雄入口缺的是"目标+路由"而不是"触发"。

**教训**：把一条"设计上缺的边"叫做"瓶颈"之前，先量**它下游有没有别的路径已经覆盖了这件事**——
这次是 `discover()` 的既有产出覆盖了它。缺边 ≠ 瓶颈。

## 11:29 UNKNOWN 通道消费（自动巡检）
- `unknown_ai_worker.py --once`：reconcile checked=1 / abandoned=1（job `40c95545`，问题 `unknown__control__b2e886bd`，45 分钟无落盘）；submitted=0。
- b2e886bd attempts 升到 2/2 → 5 条 pending 问题全部到达 attempts 上限、永久退出候选，通道全面静默。
- 根因仍是背景 job 侧不落盘（连续多个 job ABANDONED），非调度冷却/配额问题，需人工介入。


## §一 审计：AI 建议为什么没被执行（2026-09-23，issue #106/#107）

**对象**：盘上唯一一条真实答案 `unknown__control__b6546e80`（AUTO `08:31:06Z` 在 `UNKNOWN::燃霜矿区`
问，goal=DAILY；答案 `10:12:39Z` 落盘，建议 `ORDINARY_CONTROL[奖励入口]`，锚在 OCR 词 `奖励` 上）。
总量 **6 个提问 / 1 个答案**——另 5 个被 45 分钟 timebox 取消或排队未派发。

**执行侧没有断点**（`tools/probe_advice_replay.py` 把答案放回它自己的帧、用生产视觉+OCR 重跑）：
`take()` 取到=True、staleness=fresh、锚点在本帧 OCR 上、风险无拒绝、
`_advised_control` **返回落点 `[0.8819, 0.2908]`**、`_last_advice` 置位。

**第一个断点＝可达性**：`request_id` 是 `page_key` 的哈希 ⇒ **答案按屏命名**；`take()` 只在
`_advised_control` 里被调用，而那条路径**只在当前帧就是那一屏**时才被走到。实测 燃霜矿区 记录
`attempt_count=0`、`last_seen_at` 就是它第一次被看到的那一秒 —— 此后再没访问过，
`ai_advice` 在 16 条屏记录与 6900+ 条 episode 里**各 0 次**。
且**没有任何东西**把"某屏上有待用答案"变成一次可调度访问（`pending()` 只有 dispatcher/worker/tools 在用）。

**两条顺带发现（未修）**：① id 参与哈希的是 **OCR 读出的标题** ⇒ 同一屏某次标题读成别的字
（盘上同时有 `常规活动` 与 `燃霜矿区` 两条记录）id 就变、答案被孤立 —— **身份认错，后面全错**；
② `Advice` 没有"这条建议是关于哪个通知/入口"的字段。

**新指令三层次审计：代码现状已经成立**。`EntryBadge` 里**没有任何 point/centre/tap 字段**
（`box_norm`/`pixels` 只是读数证据）；`red_dots` 的消费点只有两处：`goal_utility` 的打分项、
`brain.py:809` 的分支（ABSENT 就不进邮件页）——**没有一处把红点当坐标**；入口是具名控件
（`OPEN_TASK_FROM_QUICK_PANEL_<ROW>` 四行俱全，三行兵营走共用 `OPEN_POWER_OVERVIEW → NAVIGATE_*_CAMP → OPEN_*_TRAINING`）。
已钉成不变量：`tests/test_red_dot_is_a_notification_not_a_target.py`（8 项/33 subtests），
其中一条是**"消费点只有排序层"**——出现第三个消费者即失败，强制后来者显式声明。

**教训**：审视一条"AI 没被执行"的链路时，**先把执行侧证否**（逐闸门复算），再去找上游——
否则很容易去修一个不存在的执行 bug。本案执行侧全通，断点在**可达性**。

## 限时活动（新指令 2026-09-23）：能力已存在，断在接线（issue #108）

**先查再动，结论是"不要重写"**：

| 环节 | 现状 |
|---|---|
| 截止时间分档 | `event_goal.DeadlineLevel`：<2h→P0 / <6h→HIGH / <12h→ELEVATED / else NORMAL |
| 活动计划 | `event_goal.EventGoalPlanner.plan()` 产出 `EventPlan(feasible, chosen, expected, status=CLAIM_REQUIRED…)` |
| 提权 | `goal_library.GoalState.remaining_seconds` + **`deadline_pressure()` 已在唯一排序层的 `priority` 第一项** |
| 数据来源 | `discover()` **已经在读** `world.events`（`minimum_guarantee.remaining_seconds/estimated_cost`、`bear.remaining_seconds|seconds_to_start`） |
| 读数 | `ocr.py` 已在读 `minimum_guarantee` 的倒计时（测试断言 12h03m29s） |
| **接线** | **`EventGoalPlanner` 的调用者只有 `tests/test_event_goal.py`；`runtime.py`/`brain.py` 零引用**；控制面板会显示它，执行链不用它 |

⇒ 指令 §三"按剩余时间提权"**那一半已经在跑**；缺的是"哪个活动 Goal 现在该唤醒 / 剩余时间内哪些任务可完成（`feasible`）/ 该领奖就去领奖"这一半。

**与"30 分钟活动"直接相关的两个量化缺口**：① `deadline_level()` 最紧一档是 **<2 小时**，没有分钟级档 —— "剩余 8 分钟"与"剩余 100 分钟"同级；② `EventPlan.feasible` **从未被消费** ⇒ "启动一个明显做不完的流程"目前无人拦。

**修法（不新增 Brain/Scheduler/执行器）**：活动 Goal 由现有 `discover` 产出（已成立）、提权由现有 `deadline_pressure` 承担（已成立），只补"计划 / 可行性 / 领奖"这一段在现有 Goal 上的**消费点**。

**教训（与 §一 同一族）**：这个项目里反复出现"能力写好了、单测绿了、面板显示了，但主链没接"。
排查新需求时先做一次 `grep <类名>` 找**调用者**，比读需求本身更快定位断点。

---

**[自动化 12:30] UNKNOWN 通道消费（unknown_ai_worker --once）**

网关 up；reconcile checked=0 done=0 failed=0 lost=0 abandoned=0；submitted=0（"nothing to do"）。台账尾部无新事件（仍停在 03:29:54 UTC 自己的 ABANDONED）。收尾 pending=5、answered=1、in flight=none——5 条待答问题 attempts 全部 2/2 永久退出候选、无 job，属永久封死型零提交。


## 一个坐标是关于"一屏"的断言：台账的键只有页面大类（issue #109）

**现场**：`09-23T05:23:10Z..05:51:42Z`，**82 次** `CLOSE_POPUP` → `POPUP_CLOSE_NOT_PROVEN`，
`executor_backend.jsonl` 里每次 `tap_point` 都是 `[635,456]`，一次没变。帧上是加成总览，
它自己的 X 在 (666,157)；而 (635,456) 落在面板自己的数字列上，后帧同屏 ⇒ **每 63 秒死一轮**。

**定位过程（值得复用的一串）**：
1. 落点 `[635,456]` → 换算 norm (0.8819,0.3563) → 正好等于某个模板记录的 **roi 中心**
   （`btn_close__step_001_before__1`）⇒ 说明 phash 分支"报告的是**注册位置**而不是找到的位置"。
2. 但用**生产 `find('BTN_CLOSE')`** 今天跑这些帧返回 **None**（四记录 d=28/34/40/34，阈值 8）
   ⇒ 落点**不是模板**给的。**这一步是转折点：不要停在"看起来像某个模板"上，要让真代码回答。**
3. 读 `_resolve_semantic_target` 的链条：模板 → `_client_printed_control` → **`_remembered_control_center`** → 字典提示。
4. 台账里 `POPUP|BTN_CLOSE` 的 `position_norm` 就是那个点 ⇒ **落点是台账给的**。
5. 用生产视觉读四个注册各自的**父帧**：`PURCHASE_POPUP`/`PURCHASE_POPUP`/`EXIT_CONFIRM`/`ALLIANCE`
   ⇒ 那个点属于**退出确认**，被交给了**加成总览**。

**根因**：`control_key(page, semantic)` 里 `POPUP` 是**页面大类**，不是"一屏"。语料实测：`POPUP`
底下 **22 种**叠层、**2717/2717 条读数都带着是哪一种**（面板的读取器早就知道），而键把它丢了 ⇒
**25%（3507/13883）的屏身份被丢弃**（还有 ALLIANCE section 342/233/11、TRAINING 营别 58/52/39、MAP target 53）。
项目里**早就有**正确概念：`state_signature(state)` 的 docstring 写着"更窄会跨屏复用含义不同的控件"——
但它只被 **L1 层**用，老台账没用它。

**教训（可复用）**：**"键"决定一个记忆可以跨到什么范围。** 存坐标时，键必须窄到"一屏"；
键比事实宽一寸，就会出现"用 A 屏的坐标点 B 屏"，而且**任何东西都不会报错**（消费方查得到、就用）。
问自己：这个键所描述的，是不是"这条记忆为真的最小范围"？

**教训二（过程）**：我写的守门函数第一版判据与**写入端**不一致（写入端记 `"HOME"`，我只放行空值），
被自己新写的测试当场抓住；**写完"新机制"要立刻用一个真跑过的例子（这里是一个 page-only 的帧）去撞它**。
另外我一度把"守门"写成主力，实测后改成"**主力是键**，守门只在键与记录的屏走散时生效"——
测试也纠正了这一条。

**修法**：`control_key(page, control, screen="")`（仅当 screen 比页面窄时用它）+ `ControlExperience.screen`
（与 `position_norm` **同一处写入**，两者不会走散）+ `reusable_on_this_screen()` 守门 + 运行时三处调用点同步。
**页面自己就能描述屏幕时键一字节不变**（57 条位置里的 49 条），所以迁移面被压到最小。

**第二半（能力，不只是诚实）**：台账只让失败变诚实，弹窗仍关不掉。X 逐屏会动
（退出确认 0.8819/0.3559、加成总览 0.9236/0.1301、获得体力 0.9236/0.1129、实力详情 0.9208/0.2277…），
而四条记录全是 phash（"核对它还在原位"，不是"去找"）⇒ 加**一条** `ccoeff`+`search_band`
记录，复用**已有已复核**的 X 模板。`tools/register_close_popup_band.py` 每次重算分离度、不达标拒绝写：
**有 X 的 8 帧 d=[0,0,0,0,0,1,1,1]，没有 X 的 10 帧 d=[23,27,27,27,27,29,29,30,32,35]** ⇒ 阈值 8 在空隙正中，
**默认值一个都不用动**。结果：`POWER_OVERVIEW → (0.9236,0.1301)`、`EXIT_CONFIRM → (0.8819,0.3559)`（原位未坏）、
`HOME/MAP → None`。

**A/B 的两个坑（本轮踩到并解决）**：
* `git stash` 做 A/B 在**这棵树不可靠**（第二个写入方持有 index.lock）⇒ 改用**运行时打补丁的 pytest 插件**
  （`learning/_ab_screen_key_plugin.py`，把键退回页面大类 / 把新记录从记录表里摘掉），不动磁盘、不动 git。
* 同一个文件集 **pytest 与 unittest 结果不同**（`test_live_runtime.py` 读 `knowledge/**`，AUTO 在改）
  ⇒ **A/B 必须在同一运行器下做**。
* 结果：基线 **15F/212P** vs 本树 **8F/219P**，**新增失败 0**，基线多出的 7 条正是本轮新写的测试。

**不认领的一条**：失败类型从"有点击"变成"无点击"发生在 `05:54:23`，比本轮第一处编辑落盘（`05:57:44`）
**早 3 分钟**，且台账条目当时仍在、`resolved`，按修复前代码本应仍给出那个点 ⇒ **成因未定，不写进功劳**。
（顺带记住：**AUTO 会运行工作树里未提交的代码**，所以"改动落盘时间"与"真机行为变化时间"必须对齐后再归因。）

**未做**：真机确认弹窗真的关掉（后帧不再是 `POPUP`）；`POPUP|*` 下另外 7 条控件位置没有各自的 band 记录。

## #109 真机确认（同轮内）：记录落盘 31 秒后，加成总览上首次关掉

`06:02:52 / 06:03:28 / 06:04:05 / 06:05:22×4` 全是 `SEMANTIC_TARGET_NOT_VERIFIED`（修法一已在工作树里，
不再有错误点击）→ **`06:05:51`** 注册 band 记录 → **`06:06:22` `CLOSE_POPUP` SUCCESS**。

`tap_point [665,167]`（旧值恒 `[635,456]`）× `after_page=HOME × after_popup=null`。
解析点 `(0.9236,0.1301)×(720,1280)=(665.0,166.5)` = 实际点击 = 帧上目视量到的 X —— **三者同一位置**。
56 次 `CLOSE_POPUP` 成功里 55 次在 `EXIT_CONFIRM`，`POWER_OVERVIEW` 此前 **0 次**（82 次失败）⇒ 死循环断掉。

**顺带一条通用事实**：那一步的 `repo_revision` 是 `5ce5f5ae…+86852481…`，**加号后是工作树脏哈希**
⇒ AUTO 跑的是**尚未提交**的代码。所以"改动何时生效"要按**文件 mtime** 算，不是按提交时间；
反过来也成立：**真机行为变化的时间点，可以与任何提交都不对应**（本轮 05:54 那次切换就是这么来的）。

## 15:33 UNKNOWN 通道消费（自动化第 5 轮全面静默）
- `unknown_ai_worker.py --once`：reconciled checked=0，submitted=0，nothing to do；网关 up。
- pending=5、answered=1、in flight=none；5 条待答问题 attempts 全为 2/2，永久退出候选，无 job。
- 台账尾部仍停在 03:29:54 UTC 的 ABANDONED（b2e886bd/40c95545），外部进程也未再派发。
- 根因仍是背景 job 侧 45 分钟不落盘（连续多个 job 均 ABANDONED），自动化重跑无产出，需人工排查。


## 三个固定方向（2026-09-23 下午）

### 一、同类缺陷的系统排查（不是全量重写）

按"点名控件解析不到"（`TAP_SEMANTIC` + `SEMANTIC_TARGET_NOT_VERIFIED`）**全语料**排了一次（418 次）：

```
BTN_CLOSE 65   BTN_DISMISS_INTEL_REWARD 49   BTN_OPEN_HOME 45   POPUP_GENERIC_REWARD_HEADER 45
ORDINARY_CONTROL 43   RESOURCE_DYNAMIC 38   BTN_OPEN_RESOURCE_SEARCH 33   BTN_OPEN_INTEL_WILD_HUD 14
BTN_OPEN_POWER_DETAILS 14   BTN_OPEN_MAIL 13   BTN_OPEN_ALLIANCE 10 …
```

**修复后**（`06:06:30Z` 起 252 步只 21 次失败）只剩两个目标：`BTN_OPEN_POWER_DETAILS` 8、`ORDINARY_CONTROL` 6。
`BTN_CLOSE` 在 `POWER_OVERVIEW` 上的 23 次**归零**——这是上午那条修复在目标表上的可见效果。
其余大户（`BTN_OPEN_HOME`/`RESOURCE_DYNAMIC`/`BTN_DISMISS_INTEL_REWARD`）都是历次修复已覆盖的场景。

### 二、`BTN_OPEN_POWER_DETAILS`：名字被和图标并成一个 token

它是 `POWER_OVERVIEW` 上的"实力详情"按钮，14 帧全失败。两条 phash 注册在 y 636..685 与 y 762..832，
而按钮画在 **y ~692..765 —— 正好在两条之间的空隙里**（d=36/33 vs 阈值 8）：与 #109 同族，
"注册表达不了一个会动的控件"。

**但真正的答案不是再加一个定位器，而是读客户端自己印的名字。** OCR 把那颗蓝色药丸读成
**`三实力详情`**（conf 0.901，中心 (360,728)，与按钮实际中心 (357.5,727.5) 差 3px）——
≡ 图标被并进了词，所以**精确匹配读不到**。项目里早有这个概念：
`find_printed_words(..., allow_containment=...)` 的参数注释写着"client merges a name with a number"，
但**生产没有调用者**，而它的默认精确是有实测理由的（`城镇` 命中 `我的城镇`）。

改法：把"名字可能被并进更长的 token"做成**按记录声明**的字段 `ocr_merged`（词典 `schema_note` 已登记），
只有被实测到会合并的那一条记录放宽。`_declared_record` 返回三元组 `(pages, words, merged)`，
`_client_printed_control` 把它接到读取器的 `allow_containment` 上。

**实测**：14/14 失败帧现在解析到 (0.5007,0.5684) = 按钮自身中心；**18 屏负向对照 0 命中**
（含 8 种其他弹窗）；覆盖率 **231/245 = 94.3%**（14 帧 OCR 读不到——诚实边界）。
默认精确行为没动：`BTN_OPEN_HOME`/`PAGE_MAP`/`BTN_OPEN_RESOURCE_SEARCH` 仍 `merged=False`，
且一条测试专门钉住"没有 flag 的记录仍不接受包含匹配"。

### 三、弹窗里"同一决定被每个 goal 重新推导一次"

运行 `20260923_140514_506593`：**3 秒内 5 步 CLOSE_POPUP**，五个**不同 goal**
（CLEAR_INTEL→MAIL→DAILY→EXPLORATION→AUTO_DISCOVERY）却得到**同一个决定**——因为
`brain.py` 的 `blocking_popup` 分支在任何 goal 路由**之前**（`POPUP`+有名字的弹窗即命中），
所以让位给下一个 goal 只会拿回同一步。全语料只有这 1 例 `≥4` 连续，但形状是结构性的。

`_failed_controls`（验证器失败不再点）**拦不住**：这里根本没解析出点、没跑验证器。
修法：`_unresolved_controls`（目标→(次数,原因)）+ `MAX_UNRESOLVED_ATTEMPTS = 2`
（**不是 1**：每次尝试都对新帧解析，而本项目最信的字读者单帧只有 ~94% 可读 ⇒ 第二次是真机会）
+ 让位**只给一次**（第二次出手就把同一个非尝试交回来 ⇒ 带真原因结束本轮）。
`_decision_target()` 把两处目标查找收成一处。实测：那条运行从 5 次失败 → **2 次**。

**教训（写进了常量注释）**：我一开始把 `MAX_UNRESOLVED_ATTEMPTS` 写在 `run()` 里（跟
`MAX_ORDINARY_ATTEMPTS` 一样），于是**我的 A/B 插件把类属性改了却完全无效**，
两腿结果相同——差点当成"零回归"的证据。**改了边界就要验证边界真的动了**：
现在它是类常量，测试里同时跑 bound=2 与 bound=10⁹ 两腿（负向对照证明无界时会 >2 次）。

### 四、根目录 `out*.txt` 清理（第 5 项）

`tools/inventory_root_scratch.py`：按"是否被仓库任何文本引用 / 是否 8 小时内 / 是否被 git 忽略"分类，
**移动**而不是删除，并留 `INDEX.json`（每个文件的来源路径、大小、mtime、判词）。

**334 个 → 46 个**（根目录总文件 635 → 347）：归档 **289 个 / 1.98 MB** 到
`learning/archive/root_scratch_20260923/`；保留 11 个**被引用**（`out_failwatch.txt` 被测试插件引用等）
+ 35 个**本会话在用**。归档内容抽查：95 个含 pytest 结果、53 个含失败清单、8 个含 baseline ⇒ 确是命令输出。
`.gitignore` 里 `out_*.txt` 早就有、且注释写明这机制是"没有 coreutils 只能重定向再 Read"、
同类 png 的原则是"**Ignored rather than deleted**"——所以清理口径是**归档**，
并给 `learning/archive/` 加一条忽略规则（否则移动只是在更长的路径上继续出现在 `git status`）。
**没有碰任何项目外文件、日志、截图、知识库或历史失败记录。**

**顺带发现（未动）**：根目录还有 441 项未跟踪，大量是 `.probe_*.py`、`_patch_*.py`、`.fail_list.txt`
这类同样性质的点号临时文件。操作者本轮只点了 `out*.txt`，按"只改被要求/被发现的"不做扩大处理，只登记。

## 2026-09-23（第 4 轮）AI 兜底通道：断点定位与两处修复

**操作者指令本轮**：①沿主链定位并修复 AI 兜底（答案返回→原 Goal 恢复→建议被采用→当前帧定位→MAA 执行→后帧验证），
交出真实 episode；②`OPEN_POWER_DETAILS` 真机验收；**不要**重复审计已解决项。

**第 2 项的现实**：`78bf834`（实力详情 OCR 修复）提交于 **09:58:56Z**，而 `OPEN_POWER_DETAILS` 最后几次失败在
08:02/08:33/09:05Z ⇒ **修复还没被真机走过一次**；`OPEN_POWER_DETAILS` 全史 135 次，全部 `before=POPUP/POWER_OVERVIEW`。
本轮尚未出现新的 `POWER_OVERVIEW`（自 09:58:56Z 起 0 次）⇒ 待验。

**第 1 项：第一断点是"没有任何东西把答案送回被问到的那一屏"。**
三条独立证据（`tools/probe_unknown_channel.py --revisits`）：
- 7 个提问的屏，答案落盘后 41 次 UNKNOWN 步里**被问的那一屏回来 0 次**（逐帧 OCR 标题量的）；
  燃霜矿区答案晚到 **101.5 分钟**。
- 7485 条 episode 里带 `ai_advice` 的 **0** 条（站过 UNKNOWN 屏的步骤 145 条）。
- 通道台账 13 submitted / 15 reconciled，**只有 1 条题有答案**。

**我踩过并改正的判据错误**：先用页记录 `last_seen_at == created_at` 当"从未重访"，**错**——
该字段只在 `stage`/`record_attempt` 时写，一次什么都没解析出来的重访不更新它。改成**逐帧 OCR 标题**才成立。
教训：`last_seen_at` 这类"由部分写入点维护的字段"不能当"事件从未发生"的证据。

**修好的两处（都在既有链上，未新增执行器/大脑/调度器）**：
1. `unknown_dispatch.reconcile`：**先看 job 状态、后看产物** ⇒ job `13ffdac6` 在 `10:12:39Z` 写出答案，
   台账 `10:37:49Z` 记成 `ABANDONED / no result after 45 minutes` **并取消它**。后台会话干完活仍报 `working`
   直到网关回收。修法：**只在非终态分支**先看产物（`state:"ANSWERED"` + 网关 `verdict`）；终态分支一字未改。
2. `unknown_dispatch.candidates`：`MAX_ATTEMPTS_PER_REQUEST` 被当**终身**上限 ⇒ `78694f1b`（`UNKNOWN::对战`）
   两次尝试 09-22T19:41 花光、运行时 09-23T08:30 仍在重问，**却再也提交不了 job**；那一屏 23 小时被站 20 次。
   修法：'自我们上次放弃以来运行时又问过'即再给一次（判据 `request.created_at > record.updated_at`，
   再问节奏由运行时自己的 1 小时冷却限）；另加 `orphans()` 把"只能等人"的题**说出来**。

**验收目标**：`unknown__control__78694f1b` / `UNKNOWN::对战` / goal `INTEL`（与真机 `current_goal` 一致，
决策原文 `unnamed_page_with_goal_INTEL_...`），是唯一"屏会反复回来"的开放题。燃霜矿区那条按操作者要求不强行执行。

**面板是长驻进程**：`unknown_dispatch.py` **不在** `CONTROL_PLANE_PATHS` ⇒ 修复对**面板自己那一遍**要等窗口重启；
本轮用 `tools/unknown_ai_worker.py --once` 走了一次合法手工 pass（台账 `reason: retry 3`）。

**提交** `3195af7`；`tests/test_unknown_ai_channel.py` 29→32；`check_wiring` 9 项逐条相同。
证据 `dataset/truth_audit/ai_fallback_20260923/`；登记 issue #112。

## 2026-09-23（第 5 轮）邮件 / 联盟宝箱改为红点事件驱动

**操作者指令**：红点对这两项不是加分项，而是**准入信号**——无红点就没有这个 Goal；联盟必须区分父入口与宝箱子入口；
UNKNOWN ≠ 进去看看；加最后一道出口保护；量化验收。

**第一实际断点（两条各自不同，且都不是"调 RED_DOT_BONUS"能解决的）**：
1. **Goal 仍被周期生成**（邮件）：用真 `GoalLibrary` 复算——入口 `ABSENT`、`UNKNOWN`、**甚至完全没有红点账本**，
   `discover()` 都产出 `MAIL_ROUTINE / DISCOVERED / available_skills=('OPEN_MAIL',)`，票值只随**年龄**变
   （未读过 180 / 过期 3× 155）。红点此前只经 `goal_utility.red_dot_bonus` 作 **+60 排序分**。
2. **Brain 完全没有闸门**（联盟宝箱）：`brain.py` 在联盟页 `section==HOME` **无条件**开宝箱，理由字符串写
   `alliance_gifts_badge_visible` 却**没读任何角标**。真机语料实证：97 次进入宝箱页，**宝箱瓦片自身角标 93 次 ABSENT**；
   进入前 `alliance.status=UNKNOWN` 96/97。

**改法（三处，均"是否允许生成并执行"而非打分）**：`entry_badges.ENTRY_GATED_GOALS` + `entry_gate()`（PRESENT 许可 /
ABSENT 拒绝 / UNKNOWN 两者都不是）；`goal_library._append_panel_routine` 的周期票受门控（ABSENT/UNKNOWN **不发**目标）；
`brain` 邮件只在 PRESENT 开页（原来 UNKNOWN 与缺账本都开）、宝箱由瓦片角标决定；
`scheduler.ENTRY_GATED_SKILLS` 作**最后出口**（`Scheduler.tick` 是所有决策的唯一通道，拒绝而非纠正）。

**§三 的关键实测**：联盟入口 `TAB_ALLIANCE` 是**常量**（26/26 在位）⇒ 不能决定任何事。联盟页按**瓦片**画角标
（联盟战争 2 / **联盟宝箱 18** / 联盟领地 1 / 联盟商店 有点）。新增 `TILE_ALLIANCE_GIFTS` 量测窗口
（取自已人工复核的 `RED_DOT_ALLIANCE_GIFTS_18` roi 外扩；对照窗口读不同瓦片），
**316 帧 PRESENT 8 / ABSENT 308** ⇒ 会变，可注册；工具**不达标拒绝写入**。

**回归**：同运行器、模块边界 A/B（`AB_ENTRY_GATE=off` 逐站点复现旧语义）= 本树 **100 passed / 0 failed**，
去掉改动 **10 failed**，其中 **7 条正是新写的测试**。`check_wiring` 回到基线 **9** 项。含调度器的 9 文件批 **154 passed**。

**§八 before 数字**（`tools/measure_red_dot_gating.py`，7106 步）：`OPEN_MAIL` 97 步（NO_READING 83 / PRESENT 14）；
`OPEN_ALLIANCE_GIFTS` 97 步（瓦片 PRESENT 4 / **ABSENT 93**）。after 窗口刚开，**如实标注未填**。

**未做**：§五 的 badge_generation 状态库、§七 处理成功后回到入口复读并休眠（当前靠"每帧重新读入口"自然实现，但没有
显式的 last_processed/generation 账本）。
