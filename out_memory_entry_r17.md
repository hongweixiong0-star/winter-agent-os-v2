
## 第 17 轮：把「距离 0 / 距离 26」这个谜解开（0ax）+ 拆掉无人值守的静默死结（0az）

### 1. `SEMANTIC_TARGET_NOT_VERIFIED` 的真根因：客户端把「你付不起」画成红字

项目第一大失败（全时 116）。**近期（≥09-14）只有 12 条**，形状集中在同一个：
`MARCH` 页上的派兵按钮 —— `BTN_BEAST_DISPATCH` ×4（09-15）、`BTN_DISPATCH` ×6（09-14）。

把全部 29 条记录的 `before` 帧逐张量过：

| 组 | n | pHash 距离 | 强红像素 |
|---|---:|---:|---:|
| 成功 | 25 | **0**（25/25 全等） | **0** |
| 失败 | 4 | **26**（4/4 全等） | **452** |

阈值 8。**两组各自内部完全一致** ⇒ 不是抖动、不是临界，是两种确定画面。
失败帧的 `10` 被画成**红色**。这正是 `0av` 那条机制（红 = 付不起）：
红色数字叠在模板的白色数字上，把 ROI 推出阈值 ⇒ 解析器返 `None` ⇒ 执行器报「控件不存在」。

**两次误诊的时间线**（这才是值得记住的部分）：上一轮先判「模板过期（距离 36）」，
后来因一次成功判「实测它是好的」—— **两次都没解释成功/失败为何恰好是 0 与 26 两个离散值**。
只要那个问题被问出来，答案就在屏幕上。

修法**复用** `0av` 已写好的 `unaffordable_cost_pixels()`（29 帧零错分），
**新增零行颜色判别代码**。`ocr.py` 在 `MARCH` 页写 `stamina.cost_affordable`，
`brain.py` 在该值为 `False` 时 `SAFE_STOP dispatch_unaffordable_for_stamina`。

**顺带解释了另一个数字**：09-14 那 6 条 `BTN_DISPATCH` 失败**红像素为 0**，不是同一原因 ——
裁图发现那些帧是 **`小队设置` 页**（ROI 上是绿色「战斗」按钮，距离 32 vs 正常 2），
而 `brain.py` 早有「MARCH + INTEL + 无 beast ⇒ INTEL_HERO_DISPATCH」分支专治它
⇒ **那 6 条是修复前历史，不是未解缺陷**。

### 2. `0az`：BLOCKED 巨兽卡 = 无人值守的静默死结

`Page.BEAST` 上 `available=false` 时 brain 直接 `SAFE_STOP beast_not_actionable` ——
**没人把客户端挪走，所以下一次运行撞同一页、再停**。两次独立复现，
nav 周期三条都是 `steps=1 / elapsed_s=5.4 / dispatches=0`。

修法严格按铁律「先测再改」：新写 `tools/probe_back_from_beast.py`，
在真机那张卡上按**一次** BACK —— 实测落到 `Page.MAP`（体力恢复可读 110）。
据此加 `BACK` 出路 + 一次性守卫 `beast_card_not_actionable_left`（防卡↔图乒乓）。
`verify_safe_back` 正好接受该转移，单测用真 verifier + 负向对照断言。

### 3. `0ap` 反转：自动化**确实在跑**（用生产证据闭环）

上一轮结论是「没有任何无人值守在跑」。本轮拿到硬证据：
`evidence/intel_pins_20260915_092119.json`（`09:38:19Z`，`dispatches=4 claims=10`）
+ **81 条** `intel_pins_20260915_0921` episode（`DISPATCH_INTEL_BEAST` ×4、
`INTEL_HERO_DISPATCH` ×4、救援 ×1、领奖 ×10，**全部 verifier ok**）。
**不是我手动发起的**（我唯一那次是 `09:56:05Z`，正是卡页零产出的那次）
⇒ 无人值守真的在执行，**上一轮修的 `0au`/`0e`/`0av` 真的被自动走到**
（这 81 条里 `cost_affordable` 出现 12 次）。

### 4. `0aw` 补强：让「调用了不存在的方法」这类缺陷由机器抓，而不是靠人肉 AST

`check_wiring.py` 加两条 AST 检查（私有 `self._x(` 解析、裸名解析），
并做**负向对照**：把 `0aw` 那条调用原样注入临时副本 ⇒ **抓到**；真实树 0。

### 方法论（本轮新增）

1. **两个离散的测量值（0 与 26）比一个平均值有价值得多。**
   如果一个失败有一组内部完全一致的观测量，那不是噪声，先去找那个不变量。
2. **同一个失败数字底下可能藏着两个不同根因**（`0ax` 红字 / `0ay` 小队设置页）。
   拆开之前不要宣布任何一个已修。
3. **修完要回头看那个数字的解释是否还站得住**：本轮把 6 条顽固的 `BTN_DISPATCH`
   从「未解」改成「修复前历史」，这本身是有价值的产出。
4. **「已记录」不等于「已修」**：`0az` 在 memory 里已有一条 17:20 的记录，
   但它只是记录；本轮把它变成了测量 + 代码 + 单测。
