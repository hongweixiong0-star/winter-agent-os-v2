# ALLIANCE_GIFTS — Just-in-Time Skill Research

Status: `VERIFIED` for Victory Loot (2 successes) and Ally Gift claiming (9/9); not `STABLE`.

## External prior

- Whiteout Survival Wiki event data treats claiming Alliance Gifts as a countable game action, which supports a list/claim workflow but does not establish current-client UI details.
- Community alliance guides describe gift boxes as alliance-wide rewards generated by member activity or purchases. This is mechanism prior only; V2 never initiates the purchase that may create a gift.
- The public `Shederator/wosbot` capability list explicitly combines Alliance Technology contribution with claiming Alliance Gifts. V2 extracts only the capability decomposition, not its architecture, coordinates, or assets.
- GitHub gift-code bots are a different capability and are excluded from this Skill.

## Candidate decomposition

`ALLIANCE home → Alliance Gifts → inspect tabs/counters → claim only already available free gifts → verify claimed counter/list reduction/reward feedback`.

## Live client calibration

- Page title: `联盟宝箱`; tabs: `战利品宝箱` and `盟友赠礼`.
- Victory Loot has per-item `领取`, `一键领取`, expiry timers, key progress, and `每日战利品宝箱上限`.
- First cycle: daily counter `444→463` (+19), key progress `71,636→71,867`, reward popup visible, and list entries changed to `已领取`.
- A second action crossed the game-day reset and landed on an unrelated page. Because no reward and no same-day counter transition were captured, it is recorded as `RESET_DURING_ACTION`, not success.
- After reset, the counter was `0/500`; the bounded retry claimed 61 gifts, changed the counter `0→61`, key progress `93,443→93,734`, showed reward feedback, and cleared the Victory Loot red dot.
- The Ally Gift tab states that member purchases generate gifts. V2 may claim already-created gifts but never follows its purchase-generating `前往` route.

Hybrid Vision uses templates for the page, tabs, claim control, and reward popup, while OCR supplies key progress, daily counts, gift badges, and claimed-list state. `verify_alliance_gifts_claim` requires reward feedback, a positive daily-count delta, nondecreasing key progress, and claimed entries after the action.

Nine existing Ally Gifts were claimed successfully. Eight ran through the V2 Brain → single Scheduler → Skill → semantic Executor chain. Level-1 gifts displayed 30 chest points; one level-3 gift displayed 120. The list can reorder/replenish immediately, and the alliance chest total can rise without this client acting. `verify_ally_gift_claim` therefore requires a personal badge decrease, claimed target-row transition, or final-button exhaustion in addition to positive progress. The cleared-state production smoke emitted `SAFE_STOP` with zero clicks.
