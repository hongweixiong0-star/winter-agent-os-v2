# Skill knowledge

Skill lifecycle is `DISCOVERED → CANDIDATE → VERIFIED → STABLE/BLOCKED`.

`GATHER_RESOURCE` is `VERIFIED` after three consecutive natural live cycles (attempts 6–8). It is not `STABLE`; long-duration operation has not started. P1+ capabilities remain candidates until their own just-in-time research and live verification.
