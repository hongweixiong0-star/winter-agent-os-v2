test(evidence): OPEN_MARCH_FORMATION is not the wall its deferral named

deferral:KEEP_MARCHES_PRODUCTIVE:3 blamed OPEN_MARCH_FORMATION for a route that
never showed progress. The route does not lack that capability. The frames in
dataset/truth_audit/march_formation_20260918/key/ show the RESOURCE_DETAIL page
with 采集 available before the tap, the formation page the tap produced, and the
bound verifier accepting the transition (RESOURCE_DETAIL -> MARCH, evidence
{available_resource: true, march_page: true}). What the route lacked was the
goal's own name on the steps that advanced it: the two pages a SEQUENCE route
finishes on discover no goal of their own, so those steps were filed under the
synthetic AUTO_DISCOVERY, which owns no meter -- neither progress nor stall.

This commit lands the two halves that were still local:

* tests/test_march_formation_attribution.py pins the invariant behind the
  run-scoped committed goal. It drives MAP -> RESOURCE_DETAIL -> MARCH and fails
  if the START_GATHER step is recorded under AUTO_DISCOVERY. It is
  discriminating rather than decorative: the pre-fix derivation yields
  ['KEEP_MARCHES_PRODUCTIVE', 'AUTO_DISCOVERY'] for the same run.
  (The runtime change itself was carried into cc17a1f, which swept this
  worktree while the edit was uncommitted.)

* the published evidence set, whitelisted in .gitignore and named in
  knowledge/goals/goal_capability_map.json so docs/CAPABILITY_COVERAGE.md points
  at it. report.json records what was measured and, explicitly, what stayed
  unproven.

Unproven, and said out loud rather than implied:

* DISPATCH_MARCH, the step after this one, stops at DISPATCH_NOT_PROVEN. That is
  the remaining wall of KEEP_MARCHES_PRODUCTIVE, not OPEN_MARCH_FORMATION.
* the live episode ran in named-task mode (--goal GATHER_RESOURCE), so every step
  carried that goal from brain.current_goal and the run-scoped commitment was
  never the deciding term. That path is guarded by the test above, not by this
  episode.

Verified: tools/check_wiring.py problems 0; tests/test_march_formation_attribution.py
4 passed.
