# 2026-09-25 — 停止 WorkBuddy 通道，改用本地 Qwen 做结构化 UI 动作

## 方向变更（操作者指令，本轮最高优先级）

**正式停止** V2 通过本地网关 / 开放平台 / OAuth / 会话桥接调用 WorkBuddy 桌面端模型。
WorkBuddy 退回为**人工直接使用的开发工具**。UNKNOWN 的作答改由**本机已部署的本地 Qwen**
在循环内完成：调度器决定**做什么**，Qwen 决定**怎么做**（输出结构化 UI 动作，不是坐标）。

### 实施（都是最小改动，没有新增第二套架构）

| 位置 | 改动 |
| --- | --- |
| `winter_agent_v2/local_qwen.py` | **新**：全项目唯一知道模型地址的文件。Ollama `/api/chat` + `format=json` + `num_ctx=8192` + `think=false`；`ask_json` 返回结果对象、从不抛异常；调用与失败全部落 `learning/local_qwen_calls.jsonl` |
| `winter_agent_v2/ui_planner.py` | **新**：结构化动作协议（6 个 decision / 10 个 action type）+ 精简 DecisionPacket + 严格解析 + **Advice 桥接** |
| `winter_agent_v2/runtime.py` | 只加一个可注入的 `advisor=` kwarg（与 `maa_adapter` 同款）；`_advised_control` 里 `take` 改成优先 `take_request(request)`（`getattr` 能力探测，旧 advisor 照常工作） |
| `tools/run_live.py` | 从 config 建 planner 并注入 Runtime |
| `config/v2.json` | 新增 `local_planner`（enabled=true）与 `workbuddy_channel`（enabled=false）两段 |
| `tools/control_panel.py` | `_unknown_tick` 传 `submit=False`；新增 `_workbuddy_channel_enabled()`；面板仍**照常 reconcile**（不留下悬空 in-flight 槽位） |
| `winter_agent_v2/unknown_dispatch.py` | `worker(submit=...)`：`False` 时只 reconcile 并报 `WORKBUDDY_CHANNEL_RETIRED` |
| `tools/unknown_ai_worker.py` | 读同一个开关；`--once`/`--loop` 只 reconcile，并**明说**是退役而不是坏了 |

**关键设计：复用现有答案协议，不建第二套。**
Qwen 的 plan 被翻译成项目**已有的** `unknown_advisor.Advice`（`action_kind=ACTION_ORDINARY`，
`target_anchor={"text": 客户端自己印的字}`）。于是下游的 grounding / 消耗风控 / 语义命名 /
执行 / verifier / 知识归档**一行没改**，换掉的只是"答案从哪来"。
`ManagedAdvisor` 就是那个 drop-in（`take_request` / `ask` / `read_request` / `pending`）。

**坐标不经过模型。** packet 里每个元素只有 `id / text / area`；回复里出现任何几何键
（`x/y/box/point/...`）由 `FORBIDDEN_KEYS` 递归拒绝并单独命名。落点由
`unknown_advisor.grounded_region` 在**同一帧**上从该元素自己的 OCR 框算出。

## 真机验收

- `STRUCTURED_OUTPUT_PASS` **已取得**：真实模型 2/2 合规；goal=DAILY_ROUTINE 时在 40 个元素里
  选中 `领取` 并**避开钻石**；冷启 40 s / 热 3.3 s。
- `MAA_EXECUTION_PASS` **已取得**（真机 MuMu 720×1280，前台 `com.gof.china`）：
  模型选 `E9 登录好礼` → 同一帧 grounding 出 (0.7708,0.1484) → MAA 点击 (555,190)，51 ms →
  `verify_ordinary_control_tried` ok。
- `GOAL_VERIFIED` **未取得**，第一个真实断点已确证：`page: MAP -> MAP`。

### 第一个真实断点（已用同帧证据确证，不是推测）

```
FAILURE_TYPE : SEMANTIC_TARGET_IS_A_LABEL_NOT_A_CONTROL
GROUND '登录好礼' box = y 0.1383–0.1586 (≈177–203 px)   <- 这是图标下方的标签
interactive_controls = 8，无一匹配 登录/探险/常规活动
tap = (555, 190) -> 落在标签上，没触发按钮
```

即：**模型选对了语义，是本项目的定位把"文字标签"当成了"可点控件"。**
不是模型问题、不是 MAA 问题、不是链路问题。下一步按宪法 §25 修**元素表**
（图标+标签合成一个控件盒子，以 before_frame.png 为 Positive、地图城镇名为 Negative），
**禁止**在点击处加偏移 / 按比例放大标签框 / 历史坐标兜底（宪法 A §二）。

## 测试与校验

- 新增 `tests/test_local_planner.py`（协议、几何拒绝、预算、Advice 桥接、COMPLETE 只是 claim）。
- **重写** `tests/test_qwen_decoupling.py`：旧版钉的"包内不得有模型模块、不得有端点"在
  planner 存在后已过时（宪法 §17：旧测试与当前正确设计冲突时先判断测试是否过时）。
  新断言仍逐条防水：端点只允许出现在 `local_qwen.py`；`brain/scheduler/skills/verifier/
  goal_library/executor/executor_router/maa_executor` 不得 import planner 或 client；
  `enabled=false` 时行为与从前一致。
- 测试抓到一个真实缺陷并已修：`ask_json` 在**服务不可达**时没有记账 —— 正是本项目禁止的
  "静默失败"。现在不可达也写一行。
- `tools/check_wiring.py`：`problems: 8`，与开工前**完全相同**，全部属于 goals/brain/training/gui，
  与本轮无关。
- `tests/test_control_panel.py` 的两个 `*_never_inherits_gather_actions` 失败：**开工前就存在**，
  已用 HEAD 版 `skills.py` 复现确认（与本轮无关，工作树里 skills.py 本就有未提交改动）。
- `tools/unknown_ai_worker.py --once` 的桩件签名同步更新：旧桩没有 `submit` 参数，
  会让"生产调用抛 TypeError 被 try/except 吞掉"这件事看起来是全绿的。

## 新增文件

- `winter_agent_v2/local_qwen.py` / `winter_agent_v2/ui_planner.py`
- `tools/probe_local_planner.py` / `tools/probe_planner_live.py`
- `tests/test_local_planner.py`
- `docs/LOCAL_PLANNER_ACCEPTANCE.md`
- `knowledge/failure_patterns/integration/WORKBUDDY_CHANNEL_RETIRED.md`

## 未做（按指令）

未申请或开通任何收费服务；未复制任何令牌 / Cookie / 会话 argv；未批量派发剩余 UNKNOWN；
未把模型推测存成 Skill；未因清理网关而删除任何仍被依赖的公共组件。
