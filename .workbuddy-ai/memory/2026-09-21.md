# 2026-09-21 — 当日流水（项目事实源：`.workbuddy-ai/memory/`）

## 能力预载：CAP-G09 TROOP_SELECT（出征页编队预设行）

**选择依据**：`tools/bootstrap_scan.py --top 12` 自己排的第 1 名
（P1 已解锁的 MISSING / NEVER_TRIED，score 749，知识来源 rung 1 V2_EVIDENCE），
不是我手挑的。它在真实路线上：`skill_factory.GOAL_REQUIREMENTS["PARTICIPATE_BEAR"]`
与 `goal_library` 都点名 `SELECT_TROOP_PRESET`。

**说明书来自本机已有资产，没有联网**：`knowledge/game/bear.json`（gate=VERIFIED，
confidence 0.98，带 6 张真机帧）+ `dataset/raw/bear_live_20260909/` 的 35 帧（全部在盘上）。
⇒ 原以为"需要一帧真机页面才能设计 TROOP_PRESET"，实际上那帧早就有了。

**实测（2026-09-21，720×1280 国服，全部读自归档帧）**

| 事实 | 读数 |
|---|---|
| 页面门控 | 条带 y=88..96 均色 (13,129,198)；B∈[192,198]、G∈[129,133]、R≤35 |
| 唯一干扰页 | 联盟科技页条带 B=227 —— 对称容差会放它进来，**改成按蓝通道判** |
| 全语料闸门 | 454 帧 → 26 帧读出本页；收紧前 4 个假阳性，收紧后 **0** |
| 找块 | y=104..144 偏离条带色的列连续段；本账号 9 段 = 8 预设 + 1 保存钮 |
| 预设命名 | 打野 / 书咏 / 杰西 / 杰赛尔 / 尼莫 / 熊6 / 熊7 / 开车 |
| 选中态 | 边框金 (245,188,61) vs 未选亮蓝 (87,190,255) |
| 真值复现 | `bear_preset6.png` 第 6 段 gold=81/blue=0，其余 8 段 gold=0 |

**意外发现**：这一行**不是巨熊专属**——26 帧覆盖采集 / 异兽 / 情报 / 巨熊四种出征页。
它是全局「出征」页控件，所以价值高于总表给它标的 `PARTICIPATE_BEAR` 单一目标。

**未证明（写在最显眼处）**：「点击 → 金边移动」**没有** before/after 帧对。
`bear_preset6.png`（战力 55,813,315）与 `bear_troop_setup.png`（96,098,265）是两次不同捕获，
条带外差异 37640 采样块 vs 条带内 504。⇒ 这正是真机校准要回答的唯一问题。

**落盘**

- `knowledge/preload/TROOP_SELECT.json` —— 12 个字段全部有来源（rung 3 EPISODE_EVIDENCE，
  封顶 UNVERIFIED），`missing=()`；`knowledge/preload/INDEX.json` 的 `sufficient` **0 → 1**。
- `knowledge/skills/TROOP_SELECT_RESEARCH.md` —— 实测读数 + 未证明项 + 还差什么 + 真机校准请求。
- `winter_agent_v2/vision.py::read_troop_preset_strip` / `TroopPresetStrip.center_norm`
- `tests/test_troop_preset_strip.py`（12 项：合成正负例 + 真机帧 + 全语料闸门）
- `tools/probe_troop_preset_{band,measure,colour,rows,rule,diff}.py`（6 个只读探针）

## 顺手修掉的真实缺陷

`knowledge_preload.py`：`KnowledgeRecord` 的 `notes` 每轮预载被 `+` 追加一次，
`TROOP_SELECT.json` 里同一句话累积了约 **560 份**。在 `as_json` 与 `from_json` 两侧去重
（一个叫 `_unique` 的纯函数），该文件 **560 → 5 条**。已污染的文件下次 load 自己变干净。

## 负结果（已回滚，别重试）

把设计稿放进 `dataset/candidate/TROOP_SELECT.json` 让扫描器少报一个缺字段 —— **实测更差**：
`_match_draft` 先按目录里的 `code`（`TROOP_SELECT`）命中草稿，于是
`prior = PRIORS.get("TROOP_SELECT")` 查空，Navigation / Action / Verifier 从"已有来源"
退化成 UNKNOWN，能力还被标成 `IN_FLIGHT`（既不再预载、又不在注册表里 ⇒ 死锁）。
**已删除**。根因是扫描器的草稿查找键应同时认 `existing_skill`。

## 两个扫描器盲点（新发现，未修）

1. **派生目标永远显示"未注册"**：`registered_semantics` 只认
   `dataset/candidate/template_manifest.json` 的模板记录，而 `TROOP_PRESET` 这类
   「由几何算出」的目标（同族：`RESOURCE_DYNAMIC` / `BEAST_SEARCH_TAB`）不在里面。
2. **草稿查找键**：见上。

## 真机校准：为什么没排上

`tools/bootstrap_scan.py --status` → `GATE_REFUSED: NOT_ARMED_MAIN_LOOP_P0`
（P0-A=PARTIAL, P0-D/F=NOT PROVEN）。这是操作者要求的顺序：主闭环未证完前，
预载不占设备、不占开发槽。请求内容已写在 `TROOP_SELECT_RESEARCH.md`，状态 `NOT_QUEUED`。

## Git

- commit `39eba75 feat(troop-select): find the preset row instead of pinning it`（本地）。
- **push 被拦**：`git_sync.py push` 报 `active agent d030d8b8`（OPEN_ALLIANCE_PAGE 的 job
  正在改同一棵树），且 `check_wiring` 此刻是 `problems: 9`（**全是既有项**：
  goal 路由 / 导航矩阵漂移 committed 96 vs fresh 99 / 隐藏窗口等，与本次改动无关），
  不满足工具自己写的"problems: 0"前置条件 ⇒ **没有用 `--allow-active-agent` 强推**。
  本地领先远端 28 commit，下次可用时再推。

---

## 自主操作过度限制：审计 + 三处放宽（2026-09-21 晚，操作者新指令）

操作者要求"全游戏自主探索 + 动作学习 + 经验驱动决策"。按项目 SOP 先做**审计**再动代码。

### 交付物 1：七条限制的确切位置

写进 `docs/AUTONOMY_OVER_RESTRICTION_AUDIT_2026_09_21.md`。**两条重要澄清**（都是误判）：

1. **R6「页面无法识别只等待」不存在**。实测是 `brain.py:433-434` 返回 **SAFE_STOP**，
   然后 `runtime.py:1375-1416` 会 `press_back()` 有界恢复（`max_unknown_page_backs=2`）；
   `WAIT` 只用于 `MAINTENANCE`/`LOADING`。⇒ 这条不用改。
2. **R1「未 VERIFIED 不许动」的传法是错的**：执行路径**不看 `skill.state`**，
   唯一门是 `runtime.py:1486` `decision.skill not in self.VERIFIED_ATOMIC`。
   `ready()` 只排除 `BLOCKED`。
   同理 R2：模板清单的 `status` **运行时不看**（全是 CANDIDATE），真正的门是"没有模板记录"。

另外发现一个**死字段**：`goal_library.py:139 retry_after` 全仓库无读取点 ——
"按时间重排被延期的 goal"目前**没有实现**（只有 streak + `until`）。

### 本轮实际改的三处（都有定向测试）

- `runtime.py` 验证失败分支：单步失败不再终止整轮，改为通过既有
  `_yield_to_next_goal` 把失败 goal 本轮让出，受新增 `max_verification_retries=2` 约束。
- `runtime.py` **执行器之前**新增护栏：本轮已失败过的控件不再点第二次（§七.3）。
- 位置教训：护栏**必须在执行器之前**。第一版放在 verifier 之后，实测"一个失败控件、
  两次相同点击"——它拦住了第三次，而规则说的第二次已经发出。

### 新增：控件级动作经验台账

`winter_agent_v2/control_experience.py`（与 `observation_store.py` 同形状：状态文件 + 纯函数，
不是第二个 Registry/Scheduler）。要点：

- 键是 `(page, semantic)`，**永不存裸坐标**；位置连同"它被读出的那一帧"一起存。
- `classify_change()` 给出 §六 的八类实际结果；**`NO_OP` 与 `UNKNOWN` 必须分开**——
  前者是"点了没动"（关于控件的事实），后者是"读不到"（关于读取的事实）。
- `explorable_risk()` 是白名单：真实货币 / 不可逆 / **不认识的 risk** 一律为假。
  风险未知不是风险低。
- `sterile` 只在连续 3 次 `NO_OP` 且从未有过已知结果时为真（§八：探索过 ≠ 永远重复）。

`Episode` 新增 `control` / `expected_result` / `observed_change`：
期望此前只活在 `Decision` 里、变化要靠手工 diff 两个 `asdict(WorldState)`，
现在落在同一行上。台账在 `_record_episode` 里折账、`finish()` 一次落盘。

### 踩到的两个坑（写下来省下一次）

1. `asdict(WorldState)` 里的 `page` 是**枚举**，`str()` 得到 `"Page.ALLIANCE"`。
   第一版台账因此把联盟页存成 `Page.ALLIANCE` 而所有读者找 `ALLIANCE`。
   统一走 `control_experience.label()`（取 `.value`）。
2. 测试里 `TemporaryDirectory` 会在返回路径**之后**删目录 —— 返回"路径"而不是"内容"
   会得到"台账根本没写"的假结论（实际写了）。



---

## Utility AI 融入现有 Scheduler（2026-09-21 深夜，commit d5b46c4）

操作者指令：Utility AI + 受阻重规划 + 资源感知 + 多角色公平 + 经验反馈，**直接改现有 Scheduler**。

### §二 核对结果（三条"已经存在，不要重造"）

1. **§八 场景 B（三兵营）已经实现**：`goal_library._append_camp_training_goals` 按营出票
   （busy→仅该营 COMPLETE、free→READY、未读→DISCOVERED），`world.camps` 有真机写入方
   （`ocr.py:2125/2135/2796/2800`、`vision.py:1956/1973`）。**没动它。**
2. **§五 事件驱动重规划已经存在**：`best_goal` 在动作循环内**每步**按当前帧重算
   （`runtime.py` 循环体内），不是每轮一次。**复用，没重写。**
3. **§四.7 的 DISCOVERED 公平机会已经是清扫设计**：`_sweep_value` 80→180，高于采集 70、
   低于真实领取 250。再加一个探索加分就是双重计价 ⇒ **没有加。**

### 真正缺的、也是本轮做的

- **`knowledge/strategy/stamina_routes.json` 从未被代码读取**（只在 `knowledge/game/_index.json`
  被索引）。它里面有实测的 `live_success_rate` / `cost_per_success` / `availability`
  —— 这就是 §四 要的"历史成功经验/执行成本"，一直躺在盘上没人用。现在读了。
  **只用 `cost_per_success`，绝不用界面显示值**（卡自己警告：显示 10、实际扣 7）。
- 新增 `winter_agent_v2/goal_utility.py`：history / resource_fit / fairness /
  repeat_failure 四项**有界可加**项 + `learning/decisions.jsonl` 决策日志。
- `goal_library` 新增 `rank()`（整盘 + 每项分解），`best()` 委托它；不传 world/fairness/routes
  时行为与原来**逐字节相同**（保住所有既有调用与测试）。

### 两条"故意不做"

- **没有加 retry 闸门**。`GoalState.retry_after` 确实是死字段，但让它成为**第二道资格门**
  只会延长封锁 ⇒ 违反 §六"不得更保守"；资格属于 `capability_gate`（已有 `probe_minutes`）。
  `blocked_until()` 只做**报告**："这个 goal 什么时候会被再看一眼" —— 此前项目答不出。
- **没有加探索加分**（见上"已经存在"第 3 条）。

### 关键不变量（`tests/test_goal_utility.py` 钉住）

所有正项之和必须**小于**「日常 70 → 真实领取 250」的间距。若将来有人抬高上限越过它，
测试会失败并说明原因 —— 比从一次"不再领日常"的真机运行里发现它便宜得多。
（`best()` 的注释记录过：曾经按状态分层，静默关掉了扫掠轮换，`CLEAR_INTEL` 变得不可达。）

### 真机证据（生产已加载，0 traceback）

`learning/decisions.jsonl`（7 行）+ `learning/goal_fairness.json`：

```
AVOID_STAMINA_WASTE  READY  base=2005.0 dyn=-20.0 total=1985.0   reason: repeat-failure -20.0
CLEAR_INTEL          READY  base= 800.0 dyn=+26.7 total= 826.7
KEEP_TRAINING_...    DISCOVERED base=180.0 dyn=0.0
[utility] chose KEEP_MARCHES_PRODUCTIVE (70; catalogue price only)
```

`AVOID_STAMINA_WASTE` 的 -20 是 §四.5 真的在跑（streak=1 ⇒ -60×1/3）；
`CLEAR_INTEL` 的 +26.7 是实测路线卡第一次进入调度；
`goal_fairness.json` 里 `last_block_reason` 已持久化（§五 的 `block_reason`）。

### 本轮**没有**做的（不要当成已完成）

- §八 的 A/C/E/F **没有真机验收**；B 是既有实现但也没跑真机。
- §七 **多角色完全不存在**：全仓没有 `switch_role` / `SWITCH_CHARACTER` / `SELECT_CHARACTER`，
  `role_id` 只是 Episode 上的一个字段（计划在 `docs/ROLE_SCOPED_CAPABILITY_PLAN.md`）。
  要真做必须先真机发现"怎么切角色"的 UI。
- 基线：`test_panel_routine_discovery.py` 的 hop-loop 测试失败，**A/B 判定 HEAD 上同样失败**；
  `check_wiring` 仍 problems: 9（逐条相同）。
