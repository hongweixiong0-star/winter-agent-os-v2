# 2026-09-15 — 接管 + 情报可用性假阴性修复（pin 地图）

## 接管结论（重建后的真实状态）
- git：接手时 HEAD `622fb46`，dirty 12；本轮 checkpoint `c2908ad`。
- episodes：接手时 1003（成功率 0.6914）→ 真机后 1033。
- runtime：`agent_state=DEGRADED`、`stop_reason=intel_not_available`、
  `unexpected_worker_exits=15`、`watchdog_restart_count=13`、`last_fatal_error=None`。
- evidence integrity：PASS（530/530 引用截图在盘）。
- **P0 复发：自动化接口 `list` 返回空数组。** 三份 handoff 都写着「常驻自动化 id
  `7c1c18c1-…`（ACTIVE，每小时）」，但接口查不到（磁盘上只剩 `.workbuddy/memory/automations/7c1c18c1-…/memory.md`）。
  与第九轮 `0o` 同型，**第二次复发** ⇒ 当时没有任何无人值守在跑。
  已重建 `e3485d0c-1b51-48a4-880c-c01fe0fdec19`（ACTIVE，每小时，cwds=`E:\无尽冬日智能体`），
  **并用 `list` 复核确实存在**。payload 换成真机证明有效的 `tools/run_intel_pins.py`。

## 本轮真实改进（Before → After）
同一张真机帧上的情报可用性判据：

```
Before  {"status": "NOT_AVAILABLE", "available_count": 0, "list_read": true}
After   {"status": "AVAILABLE", "available_count": 5, "pins": 5, "list_read": true}
```

根因链（真机复现，2026-09-15 07:36 GMT+8，MuMu 已连接）：

1. `tools/run_live.py --goal INTEL` 从 MAP 打开情报页 → 生产视觉报 NOT_AVAILABLE。
2. 同一帧 pin 检测器找到 4 个 pin；**另有 1 个带光晕的橙 pin 被比例闸门丢弃**
   （bbox w87 h137 ratio 0.635 < 0.65）。肉眼确认屏幕上是 **5 个任务 pin**（2 紫 / 2 蓝 / 1 橙），
   表头 `下次刷新:00:23:06`。
3. `goal_library.py:71` 把 `NOT_AVAILABLE` 映射成 `CLEAR_INTEL = COMPLETE`
   → AUTO **静默停止**，exit 0，看起来像成功。

修复（4 处，全部增量，无新架构、无第二 Scheduler/Registry）：

| 文件 | 改动 |
|---|---|
| `winter_agent_v2/intel_pins.py` | 长宽比下限 0.65 → 0.5（光晕把 blob 向下拉长） |
| `winter_agent_v2/ocr.py` | 情报页 UNKNOWN 时**先数 pin**，`pins>0 ⇒ AVAILABLE`；无 pin 才落回原表头规则 |
| `winter_agent_v2/skills.py` | 新增 `SELECT_INTEL_PIN`（点一个未试过的 pin） |
| `winter_agent_v2/runtime.py` | `INTEL_PIN` 动态解析 + 会话内已点 pin 去重 + `VERIFIED_ATOMIC` 注册 |
| `winter_agent_v2/verifier.py` | `verify_intel_pin_opened`（before 需 pin>0；after 必须是四张已复核任务卡之一） |
| `winter_agent_v2/brain.py` | INTEL + AVAILABLE + `pins>0` + 无 mission_type → `SELECT_INTEL_PIN`（排在 `READ_INTEL_LIST` 之前） |

`SELECT_INTEL_PIN` 的解析走「检测到的 pin 坐标」而不是模板：任务卡只在点开 pin 之后存在，
所以点击目标本身就是几何的。已点过的 pin 在本次运行内跳过（pin 在任务被消费后仍留在板上）。

## 重大发现：项目唯一的「空情报板」参考帧是错标
`dataset/truth_audit/intel_beast_target_20260914/03_intel_page_empty_list.png`
**实际是满板 13 个 pin**（体力 305、`下次刷新:07:59:21`）。

上一轮因为 OCR 只读到表头（无 `前往查看`）把它命名为 empty，并写了两个断言
`status == "NOT_AVAILABLE"` 的测试 —— **把 bug 写成了测试**。本轮：
- 文件重命名为 `03_intel_page_full_board.png`（**保留，未删除**；`evidence/INDEX.json` 会自动重建）。
- 两个测试改写为正确行为，并把证伪过程写进 docstring。

⇒ **本项目至今没有任何经过验证的「空情报板」帧**；`NOT_AVAILABLE` 仍然未被证实，
只能由「pin 检测器一个都没看到」到达。这也解释了为什么上一轮「等负样本」等不到：
它以为的那个负样本是错标的正样本。

## Live Evidence（真机）
`tools/run_intel_pins.py 8`（2026-09-15T00:08:23Z 起，6m22s，exit 0）
证据：`evidence/intel_pins_20260915_000822.json`；
episode_id：`intel_pins_20260915_000822_pin_00`、`intel_pins_20260915_000822_nav_01`。

- **`SELECT_INTEL_PIN` 4 次执行 / 4 次 SUCCESS（verifier PASS）** ⇒ LIVE_VERIFIED（attempts=4）。
  链路：`SELECT_INTEL_PIN → OPEN_INTEL_BEAST_TARGET → INTEL_BEAST_START_MARCH → DISPATCH_INTEL_BEAST`，
  以及 `SELECT_INTEL_PIN → OPEN_INTEL_RESCUE_SURVIVORS_TARGET → EXECUTE_INTEL_RESCUE_SURVIVORS`。
- 连带真实产出：`DISPATCH_INTEL_BEAST` ×3、`EXECUTE_INTEL_RESCUE_SURVIVORS` ×1、`INTEL_CLAIM_REWARDS` ×4。
  其中**救援任务是上一版永远看不到的**（模板层不认那个 pin）。
- 体力 39 → 26 → 16（真实消费，非记账误差）。

## 新增未决问题（已登记）
- **`INTEL_BEAST_START_MARCH` 的 verifier 写死任务等级**（`INTEL_BEAST_10`→22、`INTEL_FIREBEAST_10`→20）。
  `SELECT_INTEL_PIN` 现在会点开**未复核**的 pin，因此可能打开其他等级/类型的巨兽任务 →
  00:14:13 出现 `INTEL_BEAST_MARCH_NOT_PROVEN`（同轮前两次同技能是 SUCCESS）。
  与 P1 #17「verifier 落后于 brain/vision 的页面契约」同源。
- `run_intel_pins.py` 的导航周期被记成 `"navigation cycle"`，但它**现在会做真实工作**（本轮 nav_01 有产出）；
  且连续 3 次导航周期后无条件停止。
- pin 检测器在 **MAP 帧上会误报**（右侧 HUD 圆形按钮被当成 pin，4 个 BLUE）。
  ⇒ 它**必须按页面门控**；当前只在 `page == INTEL` 时调用，满足该约束。
  证据：`dataset/truth_audit/intel_pin_board_20260915/03_map_frame_detector_false_positives*.png`。
- 光晕橙 pin 的长宽比实测跨度 **0.521–0.644**（0.521 出现在那张满板帧上），0.5 的下限余量很小。
  稳健解法是**剥离光晕后量本体高度**，不是继续降阈值。

## 工具 / 环境
- MuMu 本轮**已启动且已连接**（`adb devices` → `127.0.0.1:7555 device`）。
- `date` 命令本轮在 Bash 里**可用**（与 handoff 记的「无 coreutils」不符）；`ls/cat` 未复测，仍按不可用处理。
- MaaFramework 取帧仍走 MAA（episode 里 `capture_backend=MAA_MUMU_EXTRAS`，`OPEN_INTEL` 51.25 ms）。
- 全量测试：`422 passed, 7 skipped`（改动前 3 个失败已按「旧测试断言了被证伪的前提」处理）。

## 新增/改动的工具与测试
- 新增 `tools/probe_intel_board.py`：同帧对比「生产视觉判据 vs pin 检测器」，并打印只被比例闸门拒掉的 blob
  （用于把误零归因到具体阈值，而不是猜）。
- 新增 `tests/test_intel_pin_board.py`（14 项）：光晕 pin、可用性、派发、verifier、注册可调度性。


---

---

# 第二轮（2026-09-15 00:20Z 起）：出征（部队编成）页被读成联盟首页

## 真实改进（Before → After）
`INTEL_BEAST_START_MARCH` 在真机上：`INTEL_BEAST_MARCH_NOT_PROVEN`（00:14:13Z）
→ 同一技能 verifier OK。

## 根因（推翻了上一轮 handoff 的猜测）
上一轮 handoff 写的是「verifier 写死了任务等级，新 pin 打开了别的等级」。**证据推翻了它**：
`state_before` 正是复核过的 `INTEL_BEAST_10 / 大角鹿 / level 22 / available`。
真正的缺陷是**出征（部队编成）页被分类成 `ALLIANCE/HOME`**。

同一个页面的两帧实测（这就是全部证据）：

| 语义 | 帧 A（同轮 SUCCESS） | 帧 B（失败帧） |
|---|---|---|
| `BTN_BEAST_DISPATCH` | d=0 命中 | **d=26 不命中**（阈值 8） |
| `PAGE_BEAST_MARCH` | d=0 命中 | d=0 命中 |
| `STATUS_VICTORY_ASSURED` | d=0 命中 | d=0 命中 |
| `PAGE_ALLIANCE` | d=8 命中 | d=8 命中 |

`BTN_BEAST_DISPATCH` 是**动画按钮**（真机帧上出征按钮还叠着 `00:00:29` 倒计时徽标），
hash 漂移越过阈值 8；它一失手，下一个命中的就是 `PAGE_ALLIANCE` 那条**标题条**模板（d=8），
于是整页被报成联盟首页。而**真正属于这一页的两个复核锚点
（`PAGE_BEAST_MARCH`、`STATUS_VICTORY_ASSURED`，都裁自 `dataset/raw/live_beast_march_selection.png`）
在清单里存在、却没有任何分支引用它们**（孤儿模板）——所以它们永远赢不了。

## 修法（`winter_agent_v2/vision.py`）
在 `BTN_BEAST_DISPATCH` 分支**之后**、`PAGE_ALLIANCE` 之前插入：

    if match("PAGE_BEAST_MARCH") and match("STATUS_VICTORY_ASSURED"):
        return WorldState(page=Page.MARCH, beast={"victory_assured": True}, confidence=0.99)

两个刻意的设计决定：
1. **放在按钮分支之后** ⇒ 按钮命中时行为逐字节不变（已 live-verified 的路径零风险），只在它失手时补位。
2. **不声明野兽名/等级**。出征页根本不显示它们；沿用邻居分支硬编码的「大角鹿/22」就是编造。
   只断言页面上真正画着的安全事实（绿色的「本次出征胜券在握」），这正是
   `verify_intel_beast_march_open` 需要的那一项。

## 语料闸门（2716 张真机帧，`dataset/raw`）
- 两个锚点任一命中的帧：**83**。其中当前判为 `MARCH` 的 **75** 帧（新分支输出相同 → 不变），
  判为 `ALLIANCE` 的 **8** 帧。
- 那 8 帧里 **3** 帧只命中标题条（`bear_live_20260909/auto_join_*`、`bear_troop_setup`）→
  因要求两个锚点同时命中而**不变**。
- 只有 **5** 帧两个锚点同时命中 → 全部改成 `MARCH`，且全部是同一张出征页
  （4 张来自失败 episode 目录 + 1 张探针帧）。
- 单独核对：**89 张联盟类帧，两个锚点零命中**。
⇒ 影响面**恰好 5 帧，零附带损伤**。

## Live Evidence
1. **真机帧 A/B（同一帧、补丁前/后）**：`dataset/raw/control_panel/probe/intel_board_20260915_003755.png`
   —— 00:37:55Z 从真机截的出征页（出征标题 + 本次出征胜券在握 + 出征按钮带倒计时徽标）。
   补丁前 `ALLIANCE {'section': 'HOME'}` → 补丁后 `MARCH {'victory_assured': True}`。
   即**缺陷与修复都在真机上被同帧证明**。
2. **真机端到端**：`tools/run_live.py --goal INTEL`（00:39:14Z 起，2m09s，exit 0）
   **10/10 步 verifier 全部 OK，`stop_reason=MAX_ACTIONS_REACHED`**（无失败）：
   `OPEN_INTEL` → `SELECT_INTEL_PIN` → `OPEN_INTEL_BEAST_TARGET` → `INTEL_BEAST_START_MARCH`(**OK**) →
   `DISPATCH_INTEL_BEAST`(**OK**，`marches=['MARCHING']`，`march_used=1`) → 再入情报页 →
   `INTEL_CLAIM_REWARDS`(**OK**) → `DISMISS_INTEL_GENERIC_REWARD` → `SELECT_INTEL_PIN`(Hero Journey) →
   `OPEN_INTEL_HERO_JOURNEY_TARGET`。
   ⚠ 诚实说明：这一轮 `INTEL_BEAST_START_MARCH` 是**走按钮分支**通过的（那一次 `BTN_BEAST_DISPATCH` 命中了），
   所以它证明的是「整条链路通」，**不是**「新分支在真机上被触发」。
   新分支的真机触发由上面第 1 条证明。

## 本轮新发现（已登记 0w，尚未修）
真机 episode 内部**自相矛盾**：目标卡是 `大角鹿 level 22`，但出征页 after 却是
`beast={'name': '麝牛', 'level': 9, 'victory_assured': True}` —— 来自
`BTN_BEAST_DISPATCH_MUSK_OX_9` + `STATUS_VICTORY_ASSURED_MUSK_OX_9` 分支在出征页上的**假命中**
（出征按钮与麝牛-9 的出征按钮同形）。
⇒ 出征页会被赋予一个**页面上不存在的具体身份**。修它要连带 `verify_beast_hunt` /
`verify_stamina_beast_*`（它们依赖麝牛/9 这些字面值），所以先记录、不动。

## 新增工具 / 测试
- `tools/probe_anchor_decision.py`：判定「孤儿锚点接线是否安全」——
  对每个命中锚点的帧，打印**当前生产分类结果**并按页聚合。这是接线前唯一该问的问题。
  ⚠ `tools/probe_page_anchor.py` 已被它覆盖（同轴），本轮**未删**，待确认后清理。
- `tools/probe_frame_semantics.py`：少帧 × 多语义的原始距离矩阵（另一根轴：单帧竞争关系）。
- `tests/test_beast_formation_page.py`（9 项）：回归帧、verifier 配对、brain 派发、
  联盟页负对照、按钮路径不变、以及两条**源码级护栏**（必须同时要求两个锚点；必须排在按钮分支之后）。
- 受保护证据：`dataset/truth_audit/beast_formation_page_20260915/`（01 回归帧 / 02 目标卡 before）。
  原始帧在 `dataset/raw/control_panel/**`，会被 retention 轮转，所以测试只能引用这份副本。

## 环境坑（本轮新增）
**不要把含反引号的文本塞进 `bash -c "..."`**：Bash 会执行反引号里的内容。
本轮第一次追加日志时就这么干了，`INTEL_BEAST_START_MARCH`、`PAGE_ALLIANCE` 等全部被当成命令执行、
文本出现空洞。**写文件内容一律用 Write 工具或临时 .py 脚本，不要经过 shell 引号。**

---

# 第三轮（2026-09-15 00:50Z 起）：先修「最高失败」的排序口径 —— 否则每轮都会追死问题

## 为什么这比修一个 bug 更值钱
`update_workbuddy_handoff.py` 的 `top_failures` 按**全时段计数**排序，于是 AUTO 块写着：

    CURRENT ROOT CAUSE: SEMANTIC_TARGET_NOT_VERIFIED x112

按天拆开：**09-12: 36 / 09-13: 37 / 09-14: 8 / 09-15: 0**（另 31 条无 `recorded_at`）。
`MARCH_PAGE_NOT_OPEN x59` 更极端：**59 条全部在 09-12 一天**。

⇒ 于是流传着两个错误印象：
1. 「`SEMANTIC_TARGET_NOT_VERIFIED` 是当前头号根因」——今天 0 次，最后一次是 09-14T14:08。
2. 「`START_GATHER` 是 94 次尝试 / 37% 成功率的最差高频技能」——**那 59 次失败全部是 09-12 的
   `MARCH_PAGE_NOT_OPEN`**；09-13 之后它零失败。它被扣上 `degraded` 帽子完全是历史包袱。

本轮差一点就按第 1 条去开工。**这不是报告问题，是决策输入被污染**。

## 改法（`tools/update_workbuddy_handoff.py`）
- `episode_state()` 为每个 failure_type 记录 `recent` / `last_seen` / `dates` / `undated`；
  `recent` 的窗口是**相对最新 episode 时间**的 2 天（`RECENT_WINDOW`），按**完整 ISO 时间戳**比较，
  不是按日历天 —— 否则「某天有一条新的」会把那天 47 小时前的失败也算成 recent。
- `top_failures` 改为 **`key=(-recent, -count)`**：近期优先，全时段计数作次键。
- `CURRENT ROOT CAUSE` 走新的 `failure_rank_line()`：recent=0 时直接输出
  `HISTORICAL, do not treat as the current defect`。
- `04_OPEN_ISSUES` 的机器块也带 `recent=` 与 `last seen`。
- `tests/test_handoff.py` 加 4 项护栏。⚠ **坑：pytest 默认只收集 `Test*` 类**，
  我第一版写成 `class FailureRecencyTests`（无 `Test` 前缀）→ 测试**静默不被收集**（10 passed 而不是 14）。
  已改成模块级函数并把这个坑写进测试 docstring。

## 修复后的实际排序（recent 优先）
    SEMANTIC_TARGET_NOT_VERIFIED  112 全时段 / 45 recent / last 09-14T14:08
    RESOURCE_NOT_FOUND             30 / 30 / 09-14T06:42   （见 0h 说明：多为 harness 行为）
    DISPATCH_NOT_PROVEN            29 / 28 / 09-14T05:25
    STAMINA_SOURCES_NOT_OPEN        8 /  8 / 09-14T23:37
    INTEL_HERO_DISPATCH_NOT_PROVEN  8 /  8 / 09-14T12:19
    INTEL_RESCUE_START_NOT_PROVEN   6 /  6 / 09-14T17:17
`MARCH_PAGE_NOT_OPEN`（59）已掉出前六 —— 它的 recent=0。

## 结论：当前几乎没有「活着的」未解释失败
09-15 全天只有 **1 条失败**，就是本轮已修的那条。所以下一阶段的价值不在「修最高失败」，
而在**能力缺口**（4 个 BLOCKED goal / 8 个 PARTIAL）与**无人值守时长**（72h soak 至今未启动）。
