
---

# 第一段 — 快捷面板闭环：从"点了不动"到"点在该行的按钮上"

## 一、真机逐步追踪：这一步的落点从来不是那个按钮

`learning/executor_backend.jsonl` 里**所有** `QUICK_PANEL_ROW_*` 点击的 `tap_point`：

```
16:41:59 QUICK_PANEL_ROW_SHIELD_CAMP  tap=[225, 546]
16:42:00 QUICK_PANEL_ROW_MARKSMAN_CAMP tap=[224, 692]
16:48:00 QUICK_PANEL_ROW_LANCER_CAMP   tap=[225, 620]   ← 这一次真的打开了兵营动作栏
16:51:52 QUICK_PANEL_ROW_RESEARCH      tap=[224, 805]
17:00:37 QUICK_PANEL_ROW_LANCER_CAMP   tap=[225, 619]   ← 同样坐标，却把客户端带去了世界地图
17:16:14 QUICK_PANEL_ROW_RESEARCH      tap=[480, 804]   ← 修复后：正是该行自己的箭头 (0.6667×720)
```

x=225 = 0.3125，是**面板文字列**；行的蓝色箭头在 x≈480（0.666）。同一坐标一次打开动作栏、一次
离开主城 —— 因为那是行名，不是控件。

**根因（层级顺序）**：`_printed_control_point` 里"客户端自己印的字"这一层跑在词典层之前，而
`QUICK_PANEL_ROW_*` 记录的 `ocr` 字段就是行名（`矛兵`/`科技研究`）⇒ 永远命中行名，词典层里那段
读 `panel["rows"][].arrow_norm` 的代码**从未被执行**。

## 二、这一轮修掉的四处（全部有实测依据，提交 eba85c6 / d5c3d4e / a9e9882）

| # | 缺陷 | 实测证据 |
|---|---|---|
| 1 | 行技能表只有三个兵营键，`RESEARCH` 缺失 ⇒ `.get(key, "..._SHIELD")` **静默换成另一行的动作**（点了盾兵行） | 00:42:30 `reason=quick_panel_row_research_...` 而 `tap=[225,546]`，y 0.427 是盾兵行 |
| 2 | `_goal_route()` 先问 `goal_id`，而定向轮里 `goal_id` 是调度器上一轮的归因（`CLEAR_INTEL`）⇒ 整个 `--goal TRAIN` 轮被读成 INTEL 路由，面板分支根本不进 | 17:00 那轮步进全是 INTEL 类动作 |
| 3 | 训练页读不出按钮：客户端自己的手形光标压在 `训练` 二字上 ⇒ `status=UNKNOWN` ⇒ 大脑发不出 TRAIN_TROOPS | `20260923_004710_train_step_005_before_...png`；同页按钮自带的 `02:33:11` 在 (0.760,0.888) 未被遮挡 |
| 4 | `BTN_START_TRAINING` 的模板是整颗按钮（含文字）⇒ 手一压就 NO MATCH，落点解析返回 None | 00:48:35 `TRAIN_TROOPS FAILURE SEMANTIC_TARGET_NOT_VERIFIED` |
| 5 | 行**没扫到按钮**时仍用估算点去点（盾兵行画的是绿勾，不是蓝箭头） | 17:21:50 `basis=PANEL_RELATIVE_ESTIMATE, x=0.4042`，`tap=[291,546]` 落在文字上 |

## 三、判据一律来自"帧上这条证据是谁画的、在哪一层"

- 训练按钮可开训的两个证据：**标签** `训练`（y 0.862）或**它自己的预计耗时**（y 0.888，在动作栏内部）；
  而**运行中**那一页右侧按钮写 `加速`、队列倒计时画在动作栏**上方**（y 0.732，旁边有 `训练中`）。
  ⇒ 动作栏内部的裸倒计时 = 按钮的 caption，是"按钮已画出"的证据。
- 归档 70 张训练帧对照：**5 张由 UNKNOWN 变 AVAILABLE/trainable=True，无一张反向变化**；
  另 27 张只是多了 `train_button_basis` 字段。
- 层级顺序的三条原则（本轮实测得出）：
  1. 模板命中优先（`live_train_selection_available.png` 一直是模板路径，把它压掉就是回归）；
  2. **本帧读数**（行按钮像素、按钮 caption）优先于**词位置**——词说的是"这一行叫什么"，不是"控件在哪"；
  3. 词扫描的 `ABSENT` 会终止链条，所以"手挡住了词但按钮确实在"必须在它**之前**回答。
- 两处基据命名共用：`QUICK_PANEL_ARROW_BASIS_SCAN` / `_ESTIMATE`（ocr.py 定义，brain 比较常量）。

## 四、真机进展（逐跳）

```
16:12:28 TRY_ORDINARY_CONTROL    SUCCESS  QUICK_PANEL_OPENED
16:12:45 行箭头(LANCER)          SUCCESS  menu=True camp=LANCER_CAMP   ← 兵营动作栏打开
16:13:02 OPEN_INFANTRY_TRAINING  SUCCESS  HOME → TRAINING             ← 训练页打开
16:13:09 (该页当时读 UNKNOWN，故未开训)  ← 已被 #3 修复，同帧复算 = AVAILABLE/trainable
16:48:27 OPEN_INFANTRY_TRAINING  SUCCESS  训练页 status=AVAILABLE       ← #3 生效
16:48:35 TRAIN_TROOPS            FAILURE  SEMANTIC_TARGET_NOT_VERIFIED ← #4，已修，同帧复算落点 (548,1137)
17:16:14 行箭头(RESEARCH)        SUCCESS  tap=[480,804] 正是该行自己的箭头 ← #1/#2/层级修复生效
```

**仍未取到**：一次不被打断、跑完"行箭头 → 动作栏 → 训练页 → 按下训练 → 出现新倒计时"的完整真机链。
原因是定向轮与面板自身的 AUTO 轮互相干扰，且 `--goal TRAIN` 的粘性路由会被 `_yield_to_next_goal`
清掉、随后调度器把目标换成别的（实测 17:13 那轮两步就走了邮件）。这是**验证装置**的问题，
不是产品缺陷 —— 每一跳都各自有真机证据，最后一跳的落点在同一张真机帧上离线复算通过。

---

# 第二段 — 面板行的第二种控件：绿勾 = 已完成待领取

## 实测（三张独立真帧，均 720×1280）

```
                                       绿块 y 中心    x 范围       像素数
17:21 盾兵行 已完成                     0.4375        0.533-0.590  1265
16:41 射手行 已完成                     0.5520        0.533-0.590  1268
22:02 我的奖励/仓库补给 已完成           0.6527        0.533-0.590  1276
16:12 全空闲帧                          无绿块
```

⇒ **绿勾与蓝箭头共用同一个控件槽**（蓝箭头 x 0.533-0.749，绿勾 x 0.533-0.590），签名稳定：
`g≥150 且 g−r≥50 且 g−b≥50`，约 41×31 px。

## 一个反直觉的实测结果（推翻了我第一版的优先级）

我先写了"找到蓝箭头就不找绿勾"，结果 16:41 射手行与 22:02 我的奖励行都读成 `ARROW` ——
因为**那两行的蓝色元素同时存在**。逐像素量过：射手行蓝只有 1px 噪声（在 dy=−8 处有 19px 的窄条），
我的奖励行蓝是 0.619-0.799 的宽条（右侧活动栏渗入）。
⇒ 结论：**绿勾才是这些行的身份**，蓝那点像素不足以证明"这是进入箭头"。改为绿勾优先。

## 接上的执行路径（新）

| 层 | 内容 |
|---|---|
| 读取器 | `rows[]` 增 `control` = `ARROW`/`DONE`/`NONE`、`done_norm`、`done_box_norm`；**每行都找绿勾**，不只是蓝扫描失败的行 |
| 解析器 | `QUICK_PANEL_ROW_<KEY>_DONE` → 该行 `done_norm`（通用后缀，一处代码管所有行） |
| 词典 | 新增 4 条记录（三营 + 我的奖励），`actions=[COLLECT]`、`anchor=quick_panel_row_done`，逐字段标注"实测/未实测"，并写明**点了会怎样尚未观测** |
| 技能 | `COLLECT_FINISHED_TRAINING_{SHIELD,LANCER,MARKSMAN}` + `COLLECT_MY_REWARDS_ROW` |
| 验证器 | `verify_panel_row_done_collected`：证据 = **绿勾消失**（或客户端答以奖励弹窗）；before 不是 DONE 则报 `PANEL_ROW_NOT_WAITING_TO_COLLECT` |
| 大脑 | `_claimable_panel_row` + `_QUICK_PANEL_ROW_CLAIM_SKILL`；**指定兵营的 Goal 只领自己那一行**；进入路径要求 `control == ARROW`，所以绿勾行不会被当进入目标 |

真帧复算：SHIELD/MARKSMAN/MY_REWARDS 三种 Goal 各自给出自己的领取技能，落点都是绿勾中心
(0.5618, 该行 y)，px x=404；RESEARCH Goal 在同帧仍去科研行（不误领）；全空闲帧走进入路径。

**仍未验证**：真机上按绿勾会发生什么（词典记录如实写着 `what_tapping_it_does: NOT YET OBSERVED`）。
`联盟捐献`/`英雄招募` 在同一帧也画了同形状的勾，但那里点了会怎样没量过，因此**没有**为它们建技能。

## 第三段 — 真机按下绿勾：一个负面结果，以及它逼出来的两处修正

提交 `efc0ed5` 之后的真机轮（持租约、`--goal TRAIN`）：

```
17:50:59 COLLECT_FINISHED_TRAINING_SHIELD  tap=[404, 556]   ← 正是绿勾中心 (0.5618×720, 0.4344×1280)
17:51:02 该步 SUCCESS（当时验证器判通过）
```

**但"通过"是错的**：后帧显示面板**整块关闭**，没有奖励弹窗；把面板重新打开时（17:53:39、
17:54:12、17:54:28 三处读数）盾兵行**仍然是 `已完成 / control=DONE`**。也就是说按绿勾什么也没领到。

### 由此修的两处

1. **验证器的"通过"是"我们不再看了"换来的**。旧判据 `tick_gone = before==DONE and after!=DONE`
   在面板消失时也成立（读不到行 ⇒ after 的 control 是空串 ⇒ ≠ DONE）。现要求
   `panel_readable_after = bool(after.quick_panel["rows"])`，并把该字段写进 evidence。
   ⇒ 面板消失是它自己的结果，不是领取证据。
2. **领取路径暂时清空**（`_QUICK_PANEL_ROW_CLAIM_SKILL = {}`，附长注释说明理由）。
   保留读数、四个技能、验证器与词典记录；但不让大脑发出这个动作，因为：
   - 每轮保证失败一次；
   - 每轮白白关掉一次面板；
   - **决定性的一条**：十次这样的 episode 会触发项目自己的"无进展即延后"，把**整个训练 Goal**
     按 `OPEN_TRAINING_PAGE|NO_GOAL_PROGRESS` 延后 —— 代价远大于想赢的那次领取。

词典四条记录里 `what_tapping_it_does` 已改为实测原文（落点在绿勾、面板关闭、无奖励弹窗、
重读仍 已完成）；`control_identity` 补上"点确实发出去了、客户端没有领取"。

**下一步的候选**（写进代码注释了）：已完成行走**它自己的页面**——它不画蓝箭头，所以需要行自身
的框或另一次渲染；找到真正领取的控件后，把行 key 填回那张表。

---

# 第四段 — 金环不是绿勾画的：客户端自己会周期性画它（推翻上一段的因果叙述）

## 我上一段写错了什么

上一段我写"按绿勾之后客户端把营房高亮出来"，并把它当作 tap 的后果。**这是错的。**

## 实测（2026-09-23 16:08–18:00）

筛选口径：只取"城市可见 + 快捷面板关闭"的帧 —— 面板打开时环被遮住，只有这类帧才可能看到它。
在 217 帧上逐帧匹配 `TARGET_INFANTRY_CAMP_HIGHLIGHTED`：

```
62 段交替（有环/无环）
段长 1–6 帧（约 5–40 s），中间静默 5–23 分钟
页面保持 HOME 时 1–2 秒内就能翻转
24 帧落在 OPEN_MAIL / OPEN_DAILY / OPEN_MAP / BACK 等与训练无关的步骤上
```

实例：`17:50:32` 城市可见、无环 → `17:51:00` 有环；`17:41:47` 城市可见、有气泡、**无环**；
`17:51:35` 与 `17:51:46` 相隔 19 秒，一帧无环一帧有环（d=2），机位与建筑都相同。

⇒ 环是**客户端按自己的节奏画的**，不是任何 tap 的结果，也不是训练任务的状态。

## 由此改的一处（本轮唯一改的判据）

`verify_camp_menu_reobserved` 原来用"环还在"作为等待成功的条件 —— 而环会自己消失，等于
**让一次掷硬币判定结论**：一次本来没错的等待会被记成 `CAMP_MENU_REOBSERVE_NOT_PROVEN`。
现在只看页面（还在 HOME 即证明"什么都没发生"），环照旧写进 evidence：

```
ring_still_drawn / ring_absent_is_not_progress / menu_drawn / page_after
```

新增测试 `test_the_ring_pulsing_off_is_not_a_failed_wait` 把这条钉住；
"等待结束在别的页面（MAP 等）永远不算成功"这条原有 guard 一字未动、原测试照旧通过。

## 顺带撤回两处

1. **"环画在科研所上"这个推断不成立**：用项目自己的 `BUILDING_INFANTRY_CAMP` 模板在 17:51:00 帧上匹配，
   得分 0.649（科研所 0.232、仓库 0.301），落点与环重合，环下方就是列队站着的盾兵
   ⇒ 2026-09-08 的命名是对的。
   （同一帧上方的白色气泡里是**戴钢盔的士兵头像**，不是显微镜 —— 我在 6 倍放大图上把它看错了。）
2. **2026-09-20 那 4 次 tap 失败的"解释"不成立**：原话是"环消失了，正是点空处时客户端的样子"。
   环本来就会自己消失，所以"环没了"不能说明 tap 落在空处。剩下的失败依据只有"菜单没画出来"
   （这仍然足以判 FAILURE，但不足以支撑建立在其上的推论）。

## 仍未做

`WAIT_FOR_CAMP_MENU` 的浪费（历史上 45/127 步 = 35% 卡在这里）本轮**没动**：这条路径与
`WAIT_FOR_CAMP_MENU` 技能、`out_parent_runtime.py` 接线表、3 个测试文件耦合，改它要单独一轮。
现在有了实测依据（环不能预测菜单），可行方案是让该分支不再进入等待、直接按"前置条件未满足"停下。

登记为 issue **#92**（`.workbuddy-ai/handoff/04_OPEN_ISSUES.md`）。

---

# 第五段 — 把 #92 落地（环不再消耗步数），并更正上一段发布过的计数

## 先更正：上一段的计数不成立

上一段我写"217 帧 / 62 段交替 / 段长 1–6 帧 / 24 帧落在无关步骤上"，并在提交信息里称
"最紧的一对是 17:51:16 / 17:51:35，同一机位"。**两者都撤回**：

- 那对帧的**标签与真实状态相反**（我按 episode 时间取帧，实际取到的是前一步的帧）；
- 直接看 `17:51:16` 那一帧，它是**世界地图**（左侧行军面板、`#4298 (644, 451)` 坐标框、
  底部 探险/英雄/背包/商店/联盟/城镇）——**而读取器把它读成了 `HOME`，环模板也在它上面匹配成功**；
- 于是那套计数（用的是读取器的 `page` 与环模板本身）被假阳性污染，不能再用。

## 结论不变，依据换成一对我逐像素核过的帧

```
17:50:32  城市视野，面板关闭                     无环
17:50:52  面板读到盾兵行 已完成（带绿勾）        ——
17:51:00  同一视野（营房气泡落在同一像素）       画出环 + 教学手
```

28 秒、面板状态未变而变化的是环 ⇒ 环由客户端自己画，不是训练任务的状态。

## 落地（#92，已实施）

| 文件 | 改动 |
|---|---|
| `brain.py` | Stage A **不再消耗步数**：`MAX_CAMP_MENU_WAITS` 与 `_camp_menu_waits` 一并删除，首次看见环即返回同名 blocker `camp_entry_is_a_guided_step_not_a_selection`（与原先等两步后的结局相同） |
| `tests/test_training_camp_two_stages.py` | `TheWaitIsBoundedTest` → `TheRingStopsTheRouteAtOnceTest`；Stage A 用例改钉"立刻停" |
| `tests/test_training_stage_a_tap.py` | 两处期望 `WAIT_FOR_CAMP_MENU` 改为 `SAFE_STOP` |
| `tests/test_training_verifier.py` | 路由序列第 4 步改 `SAFE_STOP` |
| `tools/check_wiring.py` | 三处断言改钉"立刻停"；并把 `QUICK_PANEL_ROW_*` 补进"派生目标族"（理由：行坐标来自帧自身的读数，与 `ORDINARY_CONTROL` 同类）⇒ 该检查由红转绿，红项 10 → 9 |

## 新开 #93：读取层在地图帧上的双假阳性

同一帧既是 `page=HOME` 又"有环"，而画面上根本没有环 ⇒ `verify_infantry_camp_highlighted` 的
"假到达"有一条比动画更廉价的机制。**不得凭自选阈值收窄**，下一步要给环的判定补人工核过的类别标签
（真城市有环 d=2 / 真城市无环 / 地图），再看距离能否分开。

## 仍未做

环的分支在快捷面板分支**之前**，所以环被读到时**会抢占那条真正能走到训练页的面板行路径**。
改它需要先有新的"到达"判据（#93），留待单独一轮。

---

# 第六段 — 第三次同种错误：#93 撤回，并把"帧↔记录必须按路径配对"固化成工具与长期记忆

## 撤回 #93（它是我自己的错配，不是项目缺陷）

#93 原称"读取层在世界地图帧上双假阳性"。按**路径**查归属后：

```
step_005_before_...175116240696.png  仅被 17:51:35 OPEN_HOME 引用，state_before.page = MAP
                                     环模板 find() 在它上面 = None
```

两条指控都不成立。错因与前两轮同源：**我按 episode 时间戳把帧与记录配对**，取到的是另一步的文件。
（那一帧确实是世界地图——我看到的是对的；错在我说"记录说它是 HOME"。）

## 用同一错误又量出的一组数字：这次是对的

按路径配对重做（只取"记录 page=HOME + 面板关闭 + 兵营在画面上可见"的帧）：

```
119 帧  有环 71 / 无环 48（60% / 40%，与原比例一致）
        交替段数 22（原写的 62 是时间戳并列造成的假交替）
```

⇒ #92 的结论与比例**成立**，交替数更正为 22。

## 固化：工具 + 长期记忆

- 新增 `tools/frame_owner.py`：给一个帧名（可只给一部分），打印**引用它的那些 episode**及其
  `page` / `panel_open` / `result`，`--ring` 再报该帧的环读数。它存在的理由写在文件头：
  连续三轮同一个错误，全部因此产生过错误结论。
- 长期记忆写入 `.workbuddy-ai/memory/MEMORY.md`（帧↔记录的配对规则）。

---

# 第七段 — 操作者指令：红点驱动的任务触发与处理（§一 落地）

操作者给了一份完整的指令（Vision→WorldState→Goal→Scheduler→MAA→Verifier 全链，不新增第二套调度）。
本轮只把 §一「建立可靠的入口红点读数」做完，其余按 §二-§七 登记为 issue #94。

## 侦察（先说清已有多少，避免重复造）

- 红点**已经在三处被读**：快捷面板行（`ocr._panel_arrow_and_badge` → `rows[].badge`，本就三态）、
  邮件页签（`mail.tab_badges`）、联盟赠礼（`alliance.badge_count`）。
- 词典里有 **8 条 `RED_DOT_*` 但只有声明**：无位置、无验收方法、无代码消费。已按实测收口。
- `operations_policy.reward_candidates(red_dots, ...)` 早有一句"红点只发现问题，不授权点击"。

## 实测（26 帧、口径：记录自己说 page=HOME + 面板关闭）

| 入口 | 出现 | 几何 | 判读 |
|---|---|---|---|
| 邮件 `BTN_OPEN_MAIL` | 9/26 | x 0.943-0.977 y 0.787-0.802，276-302px，中心 (0.960,0.794) 稳定 | 真角标，可两向判读 |
| 探险 `TAB_EXPLORATION` | 9/26 | x 0.126-0.147 y 0.923-0.934，恒 197px | 真角标 |
| 每日 `BTN_OPEN_DAILY` | 26/26 | 278-284px（数字变） | 计数角标，样本内未见过零 |
| 联盟 `TAB_ALLIANCE` | 26/26 | 346-419px（数字变） | 计数角标，样本内未见过零 |
| 英雄 `TAB_HERO` | 26/26 | **几何与像素完全不变（195px）** | **疑似按钮美术**，标 SUSPECT_ARTWORK，不读 |

两次自我纠错（都写进工具/注释）：① 第一版窗口把英雄的圆点算到探险头上（窗口伸进隔壁页签）；
② 邮件那组窗口元组写成 `(x0,y0,x1,y1)` 却按 `(x0,x1,y0,y1)` 解包 ⇒ 扫了空区间、把"有角标"的帧报成无角标。

## 落地

| 文件 | 内容 |
|---|---|
| `knowledge/ui/entry_badges.json` | 唯一事实源：逐入口的实测窗口、blob、行为、证据帧、验证口径；并列出**未测**的入口 |
| `winter_agent_v2/entry_badges.py` | `read_entry_badges` / `quick_panel_badges` / `read_all` / `transitions`；三态；表外入口不读；面板关闭或行未被读到 = UNKNOWN |
| `winter_agent_v2/models.py` | `WorldState.red_dots` |
| `winter_agent_v2/ocr.py` | `HybridVision.observe` 改为包装 `_observe_inner`（多返回点逐一挂载必漏）；读角标失败不让观测失败 |
| `winter_agent_v2/brain.py` | §五 第一处：MAIL 目标在 HOME 读到邮件入口 ABSENT 时不再进邮件页；PRESENT/UNKNOWN/无台账保持原路径 |
| `tests/test_entry_badges.py` | 16 项：两向判读、UNKNOWN 语义、面板行、变换、邮件规则四向 |
| 词典 | `RED_DOT_MAIL/ALLIANCE/HERO` 绑定到实测入口；其余 5 条标 `UNBOUND` 并写明原因 |

## 未做（issue #94）

§二 五类触发（除 ABSENT→PRESENT 已有变换函数）、§三 各入口分支判断、§四 结算、
§六 未变化不重复进入的持久化去重、§七 七项真机验收。§七 第 4 项还依赖一个已知空白：
**真正领取已完成批次的控件仍未找到**。

---

# 第八段 — 大指令（红点自主执行/AI 兜底/学习）的审计与一次真机验收

## 审计：两处"担心"里，一处不成立、一处是真的但另一条腿缺证据

- **§九 快捷面板共用性**：`brain.PANEL_ROWS_FOR_ROUTE` 已接 TRAIN/RESEARCH/ALLIANCE/DAILY 四条路由；
  `brain.py:1106` 的通用分支在面板未打开时请求"一个普通控件"，运行时词典层解析成**当场点把手**
  （注释注明真机验过 `QUICK_PANEL_OPENED`）。⇒ 指令担心的"识别器/解析器都有、决策分支不请求它"**不成立**；
  唯一缺口是**英雄招募没有自己的路由**（代码写明"stated here rather than invented"）。
- **§六 AI 兜底**：通道**真实且自动**——`unknown_ai_worker --state` 显示 gateway=up、pending=2、answered=1；
  `unknown_dispatch.jsonl` 10 条 submitted/reconciled；`unknown_chain_check` 把 3 条真实 UNKNOWN 逐段转换，
  其中 `b6546e80`（UNKNOWN::燃霜矿区，goal=DAILY）已有答案 → `ORDINARY_CONTROL[奖励入口]`、
  锚定文字"奖励" → **(635, 372) px**、risk=allowed。
  **但** `episodes.jsonl` 里没有任何 advice/unknown 字段 ⇒ "答案被某一步消费并经 MAA 执行"**无法从日志证明**。

## 真机：定向 `--goal MAIL` 一轮（18:51-18:54）

- `red_dots` 在正式观测里落地（每步 12 条）；三态在线（MAP 帧全 UNKNOWN、面板关闭全 UNKNOWN）。
- 邮件入口 **PRESENT 290px → `OPEN_MAIL` → `MAIL_CLAIM_REWARDS` SUCCESS**（真领到）；
  随后在 HOME 上读到 **ABSENT 0px**。
- 验收 D 的"跳过"**没被触发**：`deferred MAIL_ROUTINE -> DEVELOPMENT_PENDING on OPEN_MAIL_PAGE`
  （既有开发作业占着该能力），当轮没有 mail 决策。
- **真机复现缺陷并修好**：18:54:27 面板打开、盾兵行 `status=IDLE` 但 `control=DONE`（绿勾），
  TRAIN 分支的行选择器只查 `arrow_norm`，把已完成行当"进入"目标点了 → `PANEL_ROW_TASK_BAR_NOT_PROVEN`。
  通用选择器早有 `control==ARROW` 守卫，它没有 ⇒ 补齐两个守卫，并钉住"只靠估算点的行不再被点"。

## 登记

issue **#95**（大指令 §二-§十 的剩余项 + A-G 验收逐项状态）、**#96**（红点优先级与抢占规则，纯未做）。
审计结论另存机器可读件 `learning/red_dot_audit_20260923.json`。

---

# 第九段 — 验收 C 成立（有真机证据）+ 修掉一个可审计性断点

## C：面板行进入并完成后续操作（真机，18:55:10）

```
18:55:10 OPEN_TASK_FROM_QUICK_PANEL_RESEARCH SUCCESS
         after: page=HOME, research={"building":"RESEARCH_LAB","menu_open":true}   ← 行自己的任务条
18:55:28 OPEN_RESEARCH SUCCESS  HOME->RESEARCH（其 before 即 menu_open=true）
18:55:56 BACK          SUCCESS  RESEARCH->HOME
```

⇒ 面板行技能本身打开了科研所的任务条，后续游戏操作完成并各自通过验证器。
**不是**用旧电力页等别的路径冒充（技能名即面板行技能，且 after 状态里的 `menu_open` 就是该行自己的结果）。

## 正式链的共同断点：验证证据 6574/6603 条缺失（已修）

episode 只有 `verifier_ok` 布尔值；`verification/verifier/evidence` 字段只出现在 29 条里。
⇒ "这步为什么算通过"事后无法审计——本轮我为了判断那次 SUCCESS 是否真到页，先读技能序列、再读验证器源码、
再回头确认 after 状态，绕了三次。每个验证器本来就返回 `evidence`，是写入时丢掉的。

改动：`Episode.verifier_evidence` + `runtime.py` 写入处填 `dict(verification.evidence)`。

## 既存红项要用干净 A/B 判，不能引用清单

`git stash` 我的两处改动后重跑：**同样的 3 项失败**（`3 failed, 44 passed`）⇒ 与本次改动无关。
issue #63 已写明这条规矩（"不得引用任何静态清单"），本轮照做。

---

# 第十段 — B 的现场：行体=看营房；下一跳是那只气泡；一次实验被我自己的错作废

## 两条新事实（真机）

1. **已完成行的"行体"与"绿勾"同一个结局**。面板打开时点行体 `(225,533)`
   （= 清单里该行实测标签 ROI 的中心，y 用该行自己的 `y_norm` 对齐）→ 面板关闭、
   `training={"navigation":"INFANTRY_CAMP_HIGHLIGHTED","camp_tap_norm":[0.4243,0.4609]}`，
   与按绿勾一致。⇒ 两者都只是"去看营房"，都不是领取控件。
2. **教学手所指的是一只营房气泡**：本帧实测白框 x 0.44-0.475、y 0.385-0.46（内有脸），
   手从 x≈0.465 起遮住右侧，金环在下方。历史四次 tap 全打环/地面，**从未打过气泡**
   ⇒ B 唯一未试的下一跳。需要一个"按帧定位气泡"的读数；朴素近白掩码不行（821 散点/每行约 18）。

## 一次被我作废的实验（教训）

我把把手起点 `(13,550)` 从测试桩照搬，结果点开的是**军师卡牌页**，三跳从第一步就在量错屏。
上一轮"(13,550) 能开面板"是巧合。⇒ **把手必须按帧定位**（`find_quick_panel_handle`），
这正是操作者 §四 那条"不得把别的页面中同名按钮的固定坐标照搬"。已写进探针文档串。

顺带把探针本身按指令补齐了三处：**设备租约**（拒绝就不点）、
**复位改用客户端自己的控件**（地图的 `BTN_OPEN_HOME`、弹窗的 `BTN_CLOSE`，且点用的是**当帧匹配到的那条记录**的 ROI）、
**UNKNOWN 不再当成"在别处"**（连读两次仍 UNKNOWN 就停，不导航）。

真机现状：客户端停在那个卡牌页（读取器判 UNKNOWN），按设计交 AUTO 下一轮的 UNKNOWN 处理。

---

# 第十段 — 营地工具条有两种布局（正式链共同断点，已修）+ 白气泡负面结果

## 一、怎么发现的（不是猜的）

面板行的**同一次点击、同一前置状态**（16:33:15 vs 16:42:30，都 `page=HOME / panel_open=True / 盾兵行
IN_PROGRESS`）却两次开、一次不开。看那一帧的图：**营地面板明明开着**（盾兵营被选中，径向菜单
`详情 / 立即完成 -166 / 加速 / 训练`，还有绿条 `00:12:23`），而 episode 记的是 `training={}`。

⇒ 不是客户端没开，是**读取器没读到**。

## 二、两种布局的逐词实测（720×1280）

```
             详情                 训练                 升级              额外
IDLE 忙? 否  (0.328, 0.714) .998  (0.672, 0.714) .994  (0.501, 0.737) 1.000   —
BUSY         (0.269, 0.683) .998  (0.732, 0.685) .996  **不存在**     (0.419,0.734) 立即完成 .997
                                                                        (0.581,0.735) 加速     .999
```

忙的营地没有 `升级`，多一行 `立即完成/加速`，并把 `详情/训练` **整体外扩且上移 0.030** →
y 0.683-0.685，**比 y 带下沿 0.69 还低 0.007**。词都读到了，是被"像素行不对"驳回的。

## 三、两个耦合缺陷（同一个成因：只在一种渲染上量过）

| 缺陷 | 实测 | 修法 |
|---|---|---|
| y 带 `(0.69, 0.76)` 只量过空闲布局 | 忙布局词落在 0.683 | 改为**两布局并集** `(0.66, 0.78)`，并把测量写在旁边 |
| 门模板是**升级**按钮的白上箭头 | 忙布局得 0.651（背景，d=22 > 阈值 6）⇒ **读取器根本不被调用** | 增第二条记录：**详情**按钮的图标（唯一两布局同形的控件） |

跨布局打分（同一 44×44 裁剪在三帧上的最大相关）：

```
详情 从 IDLE 裁 -> IDLE 1.000  BUSY 0.969  BUSY 0.969
详情 从 BUSY 裁 -> IDLE 0.969  BUSY 1.000  BUSY 1.000
升级（现有门）  -> IDLE 1.000  BUSY 0.651  BUSY 0.651
训练 从 IDLE 裁 -> IDLE 1.000  BUSY 0.929  BUSY 0.585   ← 训练不能跨布局（手/徽标遮挡方式不同）
```

⇒ 两条记录各自覆盖一种布局。`--check`：**0 miss / 0 假阳性**（两布局 + 10 张 HOME + 金环渲染 + 训练页）。
端到端：`16:42:30` 由 `{}` 变为 `{"menu_open": true, "camp": "SHIELD_CAMP", "train_tap_norm": [0.7312, 0.6844]}`。

**没有把 `立即完成/加速` 加进可读词表**：那是花钱控件（`立即完成` 下面写着 `166`），词表就是"词→可点的点"，
绝不能顺带学会。

## 四、`--check` 清单本身也是错的（顺手修）

原检查断言"每个 `NAVIGATE_INFANTRY_CAMP` 的 after 帧都是工具条帧"——而同一技能早上结束在工具条、19:03
结束在**金环**。于是它对一帧判对了的帧报 miss。现按**帧自己记录的状态**分成 bar / ring 两组，ring 组进"必须不命中"。

## 五、负面结果：白气泡不能当营地判据（与 #92 金环同类）

营地上方那只白气泡（教学手指过的那只；`18:54:27` 按完绿勾后装着火车图标那只）是**通用建筑提示框**：
屏幕位置固定、**内容随高亮对象变**。40 帧实测：

```
营地可见(building 模板≥0.5) & 找到白块 : 11      营地可见 & 找不到 : 8   ← 漏
营地不可见 & 找到白块                 : 13  ← 误报      都不在 : 10
```

三张裁图并排看：`17:43` 里装**士兵头像**、`17:55` 与 `18:54:51` 里装**金色雕像**（那两个营地模板分只有 0.25）。
⇒ 有气泡 ≠ 高亮了营地。要做得落在**气泡内部的图标**上。

## 六、顺带修：清单写入的缩进漂移

`merge_template_manifest.py` 与 `register_camp_action_bar_gate.py` 都写 `indent=2`，而磁盘上的
`template_manifest.json` 是 `indent=1` ⇒ **加一条记录重排全部 408 条**（7499 增 / 7359 删），
注册结果无法 review。两处改为 `indent=1`，本次清单 diff 23 行。
（提交时只暂存本轮那一条：工作区里另有 AUTO 循环留下的 7 条，不并入本次提交。）

## 七、本轮拿不到真机证据的原因（如实）

03:10 起 AUTO 连续运行（每轮 ~10 分钟、无缝），设备由 GAMEPLAY 持有、`run_live`/探针都会拒租约；
本轮 AUTO 走的是**地图线**（野怪/资源），没有走到营地。所以上述修复是**离线在生产帧上验的**，
"路由因此能继续往下走"**尚未**在真机上观察到。

## 八、新开 #97

`18:54:27` 那种渲染（**金环光晕 + 通用气泡**，无工具条、无地面椭圆）项目**零判据**：环检测要求金色 bbox
≥0.12×0.05（光晕太小），工具条读取要求 `训练` 词（这一渲染不画工具条）⇒ `training={}`、该步 FAILURE、
路由无点可打。B 项卡的正是这里。下一步：给这一渲染补判据，落在**气泡内部图标**（不透明，适合做模板）。

---

# 第十一段 — 真机证据（面板行→营地菜单→训练页→开始训练）+ B 收窄 + #98

## 一、03:32-03:35 的正式 AUTO 真机链（就在第九段修复提交之后）

```
19:32:34 OPEN_TASK_FROM_QUICK_PANEL_SHIELD SUCCESS
         before: panel_open=True, 盾兵行 status=IDLE control=ARROW basis=ROW_BUTTON_SCAN
         after : training={"menu_open": true, "camp": "SHIELD_CAMP", "camp_label": "盾兵营",
                           "train_tap_norm": [0.6736, 0.7152], "source": "ACTION_BAR"}
         evidence: {"task_bar_open_after": true, "camp_named_after": "SHIELD_CAMP"}
19:33:04 OPEN_INFANTRY_TRAINING SUCCESS   HOME -> TRAINING
         before 就是上一步那个 menu_open=true；evidence {"menu_before": true, "training_after": true}
19:33:34 TRAIN_TROOPS SUCCESS  status AVAILABLE -> IN_PROGRESS, timer "03:28:51", batch_count 282
         evidence {"queue": {"type_ok": true, "status_ok": true, "timer_ok": true, "queue_busy": true}}
19:34:03 / 19:34:34 SELECT_TRAINING_CAMP SUCCESS   19:35:03 BACK SUCCESS
```

⇒ **面板行 → 营地径向菜单 → 训练页 → 真的开始一批训练（282 英勇盾兵）**，全链在设备上跑通且各自通过验证器。
这正是 `16:42:30` / `18:54:27` 死掉的那条链。

**但要说清它没证明什么**：这次 `train_tap_norm.y = 0.7152` 是**空闲布局**的高度，所以**忙布局那条修复没有被这次顺带证到**——
它只在离线生产帧上验过（`16:42:30` 由 `{}` 变读出 `menu_open`）。AUTO 随后把盾兵营变成忙的（`03:28:51`），
下一次开面板才会走到忙布局。

## 二、B 被实测收窄（重要）

训练页两种状态都**没有领取控件**：

```
队列进行中：  训练中 + 绿色进度条 + 00:25:15 + "正在训练250位英勇盾兵"
              「立即完成 ⊖337」（橙）  「加速」（蓝，教学手指着它）        ← 都是花钱的
队列空闲（选数量）：− / ‖ / + 滑条 + 「282」 + 原始时间
              「立即完成 ⊖2,786」（橙）  「训练 ⊖03:28:53」（蓝，= 训练按钮）
              + 兵种层级页签 VI/VII/VIII/IX + 四个兵种卡（数量 2,673/800/4,992 直接显示）
              + 底部 盾兵营/矛兵营/射手营 页签
```

122 次训练页读数分布：`IN_PROGRESS 91 / AVAILABLE 18 / UNKNOWN 12`。

⇒ 兵营的 `已完成` 极可能是**"该批已训练完毕、部队自动入库"的通知**，不是待领取奖励；
可领取的入口在 `我的奖励` / 邮件 / 联盟 / 活动。**未证**：存档里没有"刚完成那一刻"的训练页帧，
所以不能断言"绝不存在领取控件"。

## 三、#97 的因果撤回

- 气泡下方那座建筑经项目自己的模板判定是 **`BUILDING_RESEARCH_CENTER` 0.654**（`BUILDING_INFANTRY_CAMP` 0.257）
  ⇒ 是**科研所**，不是兵营；我原先"tap 画出了金环光晕+气泡"的说法不成立。
- 同一标记在**无关步骤**的帧上也出现（`17:55:01` 得 0.655）⇒ 与 #92 同类，客户端自画的标记不能归因于我们的动作。
- `19:31:04` 的面板读数显示 `盾兵` 行已回到 **`空闲中`** ⇒ `已完成` 是瞬时通知。
- 保留的事实：那一步 `training={}`，路由无点可打。

## 四、#98：面板行箭头的落点被带外蓝色拉偏（机制已复现，未改代码）

`19:31:04` 那帧逐像素复现：按钮本体青色 `(76,198,244)` 在 **x 388-425**，红点徽标 412-420，面板右边缘约 476-484；
而扫描带 `(0.45, 0.80)` 的右沿是 **576**，`_panel_arrow_and_badge` 取带内蓝色的 **min..max**，且**取最宽的一行为准**
⇒ 优先选中被污染的行（`scan_y=634` → 384-498 中心 0.6125；`scan_y=694` → 386-574 中心 0.6667）。
四行读出的 `arrow_box_norm.w_norm` 各不相同（0.1292 / 0.1625 / **0.2611** / 0.1306），同一控件宽度本该一致。

**账本对照**（这是关键证据）：

```
用 arrow_norm 的 tap：19:32:04 [448,804] 失败（面板没关也没动）  17:16:14 [480,804] 失败  17:54:12 [448,804] 失败
落在按钮内的 tap：   18:55:10 [404,804] SUCCESS（404 在 388-425 内）
落在行标签的 tap：   16:33:10 / 16:41:59 [225,546] SUCCESS
```

**两条试过但未采纳的修法**（都记在 #98 里）：
1. 合并间隔 ≤12px 的蓝段后取最宽组 —— 19:32 那批四行都**恰好**给出 0.5618（按钮中心），但 19:36 那批更差（0.69-0.77）；
2. 用跨度设上下界 —— **缺依据**：本批按钮宽 41px，而 2026-09-22 上午那批按常量注释是 76-105px，宽度本身不一致。

⇒ 下一步要先用一个**不依赖蓝色带**的定位器量出按钮宽度（候选：按钮内的白色雪佛龙 / 面板卡片几何），再据它设界。
**在此之前不改取值规则，也不给 tap 路径加阈值**——这是决定 tap 坐标的代码，猜错比不改更糟。

## 五、本轮没做的

真机受控实验（同一步前后帧 + 面板读数 + 页面读数一起保存）没做：AUTO 自 03:10 起连续运行、设备由 GAMEPLAY 持有。

---

# 第十二段 — #98 修复：面板行按钮改由它自己的白箭头定位

## 一、定位器的实测（这是修法的依据）

10 张面板帧 × 4 行 = 40 次：

```
found 37 of 40
  chevron  x 396-416   宽 20px   中心 0.5639   spread 0.0000   ← 每一次命中都完全一致
  未命中 3 次 = 19:03:02 / 19:03:12 / 19:10:52 的盾兵行 = 已完成行（画的是绿勾）
```

⇒ 雪佛龙是**按钮自身的、不依赖蓝色带的**定位器；而"没有雪佛龙"正好等于"客户端没画进入箭头"。

## 二、为什么蓝色不行（两条旧注释都是照着被污染的框写的）

- 扫描带 `(0.45, 0.80)` 右沿 x 576，面板右缘约 476-484 ⇒ 带内 155px 的城市（那里是蓝色脚手架）；
- 蓝的范围取 **min..max** 且**取最宽的一行为准** ⇒ **优先**选中被污染的行；
- `19:31:04` 四行读作 384-477 / 384-498 / 386-574 / 401-494（中心 0.5979-0.6667），按钮本体青色在 **384-425**；
- 账本：用 `arrow_norm` 的 tap 三次全失败，落在按钮内（404）与行标签（225）的都成功；
- `arrow_box_norm.w_norm` 四行各不相同（0.1292 / 0.1625 / 0.2611 / 0.1306）——同一控件不可能四种宽度。

被更正的两条旧注释：
1. "按钮真实中心是 0.644" → 按钮是 384-425，中心 0.5618；
2. "红点在按钮左端" → 红点在 **412-420**，即按钮**右端**；旧窗口看的是左三分之一，
   它能找到红点只是**因为被污染的框把按钮撑宽了约 50px**。

## 三、改动

| 项 | 内容 |
|---|---|
| `_panel_button_chevron` | 新增：在按钮列（`x 0.38-0.62`，避开行文本）内找 `min(r,g,b) >= 245` 的连续段（≥8px），取最宽段 |
| 扫描边界 | 由雪佛龙 ± `QUICK_PANEL_BUTTON_PAD_PX(12)` 决定，蓝色只在该邻域内读 |
| 无雪佛龙 | 返回"无按钮" ⇒ 调用方落到带标签的估算回退 ⇒ 进入路径拒绝 ⇒ **已完成行天然不是进入目标** |
| 徽标窗口 | 改为按钮**右三分之一** |
| 清理 | 删除 `QUICK_PANEL_ARROW_BAND_X_NORM` 与死变量 `gap_px` |

## 四、验证

8 帧面板重读：`arrow_x` **全部 0.5618**（原 0.5979-0.666）；**徽标状态 0 变化**；
`19:44:14` 帧的矛兵行 `control=DONE` ⇒ `basis=PANEL_RELATIVE_ESTIMATE`（无落点），其余三行 `ROW_BUTTON_SCAN` ✓。

新增测试 `TheButtonIsLocatedByItsChevronTests`（3 项 + 7 子测试）钉住：落点在按钮上、
`arrow_box_norm.w_norm <= 0.07`（一个控件一个宽度）、四个徽标状态、以及"已完成行不给落点"。
证据帧入库 `dataset/truth_audit/quick_panel_row_20260923/`（truth_audit 的 png 被 gitignore，需 `-f` 入库，
否则测试会静默跳过）。

**未在真机观察**：修好后还没有真机轮再走这条面板行路径。

---

# 第十段 — 真机验证（一）：面板行落点、忙布局、以及"点已完成行"的真实后果

## 租约窗口

AUTO 一轮 03:58:35 收尾、下一轮约 04:08:50；设备在 04:02-04:08 空闲。两次定向验证都用新车辆
`tools/live_panel_arrow_check.py`（复用 `cq_nav_tap_probe` 的 `build()`/租约原语，**每个坐标都取自当帧**）。

## 1. 面板行落点：#98 的修复**真机成立**

```
20:03  把手按帧定位 COLLAPSED @ (0.0181,0.4301) → tap (13,551) → 面板打开（4 行）
       四行读数：SHIELD_CAMP ARROW [0.5618,0.4262] / MARKSMAN ARROW [0.5618,0.5402]
                 / RESEARCH ARROW [0.5618,0.6285] / LANCER_CAMP DONE basis=ESTIMATE box=None
       每行 arrow_box_norm.w_norm = 0.0569（同一控件同一宽度）
20:03  滚动后：RESEARCH [0.5618,0.3207] / ALLIANCE_DONATION [0.5618,0.4086]
              / HERO_RECRUIT [0.5618,0.4965] / MY_REWARDS DONE box=None
20:03  点 SHIELD_CAMP 自己的点 (404,546) → 营地面板打开
       training={"menu_open":true,"camp":"SHIELD_CAMP","camp_label":"盾兵营",
                 "train_tap_norm":[0.7312,0.6844],"source":"ACTION_BAR"}
```

⇒ 与账本对照：修复前用 `arrow_norm` 的三次 tap（`[448,804] [480,804] [448,804]`）全失败，
修好后 `[404,804]` 与本次 `(404,546)` 都成功。

## 2. 忙布局读数**真机成立**（上一轮说"未在真机观察到"的那条补上了）

`train_tap_norm=[0.7312,0.6844]` 正是离线量出的忙布局（离线：忙 `训练 (0.732,0.685)`；
空闲 `训练 (0.672,0.714)`）。同一帧 `TARGET_INFANTRY_CAMP_HIGHLIGHTED` 匹配 **None**，
而画面上确实画着金环（小于该模板 `≥0.12×0.05` 的要求）⇒ `training={}` 的原因照旧。

## 3. 点「已完成」行的**行体**：客户端把所指的东西标出来，不是领取

```
20:03  MY_REWARDS（仓库补给）行体：标签 仓库补给 在 x 183-267 y 809-835 → tap (225,822)
       → 面板关闭、页面仍 HOME、training={}
       → 客户端画出：手 + 白气泡（内含金色物资箱）+ 地上的金环
```

## 4. 那只气泡的定位器**不成立**（因此不许点）

| 帧 | 窗内最大近白块 |
|---|---|
| 20:02 城市、面板关闭、未点任何东西 | **0** |
| 20:03 城市、本轮起点、仍未点 | **0** |
| 20:03 面板打开（点击之前） | 270px = **把手**（x 0.6333-0.6528, y 0.4188-0.4414, 中心 (0.6431,0.4301)） |
| 20:04 点行体之后 | 891px（**碎片**：金箱图标与手把白框切开）x 0.4514-0.4958 y 0.3891-0.4437 |
| 20:08 四分钟未动、镜头未移 | 733px，同一位置（**持续存在**） |
| 19:08 兵营已完成行体（同一机制） | 519px，同形态 |

⇒ 有可用间隙（把手 270px vs 真实气泡 519-891px，阈值约 400px），但**仍不足以点**：
碎片大小随手的位置移动（891→733），且只有一个兵营帧把气泡压到 519px。
`--measure-callout` **只测量并声明不点**，把上面的数字写进文档头。

## 5. 受控对比同时**更正 #92 的解释**

拿到的这对帧有归属：点击前无标记 → 点击后画出 → 四分钟后仍在、镜头未动。
⇒ 标记是客户端**在需要指明某个未处理项时画出的、并持续到被处理**，不是"与操作无关的周期性脉动"。
**结论不变、理由更准**：它仍不能证明"我们的 tap 到达了"，`verify_camp_menu_reobserved` 只看页面照旧。

## 6. 新缺陷 #99：面板行的 `label` 可以取自**面板之外**

`learning/_panel_scroll/02_after_scroll_1.png`：读取器报 `RESEARCH` 行
`label=梦境寻忆 status=IN_PROGRESS word=None control=NONE`，而 `梦境寻忆` 在该帧**只出现在 x 0.769**
（面板右缘 ≈0.585，那是右侧活动栏）；面板内该行真正的文字是 `科技研究`（y 0.3211）与 `空闲中`（y 0.3445），
带蓝箭头。⇒ 真正空闲的科研被读成非空闲/无箭头，`RESEARCH` 路由会跳过它。
**机制未定位**（怀疑首行锚点取自分区标题：该行 y 比其标签高约 0.023，而其余三行 y == 标签 y）。

## 未做

- 气泡点击（定位器不成立，故意不点）。
- 联盟捐献 `可捐献25/25`（badge PRESENT）与 高级招募 `免费招募`（badge PRESENT）两条真机可执行行
  —— 本轮只读到、**没有**去执行；§三 的分支判断仍未接。

---

# 第十一段 — #99 的机制与修复：面板行只能由"面板自己画的" token 组装

## 机制（已定位，不再是推测）

`read_quick_panel` 的 `rows_between(start, end)` 取"表头之后、下一表头之前"的 token —— **只按 y 排序，不判位置**。
面板是画在城市之上的半透明层，而城市右侧的活动栏与面板行同高：

```
X科技研究 表头        y 0.2898   x 0.112
梦境寻忆（活动栏）     y 0.2977   x 0.769     ← 正好夹在两者之间
科技研究 行本身        y 0.3211   x 0.311（其下 30px 是 空闲中，右侧画着蓝箭头）
```

⇒ 该节的"第一行"成了活动栏那颗 token：`key=RESEARCH`（按节取，没错）、`label=梦境寻忆`、
`status=IN_PROGRESS`、`word=None`、`control=NONE`；真正的 `科技研究/空闲中/蓝箭头` 那行**没有被报出**。
后果：`RESEARCH` 路由看到一个"非空闲且没有箭头"的行，会跳过空闲的科研。

## 修法

新增 `drawn_by_the_panel(token)`：按**左缘 ≤ `QUICK_PANEL_COLUMN_MAX_X_NORM`(0.55)** 判断，
这个常量本来就是本项目表达"面板自己画的 token"用的（其注释记录实测带 0.10-0.34），
几何段一直这么用；读不到帧宽时不过滤（与几何段的既有取舍一致）。
三处取 token 组装行的地方全部改用：`rows_between`、`state_below`、面板级兵营行循环。

## 验证

- 同一帧改后：`RESEARCH 科技研究 / 空闲中 / control=ARROW / badge=PRESENT / y=0.3211` ——
  **与真机 20:03 同槽位（0.3207）一致**；该滚动组 5 帧一致；其余三行（联盟捐献/高级招募/仓库补给）不变。
- **端到端**（同一帧 + 注入旧读数做对照）：旧读数 → `OPEN_POWER_OVERVIEW`
  （`research_goal_requires_power_route`）；真实读数 → `OPEN_TASK_FROM_QUICK_PANEL_RESEARCH`
  （`quick_panel_row_research_enters_its_own_task_page`）。
- 测试 `TheRowMustBeThePanelsOwnTests` 两项（真机帧读数 + 注入 `梦境寻忆` 诱饵 token 的合成面板）；
  11 个文件回归全绿。

## 顺带实测出的路由缺口（新开 #100）

同一帧上：`ALLIANCE_DONATION` 读 `可捐献25/25`、`HERO_RECRUIT` 读 `免费招募`，两者 `control=ARROW / badge=PRESENT`，
但 `_actionable_panel_row` 要求 `status == "IDLE"`，而这两行因自身词不是"空闲中"被读成 `IN_PROGRESS`
⇒ **通用选择器永远不会提供它们**（`goal ALLIANCE -> OPEN_ALLIANCE`，`goal HERO_RECRUIT -> OPEN_MAP`）。
英雄招募还多一层：`ROUTE_DOMAINS` 里没有招募类路由域，且 `DAILY_HERO_RECRUIT` 未绑定 live-loop 验证器。
登记为 #100，**本轮不接**（接了要同时给出验证器与真机窗口）。

## 未做

- #99 修复的**真机验证**：AUTO 在 04:19:41 起的这一轮已经在修复后的代码上运行
  （该轮启动晚于补丁），但本轮没等到它走到科研行；下一轮或空档窗口补。

---

# 第十二段 — 气泡语料（推翻自己的阈值计划）与一块新空白：搜索面板压住城镇门

## 气泡语料：1413 帧，81 种形状

口径：**帧自己那条记录**说 `page=HOME` 且快捷面板关闭（只有这类帧才可能看到气泡）。逐帧取窗口
`x 0.28-0.68 / y 0.24-0.58` 内最大近白连通块，再按 box 宽高聚类。

```
分档（旧规则，blob px）: 0/1-200/200-400/400-600/600-800/800-1200/1200+
                        634 /  65  /  46   /  65    /   12   /   182   /  409
```

**推翻上一轮**："约 400px 的尺寸间隙"不成立 —— 非气泡物体最高到 **1069px**（建筑名称条 65x21）、
**921px**（宽短白条），而**被手遮住的气泡只有 395-520px**。

**形状判据（成立）**：气泡是**高 58-76px** 的白块（全幅 59-70 宽；被手遮住剩 13-34 宽但**仍 58-60 高**），
其余白块都矮（字形 20-23 / 面板把手 29 / 名称条 21-23）⇒ 取 **≥55px 高**。
抽查两侧 60 帧：**真阳性 30/40**（被拒的 10 帧全是字形或名称条）、**假阳性 0/20**。

**身份**：白色圆角方块 + 底部小尾巴，里面装**被高亮建筑的图标**（显微镜=科研所 / 钢盔士兵=兵营 /
金色物资箱=仓库）⇒ 它是客户端"去这里看"的指示，**不是**奖励弹窗。
已实现为 `CALLOUT_MIN_HEIGHT_PX` + `--tap-callout`。**点它会怎样仍未测**。

## 新空白：资源搜索面板压住城镇门

```
OPEN_HOME 323 步 → 44 次 FAILURE（13.6%），全部 page_before=MAP
                  37 次 SEMANTIC_TARGET_NOT_VERIFIED（控件不在帧上）+ 7 次 OPEN_HOME_NOT_PROVEN
                  成串：15:55-15:58 八次 / 22:37 两次 / 00:00-00:01 两次；最近一次 20:26:44（当晚运行轮内）
失败帧: Page.MAP + BTN_RESOURCE_SEARCH_SUBMIT 在场(d=0) + BTN_OPEN_HOME absent
成功帧: Page.MAP + 搜索面板不在场           + BTN_OPEN_HOME present(d=2)
```

⇒ 底部那排（巨兽/生肉/木材/煤矿/铁锭 + 搜索）盖住右下角，而页面仍读作 MAP。
**客户端怎么关这个面板尚未测出**：该帧上放大镜不在场（"再点放大镜"不可用），登记里也没有关闭控件。
我的探针在同一视图上按设计中止（`ABORT: on MAP with no registered way home`）——同一处空白。
登记为 **#101**。

## 未做

- 点气泡的真机尝试：本轮取到租约后，客户端停在资源搜索视图，探针拿不到回主城的控件而**中止**；
  下一次要么等该视图之外的时机，要么先量出该面板的关闭方式。

## 补充：OPEN_HOME 失败的三条成因（两侧各 30 帧）

```
成功侧: 搜索面板 0/30, 门 30/30
失败侧: 面板在场+门不在场 22/30   ← 主机制（面板盖住右下角的门）
        门在场但步仍失败   8/30   ← 另两种，都看图核过（红框里就是发光的「城镇」门, BTN_OPEN_HOME d=2）
                                    ① 4 次 SEMANTIC_TARGET_NOT_VERIFIED：控件在帧上、模板 d=2，运行时却解析不出落点
                                       ⇒ 是**解析器层**的不一致（不是模板假阳性）
                                    ② 4 次 OPEN_HOME_NOT_PROVEN，其中 3 次 after 页=POPUP ⇒ tap 发出去了、客户端答以弹窗
```

三种成因各需单独一轮；本轮**未改代码**（没有实测的替代动作，也无法真机验证）。

## 修复：七个 OPEN_HOME 发出点补齐"先关搜索面板"

三处（MAIL/RESEARCH/TRAIN）早就有这个守卫，**四处没有**（EXPLORATION / DAILY / ALLIANCE / 兜底 HOME）
⇒ 补上同样的三行（各自理由名，satisfier `resource_search_closed`，`BACK` 与验证器都是现成的）。
触发证据来自运行时自己的日志：`20:26:44 OPEN_HOME` FAILURE → `20:27:15 BACK` SUCCESS
（`verifier_evidence={"search_closed": true}`）→ `20:28:16 OPEN_HOME` SUCCESS。

测试由 2 个目标扩到 7 个，并断言面板关掉后同一目标仍会开门。
A/B：去掉 brain 改动 → 该文件 6 失败；加上 → 2 失败，且这 2 个是**既存红项**
（`LEAVE_FOREIGN_LAYER` vs `SAFE_STOP`）。

## 红点变成优先级信号（第二份指令 §一 / §二②）—— 受控 A/B 回放取证据

接管时 AUTO 正在真机跑（`runtime_snapshot` 每秒更新、`GOAL_RUNNING`），**不抢设备**：这一轮
全程离线，用**生产帧回放**取证。

**先量后改（两个新工具）**：
- `tools/measure_mail_dot_blind_window.py`：1085 个 HOME before-帧里只有 40 帧带邮件行读数
  （红点层 2026-09-22 才落地），5 PRESENT / 35 ABSENT（276-290px，与表里 276-302 一致）。
  **"读完邮件说无事 → 15 分钟 TTL 内红点再现"在语料里一次都没出现**（唯一一次在 1841 分钟后）
  ⇒ §二①「新红点唤醒 Goal」**没有证据支持，本轮不做**（它需要一条会重复开页的规则）。
- `tools/measure_panel_row_badges.py`：38 个面板打开的帧里 12 帧带面板行台账。`RESEARCH` 行
  **9 PRESENT / 3 ABSENT**，且**只有它真正有区分度**（9 帧里它亮、别的行不亮）；矛兵/射手
  从未见 PRESENT；`联盟捐献/英雄招募/我的奖励` 三行 12/12 全是 UNKNOWN（读不出来 ⇒ 没信号）。

**改动**：`entry_badges.dot_varies`（读表新增的 `dot_variability` 段）+ `dots_pointing_at`
（只认 PRESENT × 会被观察到的角标 × 绑定到 Goal）；`goal_utility.RED_DOT_BONUS = 60` 进唯一排序层，
`UtilityBreakdown` 加 `red_dot`/`red_dot_on`，决策日志会写出是哪个入口在指。

**顺带修掉一个真实绑定缺陷**：面板「科技研究」行的红点绑的是 `"RESEARCH"` —— 那是**路由域**不是
Goal id，而这一行是面板红点信号的主体 ⇒ 原本大半信号落不到任何 Goal 上（生产记录里该行的
`goal` 字段就是 `"RESEARCH"`，可见）。

**证据（`tools/replay_red_dot_priority.py`，同帧同代码只切 `red_dots`）**：12 张生产帧 ⇒
处理组 **8/8 换板首**（邮件 180→240 登顶、科研 180→240 登顶、盾兵 80→140），负向对照 **0/4**。
归档 `dataset/truth_audit/red_dot_priority_20260923/`。

**回放本身暴露的两件事**：① `episodes.jsonl` 由运行时**并发追加**，读到半行是常态
（6734 → 6754 行、偶发一行 JSONDecodeError）⇒ 工具要**统计并报告**跳过的行数；
② 2 帧"记录说没红点"用今天代码重读**读到了红点** —— 旧记录写在 #98 角标窗口修复之前
⇒ 回放要**按图片分组**，记录只用来挑帧（#98 的效果因此在数据里可见）。

**仍未做**：§二③ 保护"最后一步"（只有 60 分界间接保证）、④ 未知红点有界探索、⑤ 已完成待领取
优先级（受 #95 B 制约）、⑥ 真机计时验收；§三 其它入口分支与 §四 结算词汇。

## 顺手修掉专家包里的死路径（每次开工都会踩）

`winter-agent-v2-dev` 专家包（`C:/Users/xhw/.workbuddy/plugins/cache/my-experts/winter-agent-v2-dev/1.0.0/`）
的 4 个技能文件里有 **31 处**把项目 venv 写成 `E:/dongri-mumu-bot/.venv/Scripts/python.exe` —— 该路径
**实测不存在**（2026-09-20 已迁移到 `E:\无尽冬日智能体\.venv`），照抄命令会直接失败。
已全部改为 `E:\无尽冬日智能体\.venv\Scripts\python.exe`，并在环境铁律表里写明旧目录已废、
补上托管 Python 3.13 的用途（`tools/git_sync.py` / `tools/cq.py` 这类纯逻辑脚本）。
⚠ **这是插件缓存目录**：专家包若被重新安装/同步，这处修复可能被覆盖 ⇒ 下次开工先用
`E:\无尽冬日智能体\.venv\Scripts\python.exe` 试一条命令，不要照抄技能里的路径。

## 假失败理由：四个闸门共用一个"帧上没有控件"（issue #103）

近 12h 失败榜第一名是 `SEMANTIC_TARGET_NOT_VERIFIED`（54 次），其中 **`TRY_ORDINARY_CONTROL` 29 次**
——一个最近才进来的技能。查下去发现它的 32 次失败里 **21 次理由是假的**。

**定性过程**（全部离线，两个新探针）：
- `tools/probe_ordinary_control_failures.py`：在失败帧上重跑读取器 ⇒ 那些帧**画着把手**
  （`state_before.quick_panel.handle` 就在，COLLAPSED，面板关闭），而**没有任何白名单词**。
- `tools/probe_ordinary_control_resolution.py`：用**生产解析器**在同帧上重跑，只切一个变量
  （`_ordinary_tried` 里有没有 `(HOME, QUICK_PANEL_HANDLE)`）：空 → 返回落点 `(0.0181,0.4301)`；
  有 → 返回 None。**同帧同代码，一个变量。**
- episode 侧的分组：**第 1 次尝试 50 次里 13 失败，第 2 次及以后 19 次里 18 失败**。

**根因**：声明控件层四个闸门（本 run 已用过 / 记录不服务这个 goal / 记录不列这个页 / risk 不可探索）
**都只 `return None` 不出声**，`executor.py:78` 一律压成 `SEMANTIC_TARGET_NOT_VERIFIED`，
写进 episode。连带：候选收集器按 "unlocated" 归档一条**假**候选；`escalation_queue` 按后缀把它分类成
`UNKNOWN_UI`（"客户端显示了 V2 读不懂的东西"）⇒ 会请开发 agent 修一个**没有东西读不懂**的问题。

**修法（只改"怎么记"，不改"点哪里"）**：`_ordinary_declined` 每次解析清空，四闸门各按名字记；
新 `_failure_type_from()` 在执行器说 `SEMANTIC_TARGET_NOT_VERIFIED` **且本步什么都没解析出来**时用那个名字。
结果：**理由为假的失败 21 → 0**；剩 11 条仍报该字符串，**全部 `handle=False`**（UNKNOWN/TRAINING 页）⇒ 它们是真的。

**新发现的知识缺口（未修）**：10 条 `NOT_FOR_THIS_GOAL` 说明 `QUICK_PANEL_HANDLE.related_goals` 只列
TRAIN/RESEARCH，而 `brain.PANEL_ROWS_FOR_ROUTE` 给 ALLIANCE/DAILY 也分配了面板行 ⇒ 那张表比面板实际
被使用的范围窄。扩宽它 = 设备上多 10 次面板打开，属行为改动，需真机验收。

## 守卫的覆盖范围：第八个发出点（issue #101 第二轮，六分钟死循环）

`OPEN_HOME` 的守卫（`c522bc9`，`20:41:32Z`）落地 **40 分钟**后，同类失败又成串出现 **8 次**
（`21:22:27`–`21:27:57Z`，全部 `search_open=True`），其中**七次是"一步长"的运行**：开局→回城→失败→
结束→面板 30 秒后重启到同一状态 ⇒ **六分钟死循环**，直到一次无关的 `SCAN_MAP_FOR_BEAST` 平移才关掉面板。

**根因**：发出点有**八处**——`brain.py` 七处（全绑具名 goal、都已加守卫）+ **运行时一处**：
`LiveRuntime._deferral_replan`（`runtime.py:651`，运行时唯一的 Decision）。它的触发条件正是"这张地图上
无事可做"，而"无事可做"最常见的成因就是上一轮把面板留在地图上 ⇒ **最需要守卫的地方没有守卫**。
⇒ 教训：**给某个 skill 加守卫时，要按 skill id grep 全包**，不要只看大脑（决策不止一个发出者）。

**证否"这条不是大脑发的"**用的是条件排他：同一帧（生产视觉+生产大脑）在 `current_goal` 为
`AUTO_DISCOVERY`/`""` 时大脑答 `SELECT_RESOURCE`，且大脑七处没有一处在 goal-less 运行时能发声。
之所以只能排除法，是因为 **episode 流里没有 `decision.reason`**（它只进 worker stdout，随日志滚动消失）
⇒ 已给 `Episode` 加上 `decision_reason` 字段，下一次直接从语料读得出。

测试直接测函数（不跑一轮）："这轮有没有可选 goal"取决于机器的公平台账，该类已有两个用例是那个 moving set
的受害者。A/B：只 stash `runtime.py`+`learning.py` ⇒ 12 失败 vs 11 失败（差的那条就是新用例，无修时按设计失败）。

## 24% 的运行是纯空转：两个方向各自都对，合起来是个环（issue #104）

先回头看**上一轮那两处改动有没有生效**：`decision_reason` 仍未出现在语料里（改动后还没跑过轮次），
但 `ORDINARY_CONTROL_ALREADY_USED_THIS_RUN` **已经落在真机语料里**（运行 `20260923_060723_868796`
step 18，`09-22T22:15:28Z`，page HOME、handle 正画在帧上、`point_norm=(0.0181,0.4301)`）⇒ #103 那一半
从「离线」升级为 **Live**。注意它记的 `repo_revision` 是 `de55d6a` —— **工作树里尚未提交**的那次改动
就已经跑在设备上，所以「帧↔提交按时间对不上时不要下结论」这条纪律同样适用于**提交号**本身。

**这一轮的主线**：量「近 6h 的 70 个失败」时先看运行形态，发现 **18/75 个运行（自 16:00Z）全部只由
`OPEN_MAP`/`OPEN_HOME`/`CHECK_MARCH` 组成**；`goal=AUTO_DISCOVERY` 的 91 步里 79 步是导航
（`OPEN_MAP` 44 / `OPEN_HOME` 25 / `CHECK_MARCH` 10）。

**链条靠回放拿到**（`tools/replay_run_decisions.py`：生产视觉重建世界 + 生产大脑按运行顺序喂进
**同一个 brain 实例** —— 它的计数器就是运行内的状态机）：`20260923_062948_568906` **3/3 精确复现** ——
`brain.decide` / `first_ready_p0_skill`（HOME→MAP）→ `runtime._deferral_replan` /
`deferred_SPEND_STAMINA_ON_BEAST_left_nothing_to_do_here`（MAP→HOME）→ `brain.decide` /
`first_ready_p0_skill`（HOME→MAP，**与第一步逐字相同**）。即**大脑的无目标兜底与运行时的兜底跳转
互为反向、互不知情**。

**教训（可复用）**：`_replan_attempted` 的注释自称「so leaving cannot become a two-page ping-pong」，
而它只给**双向环的一侧**上界 ⇒ **给环的一半加上界，环还在**；缺的不是第二个界，是**记忆**。
看到同类「界」时问一句：它保护的是**一个动作**，还是**一对动作**？

**修法**：`LiveRuntime._stop_instead_of_looking_again` + 运行内 `_barren_pages` + 类常量
`PAGE_HOPS_THAT_ONLY_LOOK_FOR_GOALS`（只为找目标而存在的两个跳转）⇒ 目的地已是荒页就换成
`SAFE_STOP` / `every_page_this_run_was_fruitless`。四条边界各有测试。
**A/B（真实帧，只切守卫这一个变量）**：36 步 → 27 步，7 个运行提前诚实停止，逐条可核对。

**工具**：`tools/replay_run_decisions.py`（新增）；证据 `dataset/truth_audit/nav_pingpong_20260923/`；
测试 `tests/test_no_repeated_look_around.py`（13 项，含用**两页客户端状态机**驱动的整轮集成测试 ——
假客户端若忽略点击，就会让运行因为真实客户端给不出的理由绕圈）。
