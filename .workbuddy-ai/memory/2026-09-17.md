# 2026-09-17 — 训练路线跑通；建筑入口找到但没落地；情报领到了奖励

操作者要求：「建筑和训练优先落地，继续，情报记得及时做」。

## 训练（TRAIN）：`--goal TRAIN` 真机 4 跳全部 verifier OK，收敛 4 步（原 7 步），exit 0

路线的**决策与 verifier 早已存在**（`brain.py` 243–246 / 446–456、`VERIFIED_ATOMIC` 已绑 5 个、
`run_live --goal` 已收 `TRAIN`）。唯一断点是**模板全裁自 2026-09-08 的另一个账号**
（战力 5,708万 vs 82.6万、不同头像、城市镜头更远）⇒ `world.training` 永为空 ⇒ 分支不可达。
**本轮没改大脑、没改 verifier**，只换模板 + 收一处门禁。

注册的 6 条记录（`tools/register_power_route_templates.py`，全部取自真机帧）：
`BTN_OPEN_POWER_OVERVIEW`（只裁拳头图标）、`POPUP_POWER_OVERVIEW`（加成总览标题条）、
`BTN_OPEN_POWER_DETAILS`（实力详情按钮）、`POPUP_POWER_DETAILS`（实力详情标题条）、
`BTN_POWER_TROOP_IMPROVE`（部队实力 提升）、`BTN_OPEN_TRAINING_FROM_CAMP`（训练按钮，300×300 圆心）。

### 缺陷与**一处自纠**

⚠ **本轮第一版结论"战力入口是值依赖模板、必须重裁"是错的**（且随首个提交写进了 message）。
真相：`skills.py` 点的是 `BTN_OPEN_POWER_OVERVIEW_ICON`（**只裁图标的既有记录**），
**今天就能命中 d=4**（09-08 账号 d=10、门禁 12）—— "避开会变的数字"这件事**早就有人做过了**。
我为 `BTN_OPEN_POWER_OVERVIEW` 加的那条记录**没有动作引用**，已删除，改由测试钉住事实。
⇒ **教训：判定控件"坏了"之前，先找到动作真正指向的语义名。**（这条值得进 MEMORY）

真正的断点 = 四跳里的三个（`POPUP_POWER_OVERVIEW` / `BTN_OPEN_POWER_DETAILS` /
`POPUP_POWER_DETAILS` / `BTN_POWER_TROOP_IMPROVE`）+ 兵营菜单门禁。

**真实缺陷（属"同一控件多种外观"）：兵营聚焦浮层是动画**（引导手指 + 呼吸光圈）。
旧裁剪一次运行 7 帧只中 3 帧 ⇒ verifier **连刷 21 秒**（07:41:18→29→39），
而浮层寿命约 **2 秒** ⇒ 下一步观测时已消失 ⇒ **整条路线被迫重走**。
新裁剪 = **以训练按钮为圆心的 300×300**（点击落点 = 该记录 ROI 中心；
执行器只有 `TAP_SEMANTIC`、没有绝对坐标动作），7 帧全 0–8 ⇒ 第一次观测即通过 ⇒ **收敛 4 步**。
⇒ **教训：verifier 的刷新重试会吃掉瞬时 UI 的寿命；识别不稳时先看刷新花了多久。**

### 门禁 17 → 12（全语料 3522 帧，`tools/probe_camp_menu_gate.py`）

≤8 有 17 帧 / ≤12 有 22 帧 / ≤17 有 36 帧，**而 plain HOME 帧是 16** —— 落进 17 里。
⇒ 保留 17 会让普通城市帧报 `menu_open`，把训练点击送进城市。
≤12 那批**抽查 OCR 全是兵营菜单帧**；09-08 菜单帧（新裁剪 d=18）靠**它自己的旧记录 d=0** 仍命中，不丢召回。

## 建筑（BUILD）：入口找到，**故意不落地**

`建筑实力 提升`(605,550) → 聚焦 **民居1（3级）**，开出家具面板 + `升级`（费用 157.7万肉 / 当时 16.6万 ⇒ 禁用）。

**不落地的理由**：该面板 `page=UNKNOWN`、`building={}`，而 `vision.py` 的 `BTN_BUILD_UPGRADE` 分支
**写死** `{"id":"STOREHOUSE","level":26,"target_level":27}` ⇒ 一注册就会为**任何**被聚焦的建筑编造这套值，
而 `verify_building_upgrade` 正是按 `before.building` 判定 ⇒ **会验证通过一个没发生的事实**。
⇒ 先让建筑身份**从画面读**（问题 #21）。

## 情报（INTEL）：**两次运行都真拿到价值**，两次都倒在同一处弹窗识别

第二次补跑（离开第一页情报后）走得更远，值得记：

```
1 SELECT_INTEL_PIN        INTEL -> POPUP(野兽任务卡)   verifier OK
2 OPEN_INTEL_BEAST_TARGET POPUP -> BEAST              verifier OK
3 INTEL_BEAST_START_MARCH BEAST -> MARCH              verifier OK
4 DISPATCH_INTEL_BEAST    MARCH -> MAP                verifier OK ← 真派出一队（体力 237→227）
5 OPEN_INTEL              MAP -> INTEL                verifier OK
6 INTEL_CLAIM_REWARDS     INTEL -> POPUP              verifier OK ← 又领到一份（未试 pin 6→5）
7 DISMISS_DAILY_REWARD    -> INTEL                    verifier FALSE
```
⇒ **6 步真机动作、2 份奖励、1 次真派兵**，只有解除那一步判错；**缺陷复现两次**。

**根因已量清（不是"阈值没调好"）**：`POPUP_DAILY_REWARD_CURRENT` 的 ROI 是
`x 0.08 y 0.2 w 0.84 h 0.4` = **整个奖励格区域** ⇒ 对**任何来源**的奖励弹窗都命中
（两次情报弹窗实测 **d=6 / d=2**），而带来源信息的 `POPUP_GENERIC_REWARD_HEADER` 是 12/16；
`vision.py` 第 1046 行又把它排在通用表头**之前** ⇒ 报成 `DAILY_REWARD` ⇒ 跑每日的解除技能。
⇒ **该裁剪在结构上无法区分来源（不是太松，是问错了问题）**。
修法方向：来源只能由**目标上下文**决定（brain.py 221–232 已有该分支），
但**改前必须量"真每日奖励弹窗"那一侧**（若它走通用路径，`goal=None` 时会不会
`SAFE_STOP generic_reward_without_goal_context` 把奖励晾着）—— 本轮答不了。**禁止调阈值修。**

## 新增/改动文件

- 新工具：`tools/probe_power_route.py`（`--tap x,y` 逐跳、每跳留帧 + 全量 OCR token、
  `--allow-page` 显式声明"我认得这个页"、`--leave` 收尾 BACK）、
  `tools/register_power_route_templates.py`、`tools/probe_camp_menu_gate.py`。
- 新测试：`tests/test_power_route_templates.py`（14 项，含两总体门禁、值无关性、赢家 ROI 落点、
  BUILD 缺口、情报误读的钉住）。
- `tools/check_wiring.py`：新增 6 条大脑决策 + 5 条 verifier 绑定检查（原文件**对这条链路零覆盖**）。
- `vision.py`：只改了 `BTN_OPEN_TRAINING_FROM_CAMP` 的阈值 17→12（含两总体注释）。
- 证据：`dataset/truth_audit/power_route_20260917/`（`key/` 12 帧进公开仓库，其余约 19 帧留本地）。
- `.gitignore`：白名单 `power_route_20260917/key/**`。

## 未决问题新增 #21–#24

#21 建筑身份写死（阻塞 BUILD）· #22 情报弹窗误读 · #23 TRAIN 结束停训练页
（下一次换 goal 立刻 `goal_page_mismatch`，本轮亲历）· #24 `troop_type` 恒为 `INFANTRY`
（三个兵营页签的标题模板都命中）。

## 续：研究路线真机跑通（第二个 BLOCKED 目标打通）

操作者要求「继续实现其他未实现功能」，按数据挑到 `KEEP_RESEARCH_PRODUCTIVE`（四个 BLOCKED 之一，
且研究在操作者优先级清单里）。**与训练同一模式**——机制早已存在：

- `knowledge/skills/RESEARCH_RESEARCH.md` 第 38 行早已记录路线「顶部战力 → 实力详情 →
  科技实力 → 提升 → 科研所」（2026-09-04，当时是 30 级账号）；
- `skill_factory.GOAL_REQUIREMENTS["KEEP_RESEARCH_PRODUCTIVE"]` 早已点名 `("OPEN_RESEARCH","RESEARCH")`；
- `BTN_OPEN_RESEARCH` 语义 + 12 距离容差、`verify_research_started`/`verify_research_queue` 早已存在。

**断点两处**：
1. **旧 `BTN_OPEN_RESEARCH` 的 ROI 中心不在控件上**：中心 (536,840) vs 实测按钮六边形
   **435..520 × 855..910、(478,878)** ⇒ 即使匹配成功也点偏 ~65px（执行器点命中记录的 ROI 中心）。
   新裁剪 = 以按钮为圆心的 340×340（也吃掉引导手指动画）。三次正样本 0/8/8、负样本全不中（最近 28）；
   旧记录在真机帧 26–36（阈值 12）⇒ 保留无害。
2. **没人读"科研所菜单已打开"，RESEARCH goal 没有导航** ⇒ 只会以 `research_entry_not_verified` 结束。

真机 `--goal RESEARCH` exit 0，**4 跳全 verifier OK** → `SAFE_STOP research_page_no_startable_node`。
**没有声称能"开始"研究**（页上无可开始项、节点/花费读数不存在、`BTN_START_RESEARCH` 零模板）；
停止是**具名的**，不是静默落空。

**新增**：`tools/register_research_route_templates.py`、`tests/test_research_route_templates.py`（10 项）、
`check_wiring` 9 条（含"TRAIN 不会走到研究那一跳"的目标隔离检查）、
`skills.py` 两个导航技能（`NAVIGATE_RESEARCH_LAB`、`OPEN_RESEARCH`）、
verifier 两条（`verify_research_lab_focused`、`verify_research_page_open`）并绑定、
brain 的 RESEARCH 路由 + 把 `加成总览 → 实力详情` 那一跳按 goal 复用（不再是 TRAIN 专属）。
注册表 88 → **90**。

⚠ 城市帧里**不写** `queue_available`（隔壁训练分支硬编码 `True`，goal library 会读成"队列空闲"——
那是谎言；本路线只写 `menu_open`）。**这条值得记：导航事实与队列事实不能混。**
