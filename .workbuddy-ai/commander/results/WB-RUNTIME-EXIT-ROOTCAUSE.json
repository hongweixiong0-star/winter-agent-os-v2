{
  "TASK_ID": "WB-RUNTIME-EXIT-ROOTCAUSE",
  "ROOT_CAUSE": "unexpected_worker_exits has TWO WRITERS in tools/control_panel.py, and they use different rules. _handle_worker_failure (line 1219) counts only  classification == 'WORKER_CRASH' and not fatal and not stop_requested, and its docstring promises that every counted crash has already been written to learning/control_panel/crashes/ with a full traceback. _handle_runtime_error (line 1250), the unclassified fallback for any unrecognised event kind, counts previous + (0 if fatal else 1) -- every non-fatal error regardless of cause, without looking at any classification. So environmental failures ARE excluded on the classified path and are NOT excluded on the fallback path. That asymmetry is the defect, and it also explains the number itself: the code promises a traceback for every counted WORKER_CRASH, no crash reports exist anywhere, and latest.log is 0 bytes -- so the historical total almost certainly came from the fallback path, not from real crashes.",
  "CHANGED_FILES": [
    "tools/update_workbuddy_handoff.py (KNOWN RISKS text: the old line only said pre-2026-09-14 exits have no traceback; it now states the two-writer asymmetry, that no crash report exists, and that the total must not be read as a crash count nor zeroed)",
    "tools/cq_runtime_exit_audit.py (NEW: the read-only audit)",
    ".workbuddy-ai/commander/REVIEW_REQUESTS.md (RR-001: the diagnosed fix lies in tools/control_panel.py, which this order's files_allowed_to_modify excludes)",
    "not changed, deliberately: tools/control_panel.py (out of scope and named in do_not_touch), the historical counter, watchdog semantics"
  ],
  "TEST_RESULT": "pytest tests/ -q on the final tree: see the suite recorded in this session's transcript. Targeted: tools/check_wiring.py => problems: 0 with all five sweeps (package, tools/, tests/, plus the two in-package sweeps). tools/update_workbuddy_handoff.py => exit 0 and tools/verify_handoff.py => exit 0, so the regenerated handoff still satisfies its own invariants. tools/truth_audit.py => episodes 1284, evidence_integrity PASS.",
  "LIVE_ATTEMPTS": 0,
  "LIVE_SUCCESS": 0,
  "LIVE_FAILURE": 0,
  "BEFORE": "unexpected_worker_exits=15 with watchdog_restart_count=13 and last_fatal_error=null, and the handoff described it only as 'before 2026-09-14 has no traceback and cannot be attributed'. Nothing recorded what the number meant, who wrote it, or whether the counter can distinguish an emulator dropout from a worker defect.",
  "AFTER": "Stratified from evidence. (a) The counter is written ONLY by tools/control_panel.py, and that GUI is not running, so nothing has incremented it recently. (b) learning/control_panel/crashes/ does not exist and latest.log is 0 bytes, mtime 2026-09-14T03:45:35Z. (c) Across all 1284 episodes there is not one crash / FATAL_* / WORKER_* failure type. (d) Today alone produced 281 episodes with no new exits and no crash artefact, so the counter did not move while real work happened. Conclusion: the historical 15 are unattributable non-fatal interruptions, most likely counted by the unclassified fallback; new runs produce zero crashes. The handoff now carries that reading so no future session re-derives it.",
  "VERIFIER_RESULT": "Acceptance assessed clause by clause. NEW RUNS CRASH=0: met and evidenced -- the counter is unchanged at 15 across today's 281 episodes, no crash report exists, no fatal failure type appears in the corpus, and last_fatal_error is null. ENVIRONMENTAL FAILURES MUST NOT COUNT AS WORKER CRASHES: met on one path, NOT met on the other -- _handle_worker_failure excludes them correctly, _handle_runtime_error cannot distinguish them and counts every non-fatal error. This clause is therefore reported as partially satisfied rather than claimed. THE 72h BASELINE IS EXPLAINABLE: met -- the baseline is 15 historical, unattributable, non-fatal interruptions from a counter whose writer is not running, and it must not be zeroed because that would destroy the only pointer to the asymmetry. target_metric's second half ('classification fields complete') is NOT met: no per-cause field exists, and adding one requires editing tools/control_panel.py, which this order's files_allowed_to_modify excludes.",
  "EVIDENCE": [
    "out_runtime_audit.txt (snapshot, absence of crashes/ and of a non-empty latest.log, both writer locations, the last-6h touched files, and the crash/fatal failure-type count of none)",
    "tools/control_panel.py lines 1219-1258 (the two writers, quoted above)",
    "out_ho_r18_summary.txt (handoff regenerated exit 0, verify_handoff exit 0, check_wiring problems: 0)",
    "out_truth_audit_tail.txt (episodes 1284 / evidence_integrity PASS)",
    "learning/runtime_snapshot.json (updated_at 2026-09-15T15:16:47Z, unexpected_worker_exits 15)"
  ],
  "PATCH_STATUS": "DONE_REPORT_ONLY",
  "REMAINING_ISSUE": "The defect itself is NOT fixed and I did not try to fix it: the fix lives in tools/control_panel.py, which this work order's files_allowed_to_modify does not include, and the same order's do_not_touch names 'watchdog semantics without evidence'. Submitting it as RR-001 rather than editing an out-of-scope file is the contract being followed, not an omission. Two further limits: no classification field exists, so the counter cannot yet be split by cause; and because the control panel has not been running, none of this was observed in a live watchdog restart -- the stratification is from the code plus the absence of artefacts, and the first real watchdog event after any fix should be treated as the confirming observation.",
  "NEXT_RECOMMENDATION": "Hold the queue here and hand back. All READY orders are now finished or formally blocked: WB-0BA, WB-0BB, WB-0AW and WB-EXECUTOR-EVIDENCE-AUDIT are DONE, WB-0AZ is BLOCKED with its live half outstanding, WB-0AX is still WAITING_FOR_NATURAL_STATE, WB-GOAL-LOW-RISK-COVERAGE is still QUEUED by Codex, and this order is DONE as a report with RR-001 raised. Three items deserve Codex's next pass, in this order. First, RR-001: the two-writer asymmetry is small, well-evidenced and directly undermines the 72-hour gate that requires unexpected_worker_exits = 0, because a counter that counts environmental failures as crashes can never be driven to zero by fixing crashes. Second, WB-0AZ should be re-issued as WAITING_FOR_NATURAL_STATE (the blocked-card state has occurred naturally at least twice today) or paired with a decision on whether pressing 前往查看 on a power-blocked master bounty is an acceptable way to reach it. Third, the queue's own premises should be re-derived: two of its before_metrics ('large numbers of empty backend fields', 'provenance completeness unknown') described the historical corpus rather than the current write path, which was already 97% complete -- the orders would have been sharper if the metric had been scoped to recent episodes.",
  "recorded_at": "2026-09-15T15:18:47+00:00"
}
