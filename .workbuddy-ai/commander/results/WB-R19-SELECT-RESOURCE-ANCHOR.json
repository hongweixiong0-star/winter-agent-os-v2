{
  "TASK_ID": "WB-R19-SELECT-RESOURCE-ANCHOR",
  "REOPENED_AND_CLOSED": true,
  "PRIOR_STATE": "BLOCKED (2026-09-16T23:38). The blocker named a strip-geometry recalibration: 'the calibrated strip geometry (pitch 157 px) does not match the live panel (cells advance 145-150 px)'. This round found that claim to be WRONG, and found the real defect.",
  "ROOT_CAUSE": "The pitch was never wrong, and neither was the strip geometry. Measured on frames whose selected tab is KNOWN (resource_cells_20260914_120027, MEAT/WOOD/COAL/IRON selected in turn), the bracket advances by 157.0 / 158.0 / 157.0 px and its absolute positions match the configured nominal to within 1 px -- the earlier '145-150 px on live frames' figure came from eyeballing a scaled screenshot, and it is retracted. The real defect was ONE NUMBER: the template ceiling. `resource_tab_max_distance` was 6.0, and the two populations it sits between are correct = 0.00..6.49 and wrong = 10.65..25.09, so the ceiling was BELOW the largest value its own calibration frames produce. Two of those frames measure 6.45 and 6.49 and were being rejected by it. On the live failures the geometry was right all along: the bracket sits on tab index 1 at x=172, which predicts 生肉 (MEAT) at x=486, and 生肉 is visibly there -- the crop simply scored 6.49 against a limit of 6.0. A second, independent defect surfaced once the ceiling was fixed: the client draws NO bracket until a tab has been selected, so on a freshly opened panel there was no anchor at all and `resource_tab_offset` stayed None, which left the run unable to tap (target position unknown) OR scroll (the scroll branch requires an offset) -- SELECT_RESOURCE then failed against a target that was already on screen.",
  "CHANGED_FILES": [
    "winter_agent_v2/vision.py",
    "tests/test_resource_tab_anchor.py",
    "dataset/truth_audit/resource_strip_20260916/",
    "tools/cq_strip_geometry.py",
    "tools/cq_strip_gate_probe.py",
    "tools/cq_strip_gate_population.py",
    "tools/cq_strip_offset_scan.py",
    "tools/cq_strip_case_verify.py",
    "tools/cq_strip_next_frame.py",
    "tools/cq_strip_crop.py",
    "tools/cq_strip_archive.py"
  ],
  "PATCH_STATUS": "APPLIED_MINIMAL",
  "PATCH_SUMMARY": "Two changes, both one concept each. (1) The ceiling moves 6.0 -> 8.0, justified by measured populations rather than by making a case pass, and the stale 'active <= 1.2 / non-active >= 13.0' comment that made 6.0 look generous is corrected -- that pair described probing FROM the bracket, not the predicted positions the gate actually scores. (2) `_offset_from_tab_contents` locates the strip from the reviewed templates ALONE, used only on the paths where the bracket fails, so the offset and the identity become independent facts: a panel with no selection has a known geometry and an unknown selection. It requires at least two cells to agree and every scoreable cell to match its own template as the unique best, which a wrong offset cannot satisfy. The bracket remains the preferred anchor because it also names the tab. The identity margin gate is untouched at 4.0 and was never binding.",
  "TEST_RESULT": "tests/test_resource_tab_anchor.py: 21 passed. Two expectations changed from None to BEAST and GIANT_BEAST, and the change was justified by evidence that does NOT include the mechanism under test: scanning every offset and keeping those where the gatherable templates land on their own cells gives a single answer (+399), under which the bracket's x inverts to tab index 0.00 on one frame and 1.00 on the other. Both are non-gatherable, so the invariant that matters -- a GATHERABLE identity is never invented -- still holds. A new test recomputes both populations from the archived frames and fails if the ceiling stops separating them, in either direction. Two more tests pin the bracket-less case and its negative control (no panel -> no offset, no targets).",
  "REPLAY_RESULT": "Through the production `selected_resource` path: 6 calibration frames keep their known selection; 4 panel-absent frames still give identity None AND offset None; the three production failures resolve (two to GIANT_BEAST at offset 399, one to SAWMILL at 352.5); the bracket-less frame gives identity None with offset 89.0 and COAL reachable at 0.7812 (px 562, against a visibly-correct 煤矿 centre near 555). Cost of the content path when it runs: 105-119 ms, and it runs only when the bracket fails.",
  "LIVE_ATTEMPTS": 4,
  "LIVE_SUCCESS": 1,
  "LIVE_FAILURE": 1,
  "LIVE_DETAIL": "Run 1 (04:08-04:15Z, goal GATHER_RESOURCE): SELECT_RESOURCE FAILED at 04:09:40 with SEMANTIC_TARGET_NOT_VERIFIED -- that failure is what exposed the bracket-less case, and the frame is archived as bracketless_panel__live_runtime_step_007_before_20260916T040931067046.png. Run 2 (04:15Z, after the bracket-less fix): SELECT_RESOURCE SUCCEEDED, MAP->MAP, resource_selected None->COAL, verifier ok, capture MAA_MUMU_EXTRAS / recognition V2 / action MAA / executor HYBRID -- and the chain then continued through SUBMIT_RESOURCE_SEARCH, START_GATHER and DISPATCH_MARCH. Runs 3-5 produced no episodes: the brain answered SAFE_STOP 'reserved_march_for_stamina' at step 1 because a march is now out (marches=['MARCHING'], reserve_for_stamina=2). That is correct policy, and it is also why the START_GATHER A/B cannot be extended right now without recalling the march, which would be manufacturing state.",
  "BEFORE": "SELECT_RESOURCE: 57 attempts / 12 successes all-time; the gather chain never once reached START_GATHER (its 59 historical failures are ALL MARCH_PAGE_NOT_OPEN).",
  "AFTER": "SELECT_RESOURCE succeeded live and the full chain completed for the first time: SELECT_RESOURCE (COAL) -> SUBMIT_RESOURCE_SEARCH (resource_available True) -> START_GATHER -> DISPATCH_MARCH, four consecutive verifier passes.",
  "VERIFIER_RESULT": "PASS on all four steps of the completed chain: SELECT_RESOURCE, SUBMIT_RESOURCE_SEARCH, START_GATHER, DISPATCH_MARCH, each with verifier ok=true, each captured by MAA_MUMU_EXTRAS.",
  "EVIDENCE": [
    "dataset/truth_audit/resource_strip_20260916/README.md",
    "learning/episodes.jsonl (2026-09-16T04:15:03 / 04:15:22 / 04:15:34 / 04:15:47)",
    "out_r21_1h.txt (the two populations)",
    "out_r21_2g.txt (per-case classifier vs independent anchor)"
  ],
  "REMAINING_ISSUE": [
    "The 5 residual corpus positives named in the earlier report are no longer the frontier: the ceiling now sits between the measured populations, and the frames that were refused for the wrong reason resolve. What remains unmeasured is how often a live panel is bracket-less (it happens on a freshly opened panel, which is the common case for the first SELECT_RESOURCE of a run, so probably often).",
    "The content path costs ~110 ms and runs whenever the bracket fails. That is acceptable now but is worth watching if more frames turn out bracket-less than expected.",
    "The pitch claim in the previous report is formally retracted; anyone reading that report should read this one instead."
  ],
  "ACCEPTANCE": {
    "failure_still_safe_and_does_not_click": "MET -- panel-absent frames yield identity None and offset None, so no target exists and TAP_SEMANTIC refuses",
    "replay_plan_met": "MET -- production failures resolve, calibration frames unchanged, negatives refuse",
    "no_threshold_loosening_without_evidence": "MET -- the ceiling moved 6.0 -> 8.0 between two measured populations, and the populations are now themselves a test",
    "live_verifier_pass": "MET"
  },
  "NEXT_RECOMMENDATION": "The START_GATHER MAA/fallback A/B (WB-R19-START-GATHER-MAA-LIVE-AB) is now reachable and should be reissued when marches are free -- it currently has 1 MAA arm and 0 fallback arms, because the brain's reserve_for_stamina policy stops further gather dispatches while a march is out. No new work is needed to make it reachable; it needs time for the dispatched march to return, or a config change by Codex.",
  "recorded_at": "2026-09-16T04:17:53+00:00"
}
